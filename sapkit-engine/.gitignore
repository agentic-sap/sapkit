# 의존성 — 워크트리 공유 링크(심볼릭/정션)도 잡히도록 슬래시 없이 유지한다.
# 트레일링 슬래시 패턴은 링크를 매치하지 못해 `git add`에 딸려 들어간다.
node_modules

# 빌드 산출물
dist
coverage

# jest 원시 보고서 — 절대 경로·머신 사정이 섞여 있다. 접힌 결과
# (evidence/contract/results.json)만 커밋한다.
.jest-report.json

# 접속 정보 — 절대 커밋 금지 (CLAUDE.md 안전 규칙)
.env
*.env
