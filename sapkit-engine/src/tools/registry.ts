/**
 * 도구 등록점 (스캐폴드 마커).
 *
 * 이 파일은 **유일한 도구 등록 지점**이다. 개별 도구 작업은 이 파일을 건드리지
 * 않는다 — 각 도구 모듈은 자기 정의를 export 하기만 하고, 등록은 물결 말미의
 * 배선 단계가 일괄로 한다(멱등, 확인-후-추가).
 *
 * 서버 코어 작업이 여기에 실제 등록 프레임(도구 정의 타입 + 빈 목록)을 세운다.
 */

export const TOOL_REGISTRY_MARKER = 'sapkit-engine/tools/registry' as const;
