# HANDOFF 과거 기록 보관소 — 2026-09

> **무엇** — `HANDOFF.md` 재개점에서 내린 **2026-08-31 ~ 2026-09-03 마감 블록 3벌**:
> D-145(프롬프트 감사 회수) · D-146(공방 `/review` 삭제) · D-144(오프라인 `COMPLETE`
> 좁히기). **51줄.**
> **언제·어디서** — 2026-09-09, **D-147**(실사용 교훈 승격 4차)의 마감이 재개점을
> 다시 쓰면서 잘라 옮겼다. 옮기기 직전 `HANDOFF.md`는 **484줄**이었고 새 마감 블록을
> 얹으면 상한 500을 넘길 자리였다.
> **왜** — 웜 레이어 상한(`check-doc-size.mjs` · `HANDOFF.md` 500줄)은 판B가 세운
> 기계 규칙이고, 넘칠 때의 처방은 **삭제가 아니라 이사**다(D-135). 재개점에는 세 건의
> 요지와 이 파일로 가는 포인터만 남겼다.
>
> ⚠ **순수 이동이다.** 블록인용(`> `) 접두어와 절 제목·순서를 포함해 **한 글자도
> 바뀌지 않았다.** 다만 2026-08 보관소와 달리 § 번호는 애초에 없다 — 이 블록들은 구
> 본문의 절이 아니라 재개점 안의 마감 기록이라, 인용은 D-번호(D-144·D-145·D-146)로
> 걸린다. 그 번호의 정본은 여전히 `docs/reference/DECISIONS.md`(append-only)다.
>
> ⚠ **여기는 기록이므로 갱신 의무가 없다.** 살아있는 정본은 `HANDOFF.md`(재개점) ·
> `CLAUDE.md`(안전 규칙·게이트·문서 계약) · `docs/RUN-PLAN.md`(판 큐)다. 아래 서술의
> 수치·설치본 상태·「다음 작업」 지시는 **그것이 쓰이던 시점의 것**이라 현행이 아니다.
> 본문의 상대 링크도 고치지 않았다 — 그것도 「한 글자도 바꾸지 않는다」에 든다.

---
> ## ✅ 방금 닫힌 것 — D-145: 프롬프트 감사 회수 (2026-09-03 · 판 밖 집행)
>
> 사용자가 `/claude-api prompt-audit`으로 **프롬프트 표면 전체**를 감사시켰다. 결과는
> **대체로 깨끗**하다 — 옛 모델용 스캐폴드와 화석이 **0건**이고, 정책 층의 `MUST`/`NEVER`는
> 전부 실제 규정 제약이다. 고칠 것 넷 중 소유자가 **셋**을 집행시켰다.
>
> **⑴ 도구 설명 3종에 부재 계약을 실었다**(핵심). `GetPackageContents`·`ReadPackage`·
> `GetInactiveObjects`는 **없는 대상에 성공 모양으로 답한다**(D-133 실측). 절차 문서 세 곳이
> 이미 「믿지 말 것」을 적는데도 재발했으므로, 계약이 있는 자리에서 닫았다. 되뜰 수 없는
> 채록본은 손대지 않고 **덧말표**(`harness/old-surface/amendments.json`)를 새로 두어
> 「원문 + 덧말」을 조립해 문다 — 원문이 한 글자만 움직여도 ⓐ는 그대로 실패한다(돌연변이로
> 확인). **⑵ 페르소나 14파일의 부스터 40행**(`**MANDATORY**: Always read` → `Read` · 이유절
> 보존) **⑶ `create-program.md` 제목 5개의 강조 장식**(본문 무접촉).
>
> **메타데이터 0.10.1 → 0.10.2 (생성물 7종 재생성).** ⚠ **이것을 빠뜨릴 뻔했다** — 배포되는
> 내용물(번들 1.3.1 · 페르소나 · 절차)이 바뀌었는데 판을 안 올리면 **설치본과 레포의 판이
> 같아져 `plugin update`가 「이미 최신」으로 지나간다.** 실측으로 잡았다: 설치본 0.10.1이
> 번들 1.3.0 · 옛 부스터 40건 · 덧말 0건을 그대로 이고 있었다. **교훈: `interactive/` 아래
> 배포물이 바뀌면 판을 올리는 것까지가 한 세트다.**
>
> **곁들여 선재 결함 하나**: `gates/test-gates.mjs` ⑧-a·⑧-b가 CRLF 체크아웃(Windows)에서
> 변형을 못 걸어 **음성시험 4건이 조용히 무증거**였다(CI는 LF라 초록). 고쳐서 52/52.
>
> ⚠ **정직 유보 3종은 D-145에 있다** — SAP 실기 재확인 0 · 실효 미측정 · **독립 리뷰 없음**
> (기계 증거만으로 닫았다). ⑵·⑶은 회귀 위험이 있는 쪽이다(D-053이 단계 건너뛰기를 실측했다).
>
> **~~보류 1건~~ → 같은 날 닫혔다 (D-146).** 공방 `/review` 명령(`.claude/commands/review.md`)을
> **고치지 않고 지웠다.** 이 레포를 보고 쓴 파일이 아니었다 — 작성일이 그것이 참조하는
> `docs/ADR.md`의 생성일보다 **4일 빠르다**(붙여 넣은 범용 템플릿). 체크리스트 5개 중 3개가
> 없는 기준을 쟀다(은퇴 로그 · `CLAUDE.md`에 한 번도 없던 "CRITICAL 규칙" · 이 레포 기준이
> 아닌 「빌드 명령어」). 정본 리뷰 경로 둘(`sap-reviewer` · `review-checklist.md` 12항목)이
> 이미 있어 고치면 **세 번째 기준**이 설 자리였다. ⚠ **git 이력에 없던 파일이라 되돌릴 수
> 없다** — 전문은 D-146과 세션 스크래치패드에 남겼다. **배포 영향 0**(제품은 `skills`와
> `mcpServers`만 싣고 설치본에 `review.md` 0건 — 실측).
>
> ## ✅ 방금 닫힌 것 — D-144: 오프라인 `COMPLETE` 좁히기 (2026-08-31 · 판 밖 후속 집행)
>
> 판D가 소유자에게 올린 열린 결정(아래 판D 절의 ▶)을 소유자가 **좁히기**로 닫았다 —
> **오프라인 지선의 `COMPLETE`도 `unit_test`·`atc`가 `PASS`이거나 사유 있는 `SKIPPED`일
> 것을 요구한다**(MCP 지선의 Phase 8 행렬과 같은 급). 근거의 핵: 오프라인에서 `COMPLETE`에
> 닿게 하는 그 MCP 가용성이 시험·ATC도 가능하게 하므로 **요구를 올려도 도달 불가능해지는
> 환경이 없다**. 기록 시점은 **Phase 8, 반입 뒤**다(Phase 5의 자동 SKIP은 그대로 —
> 그 시점엔 객체가 서버에 없다). 고친 곳: `verification-offline.schema.json`(선택 칸
> `unit_test`·`atc` 신설) · `create-program.md`(Phase 5 부기 정정 + Phase 8 오프라인
> `COMPLETE` 조건) · `develop-abapgit.md`(Step 7) · `verification-policy.md`(carve-out) ·
> `interactive/DESIGN.md` §10-3 · 메타데이터 **0.10.1**(생성물 7종 재생성). **코드 변경 0** ·
> D-136 선례를 따른 판 밖 집행(새 D-결정 D-144가 먼저 섰다). **독립 리뷰 R-FAIL(차단 1 =
> 공방 HANDOFF의 낡은 「다음 작업」 지시 · 권고 3) → 전건 회수 → 재검토 R-PASS · 차단 0.**
> 같은 날 **설치본도 0.10.1로 갱신**(user·local 두 스코프 · doctor ③ 「설치본 = 레포」 —
> 아래 ▶ ⑴ 참조). ⚠ 정직 유보는 D-144에 —
> 이 좁히기 역시 SAP 실기 0이고, 첫 실사용이 시험대다.
