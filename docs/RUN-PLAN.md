# RUN-PLAN — 사다리 완주 판 큐 (판 순서·상태의 단일 정본)

> **지위**: renew 2차(사다리 완주)의 **판 큐 정본**. 전략 결정 = D-084(짓기 우선 ·
> SAP 증거는 최종 테스트 판으로 이연). 계획 근거 = `docs/BLUEPRINT.md`(사다리 ⑴~⑷ ·
> §3 무중단 교체 규칙). 이 문서는 **큐만** 든다 — 도구 증거 수는
> `sapkit-engine/TOOL-LEDGER.md`, 마일스톤 눈금은 BLUEPRINT M-표, 세션 상태는
> `HANDOFF.md`가 정본이다(수치 복제 금지).
>
> **왜 이 문서인가**: 판마다 dryforge `/ready`→`/go`를 돌리면 3-doc은
> `.dryforge/`(머신 로컬 · 비추적)에 아카이브돼 **다음 판이 맥락을 잃는다.**
> 판 사이의 맥락은 아카이브가 아니라 **이 파일(git 정본)** 이 든다 — 어느 머신,
> 어느 세션에서든 이 파일 하나로 "지금 어느 판, 다음 판이 무엇"이 복원된다.

## 사용법 — 판 하나의 수명

1. **시작**: `/dryforge:ready docs/RUN-PLAN.md 판N` — 이 파일과 판 번호를 입력으로
   넘긴다. ready가 해당 브리프를 읽고 그 판의 「열린 결정」을 물은 뒤 3-doc을 만든다.
2. 3-doc 승인 후 같은 세션에서 `/dryforge:go` 실행.
3. **판의 완료 조건에 이 표의 상태 갱신 + `HANDOFF.md` 재개점 갱신이 포함된다**
   (갱신까지가 작업의 일부). 굵직한 결정이 나왔으면 `DECISIONS.md` append.
4. 다음 판 = 표의 다음 `대기` 행. 순서를 바꾸려면 **이 표를 먼저 고치고** 시작한다.
5. 머신 참고: 새 머신에서 첫 판을 열기 전 `.dryforge/status.json` 존재를 확인한다
   (없으면 `{"initialized": true}`로 생성 — 없으면 ready가 신규 프로젝트로 오판한다).

## 불변 레일 (모든 판 공통 — 어기면 중단)

- **교체·고지 은퇴는 검증 뒤에만**(BLUEPRINT §3.2). **다만 「검증」의 정의가 바뀌었다**
  (**D-093 ⓐ**, 2026-08-19): ~~판7·판8은 판6 완료가 전제다~~ → **판7의 전제는 BLUEPRINT
  「검증 기준」 1~4**이고 M-표 완주가 아니다(**그 판7은 재명명 뒤 `판7-b`이고, 그 전제인 기준 2는 `판7-a`가 세웠다** — D-094 ⓕ). 판8의 전제는 종전대로 **⑶ 세 갈래 소진**이며
  판6.2가 그 마지막 조각(판4 템플릿 SAP 확인)을 닫았다.
- **은퇴는 교체와 같은 판에 하지 않는다**(D-093 ⓐ) — 교체 직후가 가장 되돌릴 일이 많은
  구간이라 **되돌아갈 자리를 남긴다**. `engine/` 은퇴는 교체 이후 별도 판이다.
- **SAP 증거는 파일이 센다**(`sapkit-engine/evidence/` · 대장) — "써보니 됨"은 증거가
  아니다. 기준을 낮추려면 별도 D-항목으로 낮춘다(판6 브리프 참조).
- 증거를 태우는 **순서 M1→M6은 불변**(D-081 · D-082 ④) — 이연됐을 뿐 재배열이 아니다.
- 안전 정책은 일정과 무관한 상시 정책 — P2 건별 승인 · DEV-only write ·
  연습 대상은 반드시 새로 만든다(픽스처는 PUBLIC 레포로 나간다).

## 판 큐

| 판 | 사다리 | 이름 | SAP | 상태 |
|---|---|---|---|---|
| 판1 | ⑵ | 검사기 `sapkit` CLI 자작 + 코퍼스 대조 | 불필요 | **완료** (2026-08-16 · **D-085** · `bd826187` 은퇴 커밋) |
| 판2 | ⑶-a | GPL 빈자리 처리 — **조회** 결정 + 집행 | 불필요 | **완료** (2026-08-16 · **D-086** · `33bcb290` 결정 기록 커밋) |
| **판3.x** | ⑶-b | copy 지식 치환 (반복 판 — 실전 검증분부터) | 불필요 | **완료** (2026-08-18 · **D-087~D-089** · 판3.1~3.6 · 명부 **170/170 소진** · 잔량 **0** — ⚠ **`copy` 범주 안에서의 완주**다) |
| **판3.7** | ⑶-b | **상류 잔존 56 재저작** — 페르소나 25 · 절차 16 · 어댑터 5 · 지식 5 · 정책 2 · 도구 3 | 불필요 | **완료** (2026-08-23 · D-109~**D-111** · 배치 3회 + 일괄 패스 2회 · **명부 56/56 완주 · 잔량 0** · 독립 리뷰 누계 4회 — 차단은 전부 **한 단어**(`explicitly`·`and`) · §3.5 표본 판정 = 체크 정당) |
| **판11** | ⑷ ㉮ | **고지 범위 정합** — 두 번 데인 자리(D-104 · D-106)의 재시도 · ⑷의 마지막 조각 | 불필요 | **완료** (2026-08-23 · **D-112** · **답은 좁히기가 아니라 「서브트리 유지 + 내역의 정확성」** — 상류로 남는 것이 파일이 아니라 **산재 계약행**이라 파일 목록이라는 형태 자체가 틀린 답이었다 · 착수 역방향 스캔 **40파일 · ~598행** · 고지 4곳 갱신 · **사다리 ⑷ 완료** · 끝그림 **①~③ 도달 · ④(브랜드)는 미완으로 명시**) · **회수 D-113**(2026-08-23 독립 리뷰 R-FAIL · 차단 2 = **잔존 총계가 §3.4 갈래만**(→ `server/` 밖 **~2,200행/~130파일**) · **「전부 재저작」이 27파일에 거짓** — **수치·문장 정정 · ⑷ 완료 판정은 유지** · 권고 5 채택 · **경량 재리뷰 대기**) |
| 판4 | ⑶-c | ZRSC4SAP 템플릿 20파일 재생성 (집필분) | 불필요 (SAP 확인은 판6) | **완료** (2026-08-18 · 자체 저작 **신 22파일 정본 전환** `zsapkit_oop_ex` 계열 · **D-090** · 옛 20파일은 판6 검증 뒤 제거) |
| 판5 | ⑴ | 엔진 잔여 코드 — M2 인증(UAA·JWT) + 교체 전 필수(D18·keyring CI) | 불필요 | **완료** (2026-08-18 · **D-091** · M2 **오프라인 절반** — M-표 승격 없음) |
| **판6.1** | ⑴ | **M1 증거 완주** — 재생 9종 + `CreateInclude` 실기 + C3 · 증거 전략 결정 | **필요 (attended)** | **완료** (2026-08-18 · **D-092** · 증거 대기 143→**126** · **M1 19종 전부 증거 있음**) |
| **판6.2** | ⑴·⑶ | **⑶-c 마감 + 저비용 확인 + 사다리 방향 확정** — 신 템플릿 SAP 확인 · 옛 20파일 제거 · `--env` · JWT 축 20종 · 교체 관문 실사 | **필요 (attended)** | **완료** (2026-08-19 · **D-093** · ⑶-c 갈래 마감 · 화면·GUI 상태 4종 **미확인**) |
| **판6.3** | ⑴ | **attended 실기 46종 전용 판** — `$TMP`에서 `Create*`→`Delete*` 왕복을 배치로 | **필요 (attended)** | **완료** (2026-08-20 · **D-098** · **34종 섰다 / 12종 이유와 함께 못 섰다** · 실사용 사이클 1회 성립 · 증거 대기 126 → 92(실기) → **30**(인하 62종) · **P4 흔적 2건 잔존**) |
| **판M2-a** | ⑴ | **M2 오프라인 배선 판** — destination/UAA 토큰을 기동 경로에 잇는다(D-091 ⓑ의 정지선을 뒤집는 **새 D-결정 필요**) | 불필요 | **완료** (2026-08-23 · **D-114** · **정지선을 뒤집지 않고 그랜트 성격으로 좁혔다** — `client_credentials` 자동 취득 · `authorization_code` 정지선 유지 · 진단 신설 4종 · 엔진 **1.1.0**(3,849,389 B) · 시험 32건 신설 · Basic 회귀 0 · 표면 **186 불변** · **독립 리뷰 R-PASS · 차단 0 · 권고 5 전건 처리**. ⚠ **M-표 M2 승격 없음** — 실 UAA 왕복 0) |
| **판M2-b** | ⑴ | **M2 attended 실접속 판** — BTP 계정이 생긴 뒤 | **필요 (attended)** | **완료** (2026-08-23 · **D-115** · BTP trial `TRL` 실접속 · **P1 read만**). **`client_credentials`로는 ABAP ADT가 열리지 않는다** — 토큰은 UAA가 200으로 내주지만 `user_name`이 없어 ADT 전 경로 **401**(`sap-authenticated: false`). **`authorization_code` 사용자 토큰은 ADT 전 경로 200** (systemID `TRL` · client 100 · refresh_token 발급 · redirect_uri `localhost:8080` 등록). ⚠ **M-표 M2 승격 없음** — 실접속을 세운 것은 **엔진 기동이 아니라 프로브**다. **결함 1 회수**: `MCP_DESTINATION_CONNECTED` 과대 주장 → 문구 수정 · 엔진 **1.1.1** |
| **판M2-c** | ⑴ | **M2 `authorization_code` 기동 배선** — 정지선(D-091 ⓑ → D-114 ⓑ)을 **착수 결정으로 다시 연다** | 불필요(배선) + **필요 (attended)**(확인) | **배선 완료 · attended 확인 대기** (2026-08-23 · **D-117**). **정지선을 명시 옵트인 `--auth-interactive`로 열었다** — 부르지 않으면 오늘과 한 글자도 다르지 않다. 콜백 노브 2종(기본 **`localhost:8080`** = D-115 실측 화이트리스트) · 취득은 `TokenSource.acquire` 훅으로 `client_credentials`와 같은 자리에서 합류 · **브라우저를 열지 않는다**(URL은 stderr) · 진단 **`CONNECTED` → `TOKEN_ACQUIRED`** 개명(`CONNECTED`는 실접속 확인 자리에 **예약 · 미방출**) · **`src/auth/**` 무수정**(기구는 판5가 이미 완성) · 시험 **+26**(4,225 → **4,251**) · 엔진 **1.2.0**(배선 시 3,856,996 B → **회수 후 3,858,719 B**) · **도구 표면 186/155 불변**. **attended 확인 성립**(**D-118**) — `GetSystemInfo` → `{"supported":true,"system_id":"TRL",…}` · **M-표 M2 `짓기 완료` 승격 · D15 해소**. ⚠ **`증거` 칸은 여전히 열려 있다**. **독립 리뷰 R-FAIL → 회수 완료**(**D-119** · 콜백 호스트를 loopback 리터럴로 조임 · 번들 **3,858,719 B** · 소스 핀 **`9f69479c`**) · **이월분은 판R이 봤다**(**D-121**) |
| **판R** | ⑴ | **이월 리뷰** — D-118(attended 확인) + D-119(회수)를 함께 보는 독립 리뷰 | 불필요 | **완료** (2026-08-23 · **D-121**) — **R-FAIL · 차단 1 · 권고 7**. **차단은 코드가 아니라 결정문 한 줄**(D-119 ⓐ가 허용 집합에 `::1`을 넣었다고 적었으나 코드·시험·번들은 거부한다 — 정정 append) · 권고 **4 채택**(문서) · **3은 판Z로 이월**(엔진 소스 변경이라 재번들·재핀을 **한 번으로 모은다**) · **코드 변경 0**. 리뷰 대상을 **먼저 5커밋으로 고정**하고 돌렸다(판M2-c가 데인 자리). 마감 실측 jest **4,259**(4,258 통과·1 skip · suite 245) — 커밋 메시지의 「4,260」은 **예상이었다** · 제품 게이트 7종 재현 **exit 0** |
| **판E1** | ⑴ | **증거 · 재생 대조 18종** — 녹화 → 재생 → 정규화 diff 0 | **필요 (attended)** | **완료 — 「이유와 함께 못 섰다」로 닫힘** (2026-08-23 · **D-122**). ⚠ **브리프의 전제가 무너져 있었다**: ① 재생기는 `fixtures/` **최상위만** 수집하는데(재귀 없음) 18종은 `attended-only/`에서만 태워졌다 ② 그런데 **요구 급 산식은 재귀로 배정**해 `replay` 급을 매겼다 — **수집 단위 ≠ 배정 단위** ③ 새 기준선은 `attended-guard`가 **무조건 거부**(자기 대조)하고 구 엔진은 판7.5에서 은퇴해 **만들 수단이 없다** ④ **접속 3곳 전부 막혔다**(KR-DEV TCP 미도달 · IDES-DEV 401 · IDEA-JNC 미시도) ⑤ **신규 결함** — `replay-attended.mjs`에 keyring `NODE_PATH` 배선 0(짝인 `record-attended.mjs`엔 있다). **증거 대기 30 그대로** — 이 판은 증거를 만들지 않았고 **왜 못 만드는지를 기록**했다. → ⚠ **①과 ⑤는 2026-08-24에 수리됐다**(재생 수집이 재귀로 18종에 도달 · keyring 배선 · 상세는 `HANDOFF.md` ⑸) — 이 행은 **판E1 당시의 판정**이다 |
| **판E2** | ⑴ | **증거 · attended 실기 12종** — **부류별 처방**(면제 2 · 엔진 수리 2 · SAP 측 고장 6 · 진단 2) | **필요 (attended)** | **완료 — 「이유와 함께 못 섰다」로 닫힘** (2026-08-23 · **D-122**). 12종은 **판6.3(D-098)에서 이미 이유와 함께 갈렸고** 이 판이 그 판정을 승계했다. 부류별 처방은 **하나도 집행되지 않았다** — ⓑ 거짓 음성 2종의 엔진 수리는 **진단이 실기 1회의 가설**이라 SAP 재현 없이 고치면 맞는지 증명할 수 없어 이월 · ⓐ 면제 2종의 재분류도 **산식을 고쳐야 해서** 하지 않았다(D-122 ⓑ의 기각 사유에 걸린다) |
| **판Z** | ⑷ ④ + 잡무 | **브랜드 단일 + 잡무 5건** — `sapkit-mcp-tools*.md` 파일명(참조자 **20** — 실사 정정) · `[SAPKIT …]` 훅 태그 3 + 접두어 11 · 별건 ⓐ~ⓔ | 불필요 | **완료 — 완주** (2026-08-23 · **D-123**). **끝그림 ④가 닫혔다.** 카탈로그 4파일 개명(생성기 8곳 정본 · **도구 목록 diff 0**) · 훅 마커 14 · 자기지칭 16 · `atomic-write` **배선**(자격증명 파일이 fsync 없는 복제본으로 쓰이던 자리) · 승인 파일명 통일 · executor 문장 수리(D-110 ⓒ 승계) · **판R 이월 3건**(R3·R6·R7 — 돌연변이로 red 확인) · 재번들 **1.2.1**(3,859,183 B · 핀 `ad393e60`). 게이트 **제품 9종 + 엔진 전종 + 음성 9종** 전부 exit 0 · jest **4,261 passed**(**이 판 증분 +3** — 직전 4,258) |
| **판7-a** | ⑴ | **교체 관문 배선** — 제품 게이트 2종에 대상 이름표 · B0 다중파일 재설계 · CI 상설화 · 이월 1순위(`verify-applied`) | 불필요 | **완료** (2026-08-19 · **D-094** · **기준 2 성립** · 음성시험 20→24) |
| **판7-b** | ⑴ | **교체(swap)** — 제품 번들을 sapkit-engine 산출물로 (구 `판7`) | 불필요 | **완료** (2026-08-19 · **D-095** · 번들 8.30MB → **3.81MB** · 엔진 **1.0.0** · 서버 결부 copy 39 판정 = **3 해소 · 36 잔존**) |
| **판7.5** | ⑴ | **`engine/` 은퇴** — 고지 2→1건 (교체가 안정된 뒤 · 되돌아갈 자리를 먼저 없애지 않는다) | 불필요 | **완료** (2026-08-22 · **D-101** · 은퇴 커밋 **`843a6aba`** + 재핀 `ac26030b` · 추적 파일 **1,604개 삭제 + 1건 이관** · 고지 **2→1** · **롤백이 한 걸음 → 두 걸음** · 구 트리를 채록본 2종으로 얼림) |
| **판8** | ⑷ | ~~`interactive/` 고지 은퇴 — 고지 0건~~ → **`interactive/server/` 잔존 차용 36건을 어떻게 할 것인가** | 불필요 (전제 셋 다 성립 — 판3.6 · 판6.2 · **판7-b 판정**) | **완료 · 회수됨** (2026-08-22 **D-103** → 2026-08-23 **D-104**). **판정은 선다** — ① 기각 · 36을 **동결 30 / 활성 6**으로 갈랐다 · **「고지 0건」을 끝그림에서 내렸다**. ⚠ **집행(② 고지 범위 좁히기)은 독립 리뷰 R-FAIL로 철회** — `class: transform` 42를 빠뜨렸다 |
| **판9** | ⑴ | **표면 재평가** — 꼬리 49종을 제품 표면에서 뺄 것인가 (D-079 ⑧의 「1회」 · **교체 직전 트리거가 지나가 재배치**) | 불필요 | **완료** (2026-08-23 · **D-105** · **전량 유지 — 빼지 않는다** · 「1회」 **소진** · 실측이 「세션이 무겁다」의 과녁을 **꼬리 → 설명문 밀도**로 옮겼다 · 표면 **186 불변**) |
| **판10** | ⑷ ㉯ | **활성 차용 6 소진** — `tool-catalog/` 4를 엔진 등록점 생성으로 · `verify-engine.mjs` 골격 재저작 · `bundle-keyring.mjs` 처분 판정 | 불필요 | **완료** (2026-08-23 · **D-107** · **활성 6 소진** — 생성물 전환 4 + 재저작 2 · 역방향 스캔 **잔존 0** · 「tool-catalog 자동 대조 게이트 부재」(판6.1 이월) **폐쇄** · 엔진 게이트 **5→6종** · 음성시험 **43→52** · 표면 **186 불변**(생성 목록 diff 0)). ⚠ **독립 리뷰 R-FAIL · 차단 1(2곳)** — 엔지니어링은 전부 재현됐고 **「생성물」 지위가 절차 정본 2곳에 미전파**였다 → **회수 완료** (2026-08-23 · **D-108** · 문서만 · **코드 변경 0**) |
| **판B** | — | **프로젝트 두뇌 처방 집행** — 훅층 수리 · 문서 다이어트 수술 · 줄 수 게이트 신설 · 설계서 편입 | 불필요 | **완료** (2026-08-24 · **D-135** · 수술 커밋 `17d7e75` · 게이트 `a3aaa1c`). `HANDOFF.md`를 **4,808줄**에서, 이 문서를 **1,964줄**에서 절단(§ 번호 보존 이사 — **절단 후 현재 값은 `check-doc-size.mjs`가 센다**) · 불변 규칙 **CLAUDE.md 단권화**(구 §8 7개 항 · 규범 유실 0) · `.claude/hooks/` **git 편입**(5+설정 · 머신 절대경로 0) + 백틱 가드·세션 훅 수리 · **줄 수 게이트 신설**(제품 게이트 목록 정본은 `CLAUDE.md`) · 설계서 편입. 독립 리뷰 **차단 0 · 권고 6**(회수 3건 `604012a`) |
| **판W** | — | **콜드 층 위키화** — 주제별 합성 페이지 + 콜드 색인 + 고아 검출 게이트 + 부기 규약 | 불필요 | **진행** (2026-08-25 착수 · **D-137** · 설계서 §2.11의 유예 해제) |

판1과 판2~4는 병행 가능하다(⑶은 코드 부품과 독립 — BLUEPRINT §2.1). 판5는 판6 전이면
아무 때나. 상태 값: `대기` / `다음` / `진행` / `완료(날짜 · 커밋)`.

**▶ 완주했다 (2026-08-23 · D-121 · D-122 · D-123).** D-120 ⓒ가 「판 4개면 끝」이라고
못박은 그 넷이 전부 닫혔다 — **판R**(이월 리뷰) · **판E1**(재생 18) · **판E2**(attended 12) ·
**판Z**(브랜드 + 잡무). **사다리 ⑴~⑷ 전부 완료**이고 **끝그림 ①~④ 도달**이다.

⚠ **「완주」는 「전부 검증됐다」가 아니다.** 증거 대기 **30종은 여전히 실 SAP에서 요구 급
증거가 없다.** 완주는 D-120 ⓓ의 정의(**「✅ 또는 이유와 함께 못 섰다」**)로 닫힌 것이고
그 30종은 **후자**다. 판E1이 착수 실측에서 밝힌 이유는 다섯 가지이고(재생기의 최상위 수집 ↔
요구 급의 재귀 배정이라는 **단위 불일치** · 새 기준선 제작 수단 소멸 · **접속 3곳 차단** ·
재생 러너의 keyring 미배선), 전부 **D-122**와 장부 「도구 결함 등재」 절에 있다.

**남은 것은 판이 아니라 부채 목록이다** — 정본은 `HANDOFF.md` 재개점의 「⚠ 남은 것 —
부채 목록이지 계획이 아니다」 절이다. 실수요가 생기거나 SAP 접속이 돌아올 때 여는
것이고, **새 판을 세우려면 새 D-결정부터**다.
⚠ **판B(2026-08-24 · D-135)는 그 예외가 아니라 그 규칙을 따른 것이다** — 완주 이후에
선 첫 판이고, 코드가 아니라 **판을 굴리는 문서 기관 자체**를 고쳤다. 새 D-결정(D-135)이
먼저 섰다. 그중 하나는 **코드 변경 0으로 시험할 수 있다** — `attended-only/`
픽스처를 `--fixture`로 직접 재생하는 갈래(픽스처는 엔진 **1.0.0**에 찍혔고 현행 엔진은
그보다 여러 판 뒤라 **자기 대조가 아니라 버전 간 회귀 대조**다 — 현행 버전의 정본은
`HANDOFF.md` 📌표 · 18종 전부가 픽스처 6편 안에 있다).

▶ **다음 판 후보가 하나 서 있다 — 아직 큐에 넣지 않았다.** 「제품을 사용자 프로젝트에서
단독으로 쓸 때의 세션 간 연속성」이고, **갈래 결정(sapkit 자체 / aegis 위임)이 먼저**라
아직 판이 아니다. 실측·갈래·제약·빠진 조각은 **`HANDOFF.md` 재개점 맨 위 「다음 작업」**에
있다. 갈래가 정해지면 새 D-결정 → `/dryforge:ready` 순으로 이 표에 행이 선다.

**판별 브리프(판1~판11 · 1,880줄)는 `docs/reference/run-archive/RUN-PLAN-briefs-2026-08.md`로
옮겼다** (2026-08-24 · 판B) — 이 문서는 이제 **큐와 규약만** 든다.
