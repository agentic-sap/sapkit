# RUN-PLAN — 사다리 완주 판 큐 (판 순서·상태의 단일 정본)

> **지위**: renew 2차(사다리 완주)의 **판 큐 정본**. 전략 결정 = D-084(짓기 우선 ·
> SAP 증거는 최종 테스트 판으로 이연). 계획 근거 = `docs/BLUEPRINT.md`(사다리 ⑴~⑷ ·
> §3 무중단 교체 규칙). 이 문서는 **큐만** 든다 — 도구 증거 수는
> `sapkit-engine/TOOL-LEDGER.md`, 마일스톤 눈금은 BLUEPRINT M-표, 세션 상태는
> `HANDOFF.md`가 정본이다(수치 복제 금지).
>
> **왜 이 문서인가**: 판마다 dryforge `/ready`→`/go`를 돌리면 3-doc은
> `.dryforge/`(머신 로컬 · 비추적)에 아카이브돼 **다음 판이 맥락을 잃는다.**
> 판 사이의 맥락은 아카이브가 아니라 **이 파일(git 정본)** 이 든다 — 어느 머신,
> 어느 세션에서든 이 파일 하나로 "지금 어느 판, 다음 판이 무엇"이 복원된다.

## 사용법 — 판 하나의 수명

1. **시작**: `/dryforge:ready docs/RUN-PLAN.md 판N` — 이 파일과 판 번호를 입력으로
   넘긴다. ready가 해당 브리프를 읽고 그 판의 「열린 결정」을 물은 뒤 3-doc을 만든다.
2. 3-doc 승인 후 같은 세션에서 `/dryforge:go` 실행.
3. **판의 완료 조건에 이 표의 상태 갱신 + `HANDOFF.md` 재개점 갱신이 포함된다**
   (갱신까지가 작업의 일부). 굵직한 결정이 나왔으면 `DECISIONS.md` append.
4. 다음 판 = 표의 다음 `대기` 행. 순서를 바꾸려면 **이 표를 먼저 고치고** 시작한다.
5. 머신 참고: 새 머신에서 첫 판을 열기 전 `.dryforge/status.json` 존재를 확인한다
   (없으면 `{"initialized": true}`로 생성 — 없으면 ready가 신규 프로젝트로 오판한다).

## 불변 레일 (모든 판 공통 — 어기면 중단)

- **교체·고지 은퇴는 검증 뒤에만**(BLUEPRINT §3.2). **다만 「검증」의 정의가 바뀌었다**
  (**D-093 ⓐ**, 2026-08-19): ~~판7·판8은 판6 완료가 전제다~~ → **판7의 전제는 BLUEPRINT
  「검증 기준」 1~4**이고 M-표 완주가 아니다(**그 판7은 재명명 뒤 `판7-b`이고, 그 전제인 기준 2는 `판7-a`가 세웠다** — D-094 ⓕ). 판8의 전제는 종전대로 **⑶ 세 갈래 소진**이며
  판6.2가 그 마지막 조각(판4 템플릿 SAP 확인)을 닫았다.
- **은퇴는 교체와 같은 판에 하지 않는다**(D-093 ⓐ) — 교체 직후가 가장 되돌릴 일이 많은
  구간이라 **되돌아갈 자리를 남긴다**. `engine/` 은퇴는 교체 이후 별도 판이다.
- **SAP 증거는 파일이 센다**(`sapkit-engine/evidence/` · 대장) — "써보니 됨"은 증거가
  아니다. 기준을 낮추려면 별도 D-항목으로 낮춘다(판6 브리프 참조).
- 증거를 태우는 **순서 M1→M6은 불변**(D-081 · D-082 ④) — 이연됐을 뿐 재배열이 아니다.
- 안전 정책은 일정과 무관한 상시 정책 — P2 건별 승인 · DEV-only write ·
  연습 대상은 반드시 새로 만든다(픽스처는 PUBLIC 레포로 나간다).

## 판 큐

| 판 | 사다리 | 이름 | SAP | 상태 |
|---|---|---|---|---|
| 판1 | ⑵ | 검사기 `sapkit` CLI 자작 + 코퍼스 대조 | 불필요 | **완료** (2026-08-16 · `bd826187` 은퇴 커밋) |
| 판2 | ⑶-a | GPL 빈자리 처리 — **조회** 결정 + 집행 | 불필요 | **완료** (2026-08-16 · `33bcb290` 결정 기록 커밋) |
| **판3.x** | ⑶-b | copy 지식 치환 (반복 판 — 실전 검증분부터) | 불필요 | **완료** (2026-08-18 · 판3.1~3.6 · 명부 **170/170 소진** · 잔량 **0** — ⚠ **`copy` 범주 안에서의 완주**다) |
| **판3.7** | ⑶-b | **상류 잔존 56 재저작** — 페르소나 25 · 절차 16 · 어댑터 5 · 지식 5 · 정책 2 · 도구 3 | 불필요 | 대기 — **2026-08-23 신설**(D-104 ⓔ · **D-106 ⓐ가 42 → 56으로 고쳤다**). 어느 명부도 세지 않던 자리였다 · 명부 `copy-baseline.md` **§3.4** · **§3.5 이월분 표본 판정**도 이 판 몫 |
| 판4 | ⑶-c | ZRSC4SAP 템플릿 20파일 재생성 (집필분) | 불필요 (SAP 확인은 판6) | **완료** (2026-08-18 · 자체 저작 **신 22파일 정본 전환** `zsapkit_oop_ex` 계열 · **D-090** · 옛 20파일은 판6 검증 뒤 제거) |
| 판5 | ⑴ | 엔진 잔여 코드 — M2 인증(UAA·JWT) + 교체 전 필수(D18·keyring CI) | 불필요 | **완료** (2026-08-18 · **D-091** · M2 **오프라인 절반** — M-표 승격 없음) |
| **판6.1** | ⑴ | **M1 증거 완주** — 재생 9종 + `CreateInclude` 실기 + C3 · 증거 전략 결정 | **필요 (attended)** | **완료** (2026-08-18 · **D-092** · 증거 대기 143→**126** · **M1 19종 전부 증거 있음**) |
| **판6.2** | ⑴·⑶ | **⑶-c 마감 + 저비용 확인 + 사다리 방향 확정** — 신 템플릿 SAP 확인 · 옛 20파일 제거 · `--env` · JWT 축 20종 · 교체 관문 실사 | **필요 (attended)** | **완료** (2026-08-19 · **D-093** · ⑶-c 갈래 마감 · 화면·GUI 상태 4종 **미확인**) |
| **판6.3** | ⑴ | **attended 실기 46종 전용 판** — `$TMP`에서 `Create*`→`Delete*` 왕복을 배치로 | **필요 (attended)** | **완료** (2026-08-20 · **D-098** · **34종 섰다 / 12종 이유와 함께 못 섰다** · 실사용 사이클 1회 성립 · 증거 대기 126 → 92(실기) → **30**(인하 62종) · **P4 흔적 2건 잔존**) |
| 판M2-a | ⑴ | **M2 오프라인 배선 판** — destination/UAA 토큰을 기동 경로에 잇는다(D-091 ⓑ의 정지선을 뒤집는 **새 D-결정 필요**) | 불필요 | 대기 |
| 판M2-b | ⑴ | **M2 attended 실접속 판** — BTP 계정이 생긴 뒤 | **필요 (attended)** | 대기 — **BTP 계정 의존**(사용자 결정) |
| **판7-a** | ⑴ | **교체 관문 배선** — 제품 게이트 2종에 대상 이름표 · B0 다중파일 재설계 · CI 상설화 · 이월 1순위(`verify-applied`) | 불필요 | **완료** (2026-08-19 · **D-094** · **기준 2 성립** · 음성시험 20→24) |
| **판7-b** | ⑴ | **교체(swap)** — 제품 번들을 sapkit-engine 산출물로 (구 `판7`) | 불필요 | **완료** (2026-08-19 · **D-095** · 번들 8.30MB → **3.81MB** · 엔진 **1.0.0** · 서버 결부 copy 39 판정 = **3 해소 · 36 잔존**) |
| **판7.5** | ⑴ | **`engine/` 은퇴** — 고지 2→1건 (교체가 안정된 뒤 · 되돌아갈 자리를 먼저 없애지 않는다) | 불필요 | **완료** (2026-08-22 · **D-101** · 은퇴 커밋 **`843a6aba`** + 재핀 `ac26030b` · 추적 파일 **1,604개 삭제 + 1건 이관** · 고지 **2→1** · **롤백이 한 걸음 → 두 걸음** · 구 트리를 채록본 2종으로 얼림) |
| **판8** | ⑷ | ~~`interactive/` 고지 은퇴 — 고지 0건~~ → **`interactive/server/` 잔존 차용 36건을 어떻게 할 것인가** | 불필요 (전제 셋 다 성립 — 판3.6 · 판6.2 · **판7-b 판정**) | **완료 · 회수됨** (2026-08-22 **D-103** → 2026-08-23 **D-104**). **판정은 선다** — ① 기각 · 36을 **동결 30 / 활성 6**으로 갈랐다 · **「고지 0건」을 끝그림에서 내렸다**. ⚠ **집행(② 고지 범위 좁히기)은 독립 리뷰 R-FAIL로 철회** — `class: transform` 42를 빠뜨렸다 |
| **판9** | ⑴ | **표면 재평가** — 꼬리 49종을 제품 표면에서 뺄 것인가 (D-079 ⑧의 「1회」 · **교체 직전 트리거가 지나가 재배치**) | 불필요 | **완료** (2026-08-23 · **D-105** · **전량 유지 — 빼지 않는다** · 「1회」 **소진** · 실측이 「세션이 무겁다」의 과녁을 **꼬리 → 설명문 밀도**로 옮겼다 · 표면 **186 불변**) |
| **판10** | ⑷ ㉯ | **활성 차용 6 소진** — `tool-catalog/` 4를 엔진 등록점 생성으로 · `verify-engine.mjs` 골격 재저작 · `bundle-keyring.mjs` 처분 판정 | 불필요 | 대기 — **다음**. **전제 성립**(판8이 과녁을 확정했다 · D-103 ⓔ). 「tool-catalog 자동 대조 게이트 부재」(판6.1 이월)를 **같은 작업이 함께 닫는다** |

판1과 판2~4는 병행 가능하다(⑶은 코드 부품과 독립 — BLUEPRINT §2.1). 판5는 판6 전이면
아무 때나. 상태 값: `대기` / `다음` / `진행` / `완료(날짜 · 커밋)`.

## 판별 브리프

### 판1 — ⑵ 검사기 `sapkit` CLI 자작 — **완료 (2026-08-16)**

**도달점**: `sapkit-cli/`(TypeScript · 런타임 외부 의존 0)가 구 `vsp/`의 로컬 검사를
대체했고, 그 번들이 `interactive/checker/`로 제품에 동봉돼 **설치가 완전 오프라인**이
됐다. `vsp/` 서브트리 **570파일**과 배포 경로가 삭제됐고(은퇴 커밋의 총 삭제 577 =
서브트리 570 + `adapters/vsp/` 4 + `interactive/` 3) **고지 3→2**. 결정 기록 = D-085.

- 판정 동등성: 커밋 코퍼스 47파일 × 세 표면(lint·parse·analyze) **갈림 0**, 광역
  대조(구 vsp 내장 샘플 25 + 레포 전체 `.abap` 174)도 **갈림 0**. 독립 리뷰어가 구
  vsp를 **직접 빌드해 기준 파일을 보지 않고** 재도출한 결과도 같았다.
- 구 판정은 이제 다시 뜰 수 없다 — `sapkit-cli/fixtures/baseline/`(141칸)과
  `harness/RECORDING.md`가 그 유일한 잔존 형태이고, 코퍼스 대조 게이트가 상시 지킨다.
- 의도적 차이는 `sapkit-cli/DIVERGENCES.md`(append-only)에 D-001~D-010. **등재 없는
  차이는 결함**이라는 규율은 엔진과 같다.
- 새 게이트 셋: 코퍼스 대조(`sapkit-cli/gates/`) · `verify-checker.mjs`(번들 무결성·
  출처) · 각각의 음성시험. CI에 `sapkit-cli` 잡이 섰다.

**후속 판이 이어받는 것** — `docs/DESIGN.md` 본문(§1 좌표표·§3·§16 등 vsp를 검증
백엔드로 부르는 서술 전반)은 현행성 배너로만 막아 뒀다. 그 문서는 R1 이전 서술이라
이미 부분적으로 역사 취급이므로 전면 재작성은 별도 판단이다. **셈법마다 달라지는
수치를 여기 박지 않는다** — 행 수·출현 수·`vsp-custom` 포함 여부로 값이 흔들린다.

### 판2 — ⑶-a GPL 빈자리 결정 + 집행 — **완료 (2026-08-16)**

**도달점**: 삭제된 GPL 31파일(2026-08-09 커밋 `54fd84de` · 결정 기록은 D-074 · 08-10)
자리를 **자체 집필이 아니라 조회로** 처리하기로 확정하고 집행했다. 문법 사전은 제품에
다시 싣지 않는다 — 제품에 남는 참조 지식은 실전 검증 함정 모음
(`knowledge/abap/conventions/` 21편)이다. 결정 기록 = **D-086**. **코드 0줄 · 문서만.**

- **조회처 = 이미 동봉된 help.sap.com 조회 체계**(`interactive/core/procedures/help-portal-fetch.md`
  + `interactive/tools/fetch/` 2종). 새로 만든 배선이 없고 **소비자만 늘렸다** — 그래서
  판2가 새로 여는 외부 노출은 0이고, 수요 지점에는 **레포 내부 상대 링크만** 남는다.
- **집행 6곳**: `docs/reference/DECISIONS.md`(D-086 append) ·
  `interactive/THIRD_PARTY_NOTICES.md`(§GPL 「향후 경로」 문단) · `docs/BLUEPRINT.md`
  (§⑶-a 검증 기준 + 선택지 블록 → 결정 기록) · `interactive/DESIGN.md`(트리 주석 1곳) ·
  **수요 지점 포인터 2곳 복원**(`…/conventions/rap-odata-rules.md` ·
  `…/procedures/create-object.md` — 삭제 커밋이 지운 자리에 "사전 링크" 대신
  "조회 절차 링크"). 여기에 이 파일과 `HANDOFF.md`(마감·재개점).
- **불변 실측**: 지식 `.md` **148 유지**(신설·삭제 0) · 플러그인 버전 무변(전달은 판1의
  미배포 0.7.0 범프에 **편승** — D-060) · 게이트·훅·서버 번들·검사기 번들·`engine/`·
  `sapkit-engine/`·`sapkit-cli/` 무접촉 · 제품 게이트 9종 전부 exit 0.
  ⚠ **그러므로 push 직전에 설치본 버전을 확인한다** — `node interactive/scripts/doctor.mjs`가
  설치된 플러그인 manifest version을 레포 버전과 병기한다. **이미 0.7.0 설치본이 존재하는
  머신이 발견되면 패치 범프 후 push.** (`HANDOFF.md` 재개점에도 있으나 그 블록은 다음 판이
  덮어쓰므로, 판이 넘어가도 남는 자리로 여기에 둔다.)
- **조회 경로 생존 스모크 1회 통과** — `node interactive/tools/fetch/fetch-abap-keyword-doc.mjs
  abenwhere_all_entries` exit 0(공식 ABAP Keyword Documentation 본문 수신). ⚠ **상설
  게이트가 아니다** — 외부 문서 사이트의 이동·폐쇄는 감시하지 않기로 수용했고(D-086 ⑤ ·
  정직 유보 ⓐ), 발견 경로는 **실사용 중의 조회 실패 보고뿐**이다.

**후속 판이 이어받는 것** — **⑶-a는 닫혔다.** 차용분은 D-074의 삭제로 이미 소진됐고
빈자리 처리까지 결정됐으므로 **판8(⑷)의 ⑶-a 전제가 성립한다.** ⑶에 남은 것은
⑶-b(판3.x)와 ⑶-c(판4)다. BLUEPRINT §⑶-a의 집필 제약 세 불릿은 **폐기가 아니라 보류**다 —
향후 자체 집필이 필요해질 때 그대로 적용된다.

### 판3.x — ⑶-b copy 지식 치환 (반복 판 · **완료 — 판3.1~3.6 · 170/170**)

- **목표**: 차용(copy) 지식을 **실전에서 검증된 것부터** 자체 문장으로 치환. 파일
  단위, 치환 사실은 git 이력이 센다(분류 장부는 은퇴한 역사 — 갱신 금지).
- **완료 기준(판마다)**: 치환분을 소비하는 절차가 여전히 동작 · `check-links` 0 ·
  잔량 단조 감소가 기록됨.
- **갈래 종결 (2026-08-18 · 판3.6)**: 명부 [`reference/copy-baseline.md`](reference/copy-baseline.md)
  **170행 전량 체크 · 잔량 0.** ⑶-b에 남은 미처리 파일은 없다. 셈 밖 소관은 그대로
  열려 있다 — oop 20 = **판4** · 서버 결부 39 = **판7-b** · keyring 21 = 끝그림 ② ·
  LICENSE = **판8**. 따라서 **판8(⑷) 착수 전제 중 ⑶-b 몫은 성립**했고, 남은 것은
  판4(oop 20 · SAP 확인은 판6) + 판7-b 소관분 판정이다.
  → **갱신(2026-08-19 · D-093 ⓓ)**: `판4(oop 20)` 몫도 **판6.2에서 성립**했다(신 템플릿
  핵심 10종 SAP 확인 + 옛 20파일 제거). **남은 실질 전제는 판7-b 소관분 판정 하나**다 —
  현행 정본은 아래 **판8 브리프**다.

#### 판3.1 — 계수 방식 확정 + 명부 신설 + 1차 배치 5편 — **완료 (2026-08-17)**

**도달점**: 판3.x 브리프가 첫 판에 배정한 두 공백 — copy **잔량 계수 방식**(장부 은퇴로
생긴 공백)과 **1차 치환 대상 선정 기준** — 을 결정으로 닫고, 그 결정대로 **잔량 명부를
신설**한 뒤 **상시 로드 규약 5편을 실제로 재저작**했다. 결정 기록 = **D-087**.
**코드 0줄 · 문서만.**

- **잔량 명부 정본 = [`reference/copy-baseline.md`](reference/copy-baseline.md)** ·
  **총계 170** = 1부 지식 갈래 146 + 2부 편입분 24. **잔량 = 총계 − 체크 누계**이고,
  체크는 **재저작만** 인정하며(삭제로 갚는 길 없음) **독립 새-컨텍스트 리뷰 통과 후**에만
  찍는다. **이 명부를 게이트·스크립트로 만들지 않는다**(진척은 사람 판단이 센다).
- **편입 24**(지식 폴더 밖 copy — 훅 4 · `tools/spec/` 5 · `tools/fetch/` 2 ·
  `data-protection/` 12 · 사양서 xlsx 1)는 **이번 판이 등재만** 했다. 갚는 방법은 그 파일을
  맡는 후속 판3.x가 정한다.
- **셈 밖 소관**: 서버 결부 copy 39건 → **판7-b**(⑴ 교체가 은퇴·대체 범위에서 판정) ·
  `runtime-deps/keyring/` 21 → 끝그림 ② · 루트 `LICENSE` 1 → ⑷ 자체.
  **판8(⑷) 착수의 실질 전제 = 명부 170 소진 + 판4(oop 20) + 판7-b 소관분 판정.**
  → **갱신(2026-08-19 · D-093 ⓓ)**: 앞의 둘은 성립했다(명부 170 소진 = 판3.6 · oop 20 =
  판6.2의 SAP 확인 + 제거). **남은 것은 판7-b 소관분 판정 하나**이고 현행 정본은 판8 브리프다.
- **1차 배치 = 상시 로드 규약 5편**(`sap-standards.md`가 직접 로드하는 전부):
  `naming-conventions` · `sap-version-reference` · `cloud-abap-constraints` ·
  `ecc-ddic-fallback` · `abap-release-reference`. **잔량 170 → 165.**
- **검증**: 독립 새-컨텍스트 리뷰 3건(사실 전수 대조 · 구성 독립성 · 소비자 내용 대조 ·
  명부 검산) 전부 통과 — **사실 누락 0**(대조 항목 793 · 표 셀 전수 일치 · 릴리스 번호
  49쌍 양방향 일치), 명부 **170/170 멤버십 일치**, 소비자 정합에 **이번 판이 만든 어긋남 0**.
  제품 게이트 9종 + doctor 전부 exit 0 · 지식 `.md` **148 유지** · 버전 무변.
  ⚠ **push 직전 설치본 확인 의무 승계**(판2와 동일 — D-060 편승): `node
  interactive/scripts/doctor.mjs`로 확인하고 **이미 0.7.0 설치본이 있는 머신이 발견되면
  패치 범프 후 push**.

**판3.2 재료** (다음 판이 이어받는 것)
- **다음 배치 후보**: 1-D 규약 잔여 13편(앵커 3편 `clean-code-oop`·`include-structure`·
  `oop-pattern`은 `zrsc4sap_oop_ex` 보존 필수 — 명부 §6 비고. ⚠ **이 앵커 지시는 그 뒤
  실효됐다** — 판4가 정본을 `zsapkit_oop_ex`로 옮기며 게이트 ANCHORS 6항도 함께
  교체했다(**D-090** ⓐ). 위 문장은 판3.2 시점의 기록이다). 선정 기준은 D-087 ⑧
  **소비 실증**을 그대로 적용한다.
- **이월 지적 (이번 판 범위 밖 — 소비자 무수정 원칙 때문에 손대지 않음)**:
  ① `interactive/core/policies/sap-standards.md:56` Cloud Private 행이
  `sap-version-reference.md`를 인용하지만 그 규칙은 실제로
  `core/procedures/create-program.md:117`에 있다(오인용) · ② 같은 파일 :18/:28과
  `sap-executor.md:91`·`clean-code.md:15`가 「Z/Y」를 말하나 `abap/conventions/`의
  명명 규약은 Z 전용이다(Y는 `modules/common/naming-conventions.md`) ·
  ③ `sap-executor.md:60`·`analyze-code.md:62`가 변수 접두사(`LV_`/`LS_`/`LT_`)·상수
  (`GC_`/`LC_`)를 `abap/conventions/naming-conventions.md`에 있다고 하나 그 내용은
  `modules/common/naming-conventions.md:51–71`에 있다 · ④ `clean-code.md:5`의
  `common/abap-release-reference.md`는 이식 전 경로다. **넷 다 이번 판 이전부터 있던
  것**이고(구본 대조 확인), 고치려면 소비자 문서를 여는 별도 결정이 필요하다.
- **재저작이 남긴 규율 하나 (판3.2가 그대로 물려받는다)**: 리뷰 근거는 **양방향**이다 —
  누락 0뿐 아니라 **추가 0**(원문에 없던 단언·지시가 안 들어갔는가)까지 잰다. 판3.1은
  한쪽만 재다가 새 SAP 단언이 5편 전부에 얹혔고 **세 차례에 걸쳐 회수**했다. 명부 §2에
  이 규율을 명문화해 뒀다.
- **제품명 표기 갈림 (이월)**: 재저작 5편 중 `ecc-ddic-fallback.md`만 본문에 `SAPKIT`을
  쓰고, 나머지 4편과 코퍼스 다수는 아직 `sc4sap`이다. 원문 어휘 보존과 제품명 현행화가
  부딪히는 자리라 **코퍼스 일괄 정리로 다루는 편이 낫다** — 파일 단위 재저작 판이 건드릴
  일이 아니다.
- **사실 자체가 의심스러운 지점 (재저작이 아니라 내용 개정 소관 — 원문 그대로 보존함)**:
  `sap-version-reference.md`의 `MATNR 40 characters (1909+)` — 외부 확인으로는 40자리
  확장이 S/4HANA **1511**이고 기본 비활성(MFLE 활성화 필요)이다. `abap-release-reference.md`
  체크리스트의 `GetPackage`도 실제 도구 표면은 `GetPackageContents`/`ReadPackage`다.

#### 판3.2 — 1-D 규약 잔여 13편 재저작 — **완료 (2026-08-17)**

**도달점**: D-087 ⑧ **소비 실증** 기준으로 명부 1-D(규약)의 **잔여 13편을 전량 재저작**했다.
앵커 3편(`clean-code-oop` · `include-structure` · `oop-pattern`)은 `zrsc4sap_oop_ex`를
그대로 보존했고 개명 게이트로 확인했다. 이로써 **1-D 갈래가 소진**됐다(18/18).
**잔량 165 → 152.** **새 D-결정은 없다** — 이 판의 선택은 전부 D-087의 적용이거나 기존
규율의 승계, 또는 사용자 확정 답이었다. **코드 0줄 · 지식 `.md`만.**

- **재저작 13편**: `abap-release-examples` · `alv-rules` · `clean-code-oop` ·
  `clean-code-procedural` · `clean-code` · `constant-rule` · `field-typing-rule` ·
  `function-module-rule` · `include-structure` · `ok-code-pattern` · `oop-pattern` ·
  `procedural-form-naming` · `text-element-rule`. 커밋 해시와 리뷰 근거는 명부
  [`reference/copy-baseline.md`](reference/copy-baseline.md) §6 1-D가 든다.
- **검증**: 독립 새-컨텍스트 리뷰 **4건**(사실 전수 대조 3그룹 + 통합 리뷰) 전부 통과 —
  **사실 대조 13/13 PASS**(누락 0 · 추가 0 · 코드 fence·표·링크·코드스팬 다중집합 기계
  대조) · 리뷰가 지적한 **강도·범위 미세 이탈 9곳**은 회수 커밋 `bcbe36f6`으로 원문 수준
  복원 후 **재확인 9/9 OK** · **구성 독립성 13/13** · **소비자·상호 인용 125곳 전부 유효**
  (새로 깨진 것 0). 제품 게이트 9종 + doctor 전부 exit 0 · 지식 `.md` **148 유지** ·
  버전 무변.
  ⚠ **push 직전 설치본 확인 의무 승계**(판2·판3.1과 동일 — D-060 편승): `node
  interactive/scripts/doctor.mjs`로 확인하고 **이미 0.7.0 설치본이 있는 머신이 발견되면
  패치 범프 후 push**.

**판3.3 재료** (다음 판이 이어받는 것)
- **다음 배치 후보**: 명부 1부의 나머지 갈래 — **1-A 모듈 91 · 1-B 산업 15 · 1-C 국가 17 ·
  1-E 견본 5**. 선정 기준은 D-087 ⑧ **소비 실증**을 그대로 적용한다.
- **이월 승계 (판3.2는 손대지 않았다 — 사용자 확정)**: 위 「판3.2 재료」로 넘어왔던
  **소비자 오인용 4건** · **사실 의심 2건**(`MATNR 40 characters (1909+)` · 체크리스트의
  `GetPackage`) · **제품명 표기 갈림**(`SAPKIT` vs `sc4sap`)을 **전부 그대로 이월**한다.
- **이식 전 참조 보존 목록 (신규 등재 — 일괄 정비 안건)**: 재저작이 **원문 그대로 보존**한
  옛 경로 표기다(재저작은 표현·구성만 바꾸므로 경로 현행화는 범위 밖이었다).
  `clean-code.md` 3곳(`common/abap-release-reference.md` · `configs/{MODULE}/` ·
  `exceptions/*.md`) · `field-typing-rule.md` 5곳(`skills/…` 4 + bare `cbo-context.md`
  유령 참조) · `function-module-rule.md` 4곳(`skills/…` 3 + `agents/sap-executor.md`) ·
  `ok-code-pattern.md` 4곳(`common/…` 2 + `skills/…` 2) · `oop-pattern.md` 3곳
  (`common/oop-sample/…` 2 + `abap/alv-oop-handlers/` 라벨-타깃 불일치) ·
  `alv-rules.md` 1곳(`sc4sap/common/alv-sample/…`). ⚠ 이 중 `clean-code.md`의
  `common/abap-release-reference.md`는 위 이월 승계의 **소비자 오인용 ④와 같은 건**이다 —
  두 목록에 겹쳐 있으니 다음 판이 이중으로 세지 말 것.
- **소비자 서술 불일치 신규 발견 4건** (전부 **재저작 이전부터 존재** — 구본 대조로 확인 ·
  내용 개정 판 소관): ① `analyze-code.md:63`이 `constant-rule`을 "GC_/LC_/CO_ patterns"로
  설명하나 실제 파일에서 `GC_`는 `gc_status` 예시로만 등장하고 `LC_`·`CO_`는 없다 ·
  ② `include-structure.md`가 인용하는
  `clean-code-procedural.md § Mandatory Main Program Header`가 실제 헤딩
  (`… Template (MUST match)`)과 다르다 · ③ `ok-code-pattern.md`가 인용하는
  `§ PBO / PAI Module`은 헤딩이 아니라 불릿이다 · ④ `text-element-rule.md`가 지시하는
  도구 `GetTextElement`·`ReadTextElementsBulk`가 현행 도구 표면에 없다(실재는
  `WriteTextElementsBulk`).
- **기록 2건 (헤딩이 바뀐 파일 — 나머지 11편은 헤딩 집합 불변)**: ⓐ `constant-rule.md`는
  재저작에서 **절 헤딩 4종이 번호부로 재작성**됐다 · ⓑ `field-typing-rule.md`는 h3 1종이
  재작성됐다(`Why this rule exists` → `What each category loses without a DE`). 리뷰
  실측으로 **둘 다 옛 헤딩을 인용하는 문서 0건**이라 실질 피해가 없다 — 스펙의 계약 앵커는
  「소비자가 인용하는 절 제목」에만 걸린다.

#### 판3.3 — 1-B 산업 15 + 1-C 국가 17 재저작 — **완료 (2026-08-17)**

**도달점**: D-087 ⑧ **소비 실증** 기준으로 명부 1-B(산업 15)·1-C(국가 17) **32편을 전량
재저작**했다. 실증한 소비 지점은 **절차 4곳** — `create-program.md:176`(industry만 로드) ·
`ask-consultant.md:44`(industry·country 둘 다) · `package-to-process.md:202-203`(둘 다) ·
`compare-programs.md:370`(**country만** 로드) — 과 **페르소나 18편의 트리거 로드**다.
이로써 **1-B·1-C 두 갈래가 소진**됐다(15/15 · 17/17). **잔량 152 → 120.**
**새 D-결정은 없다** — 이 판의 선택은 전부 D-087의 적용이거나 판3.2 선례의 승계이거나
위임 채택이었다. **배치 선정과 강화 조항 3건은 사용자 상시 위임 하 「산출자 권고 채택」으로
기록한다**(독립 점검 반영). **코드 0줄 · 지식 `.md`만.**

- **재저작 32편**: 커밋 해시와 리뷰 근거는 명부
  [`reference/copy-baseline.md`](reference/copy-baseline.md) §6 **1-B·1-C**가 든다.
- **검증**: 독립 새-컨텍스트 리뷰 **5건**(전수 대조 4그룹 + 통합 리뷰) 전부 통과 —
  **사실 대조 32/32 PASS**(누락 0 · 추가 0 · 헤딩 298행 고유 78종 · 표 64행 중 데이터
  58행 — README 14+16 · eu-common 28 · 코드스팬 · 볼드 · 숫자 · 한자/한글/가나 다중집합
  기계 대조) · **회수 2회**(`05643eda` 10곳 · `41110412` 2곳) 후 재확인 정상 ·
  **구성 독립성 32/32** · **허브 인용 대조 + 외부 로드 표면 22편(절차 4 + 페르소나 18)
  전부 유효**(새로 깨진 것 0). 제품 게이트 9종 + doctor 전부 exit 0 · 지식 `.md`
  **148 유지** · 버전 무변.
  ⚠ **push 직전 설치본 확인 의무 승계**(판2·판3.1·판3.2와 동일 — D-060 편승): `node
  interactive/scripts/doctor.mjs`로 확인하고 **이미 0.7.0 설치본이 있는 머신이 발견되면
  패치 범프 후 push**.

**판3.4 재료** (다음 판이 이어받는 것)
- **남은 갈래 3**: **1-A 모듈 91** · **1-E 견본 5** · **2부 24**.
  - **1-A 모듈 91** — 한 판에 담기엔 분량이 커서 **모듈 묶음 분할을 제안한다**(예:
    `common` 7 + 대형 모듈부터). 선정 기준은 D-087 ⑧ **소비 실증**을 그대로 적용한다.
  - **1-E 견본 5 — 후순위 사유**: 코드 파일이라 재저작이 곧 **생성 코드 골격 변경**이 되고,
    그러면 판4급 **SAP 확인(판6 이연)** 이 필요해진다. **산문 재저작 계약이 그대로
    적용되지 않는다** — 이 갈래를 여는 판은 그 차이를 먼저 정해야 한다.
  - **2부 24** — 갚는 방법이 **미정**이고 **그 판이 정한다**(명부 §7이 등재만 해 뒀다).
- **이월 승계 (판3.3도 손대지 않았다 — 전부 그대로)**: 「판3.3 재료」로 넘어와 있던
  **소비자 오인용 4건** · **사실 의심 2건**(`MATNR 40 characters (1909+)` · 체크리스트의
  `GetPackage`) · **제품명 표기 갈림**(`SAPKIT` vs `sc4sap`) · **이식 전 참조 보존 목록
  20곳**(⚠ `clean-code.md`의 `common/abap-release-reference.md`는 소비자 오인용 ④와 **같은
  건**이므로 **이중으로 세지 말 것**) · **소비자 서술 불일치 4건** · **헤딩 재작성 기록
  2건**(`constant-rule.md` · `field-typing-rule.md`) — 전부 위 판3.2 브리프의 「판3.3 재료」
  본문이 정본이다.
- **사실 의심 신규 등재 (재저작은 원문대로 보존함 — 내용 개정 판 소관)**:
  `fr.md`의 `PDF-A/3`(통용 표기는 `PDF/A-3`) · `in.md`의 「Income Tax return — by July for
  companies audited」(통상 7월은 **비**감사 기한)와 「GSTR-9 (annual reconciliation),
  GSTR-9C (audit)」(주석이 뒤바뀐 의심) · `kr.md`의 「KERIS-certified ASP」(KERIS는
  한국교육학술정보원 — 전자세금계산서 인증 주체가 아니라는 의심)와 「Zengin-like」(일본
  시스템에 댄 비유) · `chemical.md`의 「IS-Chem」 표기 통용도 · `food-beverage.md`의
  catch weight — **특성 절의 `nominal` vs 커스터마이징 절의 `invoiced`** 대비 불일치 ·
  `es.md`의 Cuaderno **43/34/19 vs 68/58/43** 불일치(원문 상태).
- **배치 내 기존 불일치 기록 (재저작 이전부터 존재 — 보존함)**: `industry/README:58`의
  전칭 선언 vs `banking`·`public-sector`의 h2 2절 부재(**12/14**) · README fence의
  「Master Data」 vs 실헤딩 「Master Data Specifics」 · `country/README:32`의
  「DDMMYYYY → actually YYYY.MM.DD」 자기-정정 · `eu-common` **:3**(gb pre-Brexit 안내) ↔
  **:92**(Brexit 경고) 긴장 · 국가별 헤딩 변형(`Tax System — VAT/IVA/GST…` 류 13종)과
  `eu-common`의 상이한 h2 구성(README 선언과 다름).
- **재저작 구조 기록 2건 (계약 위반 아님 — 후속 감사가 오독하지 않도록)**:
  ⓐ `chemical.md`만 행수가 변했다(51→50 — Campaign production 불릿을 첫 불릿에 병합,
  사실 양쪽 잔존 · 32편 중 유일한 구조 이탈) ⓑ `construction.md`의 볼드 라벨 1곳이
  수식어를 볼드 밖으로 뺐다(`**Cost/revenue planning at WBS**` → `**Cost and revenue
  planning**, held at the WBS` — 사실 잔존). 「볼드 라벨도 앵커로 볼 것인가」는 판3.4
  계약 설계의 판단 재료다.
- **이식 전 참조**: 이 배치 본문 **0건**(신규 등재 없음).

#### 판3.4 — 1-A 모듈 전반부 43편 (재저작 30 + 검증 체크 13) — **완료 (2026-08-17)**

**도달점**: D-087 ⑧ **소비 실증** 기준으로 명부 1-A(모듈 91)의 **전반부 43편**을 처리했다 —
`common` 7 + **FI·CO·MM·SD·PP·PM 6모듈 × 6편**. 실증한 소비 표면은 넷이다: **모듈 컨설턴트
페르소나 14편이 자기 모듈 6편을 파일별로 지시 로드**(예 `sap-fi-consultant.md:10` +
MANDATORY 3곳 `:37`·`:64`·`:69`) · **컨설턴트 공통 블록이 `common` 5편**(bapi·tcodes·
tables·spro·enhancements)**을 로드** · **naming 2편의 소비자** = `analyze-code.md:73`·`:82`
(module-aware naming extension) + `create-object.md:77`(내용 요약 인용) +
`sap-bc-consultant.md:10` 글롭 · **모듈 폴더 패턴 로더 6편**(`spro-lookup` ·
`troubleshooting` · `package-to-process` · `customization-lookup` · `create-program:294` ·
`analyze-cbo-obj` — 플레이스홀더 표기는 `{MODULE}`/`<MODULE>` 두 형태가 섞여 있다).
처리 방식이 두 갈래로 갈렸다 — **재저작 30편** + **검증 체크(무변경) 13편**. 후자를 위해
**D-088을 신설**했다(표 지배 참조 파일의 셀 분류 재정의 + 무변경 인정 · **사용자 상시 위임 하
산출자 권고 채택**). **잔량 120 → 77.** **코드 0줄 · 지식 `.md`만.**

- **재저작 30편 · 검증 체크 13편**: 커밋 해시(무변경분은 「검증 체크(무변경)」)와 리뷰 근거는
  명부 [`reference/copy-baseline.md`](reference/copy-baseline.md) §6 **1-A**가 든다. 무변경
  13편 = FI·CO·MM·SD·PP·PM의 `spro`·`tcodes` 각 6편 + `SD/tables`(즉 spro 6 · tcodes 6 ·
  tables 1). 규칙 근거는 **D-088 ②**이고 명부 §2에 보충 1줄로 명문화했다.
- **근거 문장 정정 3건**(선정 근거를 실측으로 바로잡은 것 — 다음 판이 물려받는 정확한 값):
  컨설턴트 공통 블록은 `common` **5편**이다(7편 전부가 아니다 — `naming-conventions` 2편은
  그 블록에 없고 절차·글롭이 따로 부른다) · naming 2편의 소비자는 위 도달점의 4지점이다 ·
  `FI/tcodes.md`는 **실측 81행 중 표가 63행**이다.
- **검증**: 독립 새-컨텍스트 리뷰 **6건** 전부 통과 —
  - **전수 대조 4그룹 43/43 PASS**(누락 0 · 추가 0 양방향 · 셀 **1,356+** · 산문 **445행+** ·
    식별자·수치·한글 다중집합 기계 대조 · **식별자 열 밀림 0**).
  - **무변경 13편 총괄 재검**(RD) — **551셀 전수 분류** · **blob 해시 무변 13/13** · 모듈 spro
    경고 블록이 차용분이 아니라 **자작 신설분(커밋 `5fb1d901`)임을 확정**.
  - **통합 리뷰**(RF) — 구성 독립성 **43/43**(재저작 표면 기준 조정 판정) · **소비 표면 28지점
    유효** · 새로 깨진 것 **0**.
  - **회수 2회**: `88165eeb` **9곳**(규범 voice·과잉 환언·어미체 이탈 — `common/tcodes` ·
    `common/spro` · `CO/enhancements` · `MM/enhancements` · `PM/enhancements`) ·
    `d24e18a4` **3곳**(미재저작 1셀 + 과잉 회수 1행 재환언 — `SD/bapi` · `SD/enhancements` ·
    `MM/enhancements`). 둘 다 후 재확인 정상.
  - 제품 게이트 9종 + doctor 전부 exit 0 · 지식 `.md` **148 유지** · 버전 무변.
  ⚠ **push 직전 설치본 확인 의무 승계**(판2·판3.1~3.3과 동일 — D-060 편승 · 미배포 0.7.0):
  `node interactive/scripts/doctor.mjs`로 확인하고 **이미 0.7.0 설치본이 있는 머신이
  발견되면 패치 범프 후 push**.

**판3.5 재료** (다음 판이 이어받는 것)

- **다음 배치 = 1-A 후반부**: **Ariba·BW·HCM·PS·QM·TM·TR·WM 8모듈 × 6 = 48편.**
  **계약은 판3.4와 같다 — D-088 그대로** 적용하면 되고 새 결정이 필요하지 않다. 이 48편으로
  **1-A 갈래가 소진**된다(43 + 48 = 91).
- **정찰 2건 (판3.4가 실측해 남긴 것 — 무변경 선례를 전제하지 말 것)**:
  ⓐ **`PS/spro.md`는 표 밖 산문이 실재한다**(L82-84 불릿 — Fiori 대체 · Hierarchical
  Projects · ACDOCA). 다른 모듈 spro가 무변경이었다는 사실을 **PS에 그대로 옮기면 틀린다.**
  ⓑ **spro 술어 탐침의 Title-Case 명사 함정** — `Sets`(QM/PM Cycle Sets) · `Means`
  (TM Means of Transport). 동사로 읽으면 문장형 셀로 오분류된다.
- **이월 전량 승계 (판3.4도 손대지 않았다 — 전부 그대로)**: 위 「판3.4 재료」(판3.3 브리프)
  본문의 항목 전부 — 소비자 오인용 4건 · 사실 의심 2건(`MATNR 40 characters (1909+)` ·
  체크리스트의 `GetPackage`) · 제품명 표기 갈림(`SAPKIT` vs `sc4sap`) · **이식 전 참조 보존
  목록 20곳** · 소비자 서술 불일치 4건 · 헤딩 재작성 기록 2건 · 판3.3이 등재한 사실 의심
  (`fr.md` · `in.md` · `kr.md` · `chemical.md` · `food-beverage.md` · `es.md`) · 배치 내 기존
  불일치 5건 · 재저작 구조 기록 2건. ⚠ **이중 계수 금지** — `clean-code.md`의
  `common/abap-release-reference.md`는 「이식 전 참조 20곳」과 「소비자 오인용 ④」에 **겹쳐
  있는 같은 건**이다.
- **낡은 도구명 동결 36곳 (사실 의심 등재)**: 현행 도구 표면에 없는 `GetTable`(16곳) ·
  `GetFunctionModule`(9) · `GetClass`(5) · `GetProgram`(3) · `GetView`(2) · `GetStructure`(1)
  을 **원문 그대로 보존**했다(Read* 현행화 금지 — 판3.2 발견 ④의 `GetPackage`와 동종 ·
  내용 개정 판 소관).
- **사실 의심 신규 등재 (재저작은 원문대로 보존함 — 내용 개정 판 소관)**:
  - `common/naming-conventions.md` — 소비자 `create-object.md:77`이 이 파일의 범위를 잘못
    요약한다(오브젝트별 패턴은 실제로 `naming-conventions-objects` 소관 — 소비자 서술 불일치
    계열).
  - `CO/tcodes.md` — KSU5/KEU5 국문 「배분/분배」 용어 갈림 · OKEON 대역에 '원가 센터' 누락.
  - `SD/spro.md` — `V_TVAPt` 소문자 표기 · Config Name 「Define Item Categories…」 중복 2회 ·
    `V_MTVFP`↔`V_TMVFP` 전치 의심.
  - `SD/tcodes.md` — V/06·V/08 명칭 한 칸 밀림 의심 · VFX3 국문 대역 「전기」 vs Release ·
    `SPRO = SAP Project Reference Object` 역두문자 · VL60·SD70 실재 미확인.
  - `SD/bapi.md` — `AVAILABILITY_CHECK`·`CREDIT_EXPOSURE_CALCULATE` FM 실재 의심 ·
    `BAPI_OUTB_DELIVERY_CONFIRM_DEC`의 Description(Transfer Order)↔Usage(goods issue) 긴장.
  - `PP/bapi.md` — `BAPI_BOM_GETDETAIL`·`BAPI_ROUTING_GET_DETAIL`·`BAPI_WORKCENTER_GET_DETAIL`·
    `BAPI_CAPACITY_REQUIREMENTS`·`MD_MRP_PARAMETERS_MATERIAL` 이름 의심 · PBHI/PBIM 짝
    (통상 PBIM+PBED) 의심 · `BAPI_GOODSMVT_CREATE`의 PP 한정 gloss.
  - `PP/spro.md` — 합성 의심 뷰명 7종: `V_T003O_PP`·`V_MARC_MRP`·`V_MARC_PTF`·`V_MTVFP_PP`·
    `V_TC44_ALT`·`V_T024D_W`·`V_TCO15`.
  - `PP/workflows.md` — WF2의 `PLSC (planned order)` vs Required Tools의 `PLAF`(문서 내부
    불일치).
  - `PP/tcodes.md` — MDVP↔MD09 의심(유력) · OPJN↔OPJH · OP43↔OMD0/OPPU · MD45 라벨 과대 ·
    COOIS 중복 표기 갈림.
  - `PM/bapi.md` — `RIIFLO20`(FM 아닌 리포트 의심) · `MPLAN`(통상 MPLA/MPOS) ·
    `BAPI_MAINTPLAN_CREATE`/`BAPI_MAINTPLAN_GETDETAIL` 실재 의심.
  - `PM/enhancements.md` — `IWP1` 4자 형태 이질 · `IEQM_SERNO_CHECK` 표기 관례 · `ITOB`가
    Table 열에 있는 점.
  - `PM/tcodes.md` — 해석적 라벨 4종(IP10·IW37N·PMSM·IW65) · 코드 의심 9종
    (S_ALR_87012894·IWBK·MCJB·MCIZ·OIOF·OIB2·OIH2·OIYL·OIM0).
  - `PM/tables.md` — IFLO(뷰)·IFLOTX·MMPT·T352T 라벨 사실성 의심.
  - `CO/enhancements.md` — §4.2 표 3열 헤더에 4셀 행(**렌더링 결함** · 이식 시점부터) ·
    MP-variant↔평가 변형 대역 불일치.
  - `FI/bapi.md` — `Post GL Posting…`의 GL→G/L **표기 정규화 1건 인정**(같은 셀 KR이 이미
    G/L이라 리뷰가 정규화로 판단).
  - `FI/bapi.md` KR 「전기」 보유 5셀 중 **4셀이 「계상」으로 전환, 1셀(전기 기간 판별)은
    「전기」 유지** — 파일 내 용어 갈림, **판3.5 일관성 재료**(같은 어휘가 다른 파일에서
    어떻게 처리되는지 함께 볼 것).
  - `MM/tcodes.md`·`FI/tcodes.md`·`common/tcodes.md` 등 — System 열·명칭 의심 소량(각 리뷰
    보고서에 기록).
- **판정 폭 기록 (위반 아님 — 후속 감사가 오독하지 않도록)**: 표 셀 재저작 폭이 파일 간에
  갈렸다 — **광폭**(`common/bapi` Description 62셀 · `common/enhancements` gloss 36셀 ·
  `FI/bapi` Description+Usage 54셀) vs **협폭 수렴선**(`FI/enhancements`·`FI/tables`·
  `CO/tables`·`MM/enhancements`·`MM/tables`·`PP/tables`·`PM/tables` 등 — 명칭·명사구 보존,
  진짜 술어절만. `SD/tables`는 이 계열이 아니라 **무변경 검증 체크 13편**의 하나다).
  D-088 ①의 「저작 gloss냐 공식 명칭이냐」 판정 재량 범위이고, **전 파일에서 식별자·수치
  다중집합 불변이 기계로 증명**돼 사실 무변은 성립한다(D-088 정직 유보 ⓒ). 같은 계열 2건:
  `Internal FM:` 셀은 `SD/bapi` 보존 vs `MM/bapi`·`PP/bapi` 재저작으로 **갈렸고**, 스텝 태
  drift가 `PP/workflows` vs `SD/workflows`에서 갈렸다 — **전 파일 사실 무변은 기계 증명됨**.
- **EN/KR 쌍 규정 적용 기록**: 산문 쌍은 두 언어를 함께 재저작했고(`common/spro`·
  `common/tables`·`common/tcodes`·`MM/enhancements`·`CO/enhancements`·`PP/enhancements`·
  `PM/enhancements` 등) **EN 단독 본문에 KR을 신설한 것은 0**, KR 단독 산문도 재저작했다
  (`MM/bapi.md:19`).
- **배치 내 상호 인용 (판3.5도 같음)**: 14개 모듈 `spro.md:9`가 `common/spro.md`의 헤딩
  **「IMG Activity Verification」**을 인용한다. 재저작으로 그 절의 **행 좌표는 이동했고
  헤딩 문구는 보존**됐다 — 후속 판은 이 인용을 **절 이름으로만** 다루고 행 좌표를 박지 말 것.
- **이월 지적 ③의 좌표 전환**(판3.1 등재분 — 행 좌표를 박지 않도록 절 좌표로 갱신): `sap-executor.md:60`·
  `analyze-code.md:62`가 변수 접두사·상수를 `abap/conventions/naming-conventions.md`에 있다고
  하나 그 내용은 `modules/common/naming-conventions.md` **§ Code-Level Naming의
  Variables·Constants 표**에 있다(구 표기 `:51–71`).
- **이식 전 참조**: 이 배치 본문 **0건**(신규 등재 없음).

#### 판3.5 — 1-A 모듈 후반부 48편 (재저작 35 + 검증 체크 13) — **완료 (2026-08-18)**

**도달점**: D-087 ⑧ **소비 실증** 기준으로 명부 1-A(모듈 91)의 **후반부 48편**을 처리했다 —
**Ariba·BW·HCM·PS·QM·TM·TR·WM 8모듈 × 6편.** 처리는 판3.4와 같이 두 갈래로 갈렸다 —
**재저작 35편** + **검증 체크(무변경) 13편**(spro 7 · tcodes 6). 무변경 13편은 **사양이 미리
지목한 후보 명단과 정확히 일치**하고 **근사-무변경 → 재저작 전환은 0건**이었다(선정 정확도의
실측). 이로써 **1-A 갈래가 소진**됐다(43 + 48 = 91/91) — 명부 1부에 남은 것은 1-E 견본 5뿐이다.
**잔량 77 → 29.** **새 D-결정은 없다** — 이 판의 선택은 전부 **D-087·D-088의 적용**이거나
판3.4 선례의 승계다. **코드 0줄 · 지식 `.md`만.**

- **재저작 35편 · 검증 체크 13편**: 커밋 해시(무변경분은 「검증 체크(무변경)」)와 리뷰 근거는
  명부 [`reference/copy-baseline.md`](reference/copy-baseline.md) §6 **1-A**가 든다. 무변경
  13편 = Ariba·BW·HCM·QM·TM·TR·WM의 `spro` 7 + Ariba·BW·PS·QM·TM·TR의 `tcodes` 6. 규칙
  근거는 **D-088 ②**.
- **판3.4 정찰 2건이 실제로 갈랐다**: ⓐ **`PS/spro.md`는 재저작**(표 밖 불릿 3행) — 다른 모듈
  spro 7편의 무변경 선례를 PS에 옮기면 틀렸다는 판3.4 경고가 맞았다. ⓑ **Title-Case 명사
  함정**도 실증됐다 — `TM/spro.md:18`의 `Means`를 동사로 읽으면 문장형 셀로 오분류되는데,
  RE 총괄 재검이 **명사 확정**으로 닫았다. **`HCM/tcodes`·`WM/tcodes`는 tcodes인데도 재저작**
  이다(경고 blockquote가 있어 표면이 0이 아니다) — tcodes=무변경으로 일반화하지 말 것.
- **검증**: 독립 새-컨텍스트 리뷰 **6건 전원 PASS** · **회수 0건**.
  - **전수 대조 4그룹 48/48 PASS**(RA=Ariba+BW · RB=HCM+PS · RC=QM+TM · RD=TR+WM) —
    누락 0 · 추가 0 양방향 · 식별자·수치·한글 다중집합 **기계** 대조 · Get* 동결 **55곳이 행
    좌표까지 동일** · blockquote·fence·코드스팬 sha256 동일 · 무변경분 diff-빈 이중 확인.
  - **RE 총괄 재검 13/13 CONFIRM** — blob **3중 대조**(보고 해시 = 현재 = base `43ab754a`) ·
    **477 데이터 행 전수** 재분류(잔여 0) · 어휘 전수 덤프(EN 530 · KR 364 토큰 — 계사·조동사·
    산문 주어·국문 종결어미 전부 부재) · 모듈 spro `:4-9` 자작 카브아웃 7파일 sha1 동일 + blame
    확인.
  - **RF 통합 리뷰** — 구성 독립성 표본 6편 · **소비 표면 14지점 유효** · 새로 깨진 것 **0** ·
    **자작 경고 블록 8편 무접촉**.
  - **⚠ 회수 0은 이 판의 특기다** — 판3.1~3.4는 **매 판 회수가 났다**(3회 · 9곳 · 12곳 · 12곳).
    이번 판은 그 **실패 모드와 판정 폭을 재저작 프롬프트에 선재 주입**했고 그 결과 회수가 0이
    됐다. 후속 판은 이 주입을 승계할 것.
  - 제품 게이트 9종 + doctor 전부 exit 0 · 지식 `.md` **148 유지** · 버전 무변 ·
    `interactive/plugin-metadata.json`과 생성물 7종 무접촉.
  - **529 장애 기록**: 배치 4에서 API 혼잡(529 Overloaded)으로 4편(`QM/enhancements` ·
    `QM/tables` · `QM/workflows` · `TM/enhancements`)이 3~4회 중단됐다 — 백오프(150s→420s) +
    동시도 2로 낮춰 재시도해 전원 완료했고, 매 실패마다 워크트리를 리셋해 부분 수정 잔재 0을
    확인했다. **결과물 영향 0.**
  ⚠ **push 직전 설치본 확인 의무 승계**(판2·판3.1~3.4와 동일 — D-060 편승 · **미배포 0.7.0**):
  `node interactive/scripts/doctor.mjs`로 확인하고 **이미 0.7.0 설치본이 있는 머신이 발견되면
  패치 범프 후 push**.

**판3.6 재료** (다음 판이 이어받는 것)

- **남은 갈래 2 = 잔량 29 전부**: **1-E 견본 5** + **2부 24**. ⚠ **둘 다 그 갈래를 여는 판이
  계약을 먼저 정한다**(판3.3 브리프의 명문을 승계) — 산문 재저작 계약(D-088)이 그대로 적용되지
  않기 때문이다.
  - **1-E 견본 5** — `.abap` **코드 파일**이라 재저작이 곧 생성 코드 골격 변경이 되고, 그러면
    판4급 **SAP 확인(판6 이연)** 이 걸린다. D-088의 「표 셀 분류」는 여기 쓸 수 없다.
  - **2부 24** — 갚는 방법이 **미정**이고 성격이 셋으로 갈린다: ⓐ **훅 4 + 사양서 도구 5 +
    조회 도구 2 = 11편은 `.mjs` 실행 코드**이므로 **동작 계약 유지가 필수**다(특히
    `tools/fetch/` 2종은 `core/procedures/help-portal-fetch.md`가 기대하는 계약 · 훅 4종은
    PreToolUse 안전 배선) ⓑ **데이터보호 정책 12는 `.md`**이므로 **D-088 적용이 가능**하다
    (2부 중 유일하게 산문 계약이 그대로 서는 갈래 — 판3.6의 유력 배치 후보) ⓒ **xlsx 1은
    바이너리**로 **재제작 방법 자체가 미정**이다.
- **이월 전량 승계 (판3.5도 손대지 않았다 — 전부 그대로)**: 위 「판3.5 재료」(판3.4 브리프)
  본문의 항목 전부 — 소비자 오인용 4건 · 사실 의심 2건(`MATNR 40 characters (1909+)` ·
  체크리스트의 `GetPackage`) · 제품명 표기 갈림(`SAPKIT` vs `sc4sap`) · **이식 전 참조 보존
  목록**(판3.4까지 20곳 · ⚠ 그중 1곳은 소비자 오인용 ④와 **같은 건**이므로 **이중 계수 금지**) ·
  소비자 서술 불일치 4건 · 헤딩 재작성 기록 2건 · 판3.3·판3.4가 등재한 모듈별 사실 의심 전부 ·
  배치 내 기존 불일치 5건 · 재저작 구조 기록 2건 · 배치 내 상호 인용(모듈 `spro.md:9` →
  `common/spro.md` 헤딩 「IMG Activity Verification」 — **절 이름으로만** 다루고 행 좌표를 박지
  말 것).
- **낡은 도구명 동결 55곳 (사실 의심 등재 — 판3.4의 36곳을 대체하는 1-A 전체 실측치)**: 이 판이
  다룬 **workflows 8편에 55곳**이 있고 전부 **원문 그대로 보존**했다 — `GetTable` **26** ·
  `GetFunctionModule` **15** · `GetClass` **11** · `GetProgram` **2** · `GetView` **1**.
  `GetInclude` 3곳(`Ariba/workflows:40` · `BW/workflows:63` · `PS/workflows:38`)은 **현행
  도구명이라 동결 목록 밖**이다. Read* 현행화는 금지(판3.2 발견 ④의 `GetPackage`와 동종 —
  내용 개정 판 소관).
- **이식 전 참조 신규 1곳 (보존 목록 20 → 21)**: `Ariba/tables.md:43-44`의
  `configs/Ariba/spro.md` 코드스팬 — 이식 전 경로 표기이고 **바이트 보존**했다(재저작은 표현·
  구성만 바꾸므로 경로 현행화는 범위 밖).
- **KR 용어 관찰 (판3.4가 「판3.5 일관성 재료」로 넘긴 `FI/bapi` 「전기→계상」 5셀 갈림의 후속)**:
  이 배치는 KR 산문 표면이 적어(대부분 표 지배 참조 파일) **「전기/계상」 계열 갈림이 재현되지
  않았다.** 그래서 코퍼스 일괄 규칙을 세울 재료가 늘지 않았고, **파일 내 일관 유지**로 처리했다 —
  이 안건은 여전히 **내용 개정 판 소관**으로 열려 있다.
- **⚠ 참조표 사실 의심 밀도 경보 (문장 재저작과 별개 — SAP 접속이 필요한 사실 검증 백로그
  후보 · 판6 이후 소관)**: 이 배치의 참조표는 **의심 밀도가 판3.4 배치보다 뚜렷히 높다.**
  계열로 묶으면 —
  - **BW**: `spro`의 Table/View 열 **34개 중 33개가 자기 레포 `BW/tables.md`와 무대응** +
    **내부 모순 5건** · `tcodes`는 38행 중 **~9행**이 의심(테이블명이 TCode 열에 있는 등).
  - **QM**: `spro` 뷰명이 **계통적으로** 의심스럽다(`V_T161_QM`·`V_T705B`·`V_T708`·`V_MARC_QM`
    계열 — T161=MM · T705/706/708=HR 번호대 정황) + **내부 모순 5건**. `workflows`·`tcodes`가
    교차 확증한다.
  - **TR**: `spro`의 Table/View 열이 **「한 stem + 구분 1글자」 블록을 반복**한다(패턴 생성
    참조 데이터의 특징) · 그중 **`V_T030`은 실재 테이블의 오용**이라 최위험.
  - **TM**: `V_TM*` 설정 뷰 계열이 **27행 규모**로 의심된다(실제 네임스페이스는 `/SCMTMS/` —
    `TM/spro`·`TM/workflows` 교차).
  - **WM**: **`WM/bapi` ↔ `WM/tables`가 TO/TR 테이블 쌍(LTAK/LTAP ↔ LTBK/LTBP)을 서로 반대로
    서술**한다 — **2워커 교차 확증**이고 `tables` 쪽이 실제 SAP 정합이다(원문 대물림) ·
    `WM/tables.md:45`의 `T333` 라벨은 spro·workflows·실제 SAP **3처가 2대1로** 오기재를 가리킨다.
  - 개별 항목은 각 리뷰 보고와 판 진행 대장이 든다. **재저작 계약상 전부 원문 보존**했다.
- **⚠ 판3.4 체크 완료분에 잔존 1건 (다음 판 처리 대상)**: `SD/bapi.md:31`의
  `SD_SALESDOCUMENT_PRICING_GET` 행 — `Internal FM: read pricing procedure results` 셀이
  **미재저작으로 남아 있다**(판3.4 회수 2차 `d24e18a4`가 같은 파일 `:7`은 고치고 이 셀을 누락한
  것으로 추정). 판3.5는 **체크 완료 93편 무접촉 계약**이라 손대지 않았다 — **판3.6이 처리한다**
  (명부 체크는 이미 서 있으므로 회수 성격의 수리 커밋 1건이면 닫힌다).
- **무접촉 등재 권고 1건 (RB 리뷰)**: `PS/workflows.md:82`의 DDLS 행은 이식분이라 재저작이
  정당했으나, **후속 감사가 「자작분을 건드렸다」로 오독할 여지**가 있어 등재해 둔다.
- **오기 등재 1건 (정정은 내용 개정 판 소관 — 재저작은 바이트 보존함)**: `BW/workflows.md:9`의
  `AEDАТ`는 **키릴 동형 이체자**(U+0410 А · U+0422 Т)가 섞여 있다 — 눈으로는 `AEDAT`으로 보이나
  **검색·복붙이 깨지는 오기**다. RA 리뷰가 바이트 보존을 확인했다.
- **배치 실측 특기 2건**: ⓐ `HCM/workflows`·`PS/workflows` 둘 다 **W1 Steps 번호에 6이 결번**
  (5→7 · 서로 다른 파일에서 같은 결함 · **원문 보존**) ⓑ **판정 폭 기록** — 협폭은 **열 단위
  판정**으로 확정됐다(Description 열 보존 / Usage 열 재저작 · Usage 안의 명사구 셀도 재저작
  대상 — 판3.4 회수 2차 `d24e18a4`의 `SD/bapi` 동형 셀 선례 준거). 같은 계열 갈림 2건:
  `QM/enhancements`(표 0셀) ↔ `TM/enhancements`(37셀) 표 정책 · `BW/enhancements` `:11`
  (저작 gloss → 재저작) ↔ `:15`(RSPC 공식 개념명 → 보존) · `PS/tables`의 ACDOCA 셀(정동사가
  있으나 후속객체 태그 계열로 보존). **전 파일 사실 무변은 기계로 증명됐다**(D-088 정직 유보 ⓒ).

#### 판3.6 — 잔량 29편 5갈래 전량 (⑶-b **완주** · D-089 신설) — **완료 (2026-08-18)**

**도달점**: 명부 잔량 **29편을 한 판에 전량** 처리했다 — **견본 `.abap` 5**(1-E) · **안전 훅
`.mjs` 4**(2-A) · **사양서 도구 `.mjs` 5**(2-B) · **조회 도구 `.mjs` 2**(2-C) · **데이터보호
정책 `.md` 12**(2-D) · **xlsx 바이너리 1**(2-E). **잔량 29 → 0 · 체크 누계 170/170 —
⑶-b 갈래가 완주했다.** 산문 재저작 계약(D-087·D-088)이 그대로 서지 않는 갈래들이라 **새 계약을
정했고 그것이 D-089**다(사용자 상시 위임 + 밤샘 위임 하 **산출자 권고 채택**). 이 판은 판3.x 중
**유일하게 코드를 만진 판**이다.

- **D-089 갈래 계약 5종** (원문이 정본 — 여기는 요지):
  - **ⓐ 정책 12 = D-088 변형 + 「구문 층」 신설.** 훅이 정규식으로 읽는 서식(헤딩 구조 ·
    표 헤더 열 이름 · `tier`/`action` 주석 · 식별자 · 구분자 · 와일드카드 · 파일명)을 **재저작
    원천 배제**하고, **게이트 하드코딩 6종**(USR02·BNKA·KNA1·VBRK·BALDAT·PA0008)은 소속
    파일·tier·action이 한 글자도 변하지 않는다. **체크 전제 = 파싱 동치 기계 증명.**
  - **ⓑ 코드류 16편 = 동작 동결 재구현.** 판정·출력·exit·export 동치를 채록·차분으로 증명하고
    표현만 자체 저작. **알려진 동작 갭도 함께 동결**(개선은 별건 — 섞이면 회귀 기준이 사라진다).
  - **ⓒ xlsx = 생성 스크립트 재제작.** `interactive/tools/spec/gen-template-base.mjs` 신설로
    전 구성 자체 산출 + 재현성 기계 증명. theme/metadata 파트는 의도적 미포함.
  - **ⓓ `alv-rules.md` fence 1곳 동기화 예외** — 「체크 완료편 무접촉」의 명시 예외(코드 인용
    fence만 · 산문 무접촉).
  - **ⓔ 훅 판정 채록 러너 신설** `interactive/scripts/test-hook-decisions.mjs`(**74케이스**) —
    안전 계층 L3 판정의 **첫 회귀 기준**. **CI 편입은 판5에서 판단**(이 판은 로컬 러너로 둔다).
- **검증**: 독립 새-컨텍스트 리뷰 **4팀 전원 PASS** · **회수 1건 + 경량 수정 1건**.
  - **R-DP(정책 12) PASS** — 파싱 동치 독립 재검 **11/11**(절취 충실도 기계 대조 + 변이 15종
    음성) + **실훅 바이너리 530회 종단 대조**(5프로파일 · **판정 차이 0** · 종단 하네스도
    음성시험) · 하드코딩 **6/6 바이트 동일** · 구문 층 **12/12 불변**(H3→H2 승격 0 · 링크 11 ·
    fence) · 서문 12/12 재저작 · 숫자 변경 0.
  - **R-CODE(코드) 11 PASS / 1 FAIL → 회수 완료** — FAIL = `element_create_sample.abap:71`
    closing line이 `ecc-ddic-fallback.md` §4 **축자 계약 이탈**(Major) → `378db9c7`로 원문
    문자열 복원 · lint 0. 훅 4종은 **74건 스위트를 원본 훅에도 걸어 동일 통과** + 변이 3종 검출
    실증 + 원·신 payload 문자 대조.
  - **R-SPEC(사양서 체인 6건) PASS** — build-spec **E2E 43/43**(독립 ZIP 파서 + OPC +
    SpreadsheetML 3종 검증기) · `screen-image-renderer` **export 18종 확정**(정찰 17은 오기) ·
    원↔재 77/77 + 엣지 384콜 대조 · 템플릿 신조·**재현성 기계 증명**(재실행 산출물 해시 = 커밋
    자산 해시).
  - **경량 수정 1건**: `core/procedures/program-to-spec.md`의 수치·예시 **5곳**(`a5f5279a`) —
    ⓒ의 템플릿 재제작이 만든 어긋남을 R-SPEC이 특정했고 **같은 판에서 회수**했다.
  - **RF 통합 리뷰** — 제품 게이트 **12종 green**(9종 + doctor + `test-hook-switch` 13/13 +
    신설 `test-hook-decisions` 74/74) · 지식 `.md` **148 유지** · 버전 무변 · 생성물 7종 무접촉.
  ⚠ **push 직전 설치본 확인 의무 승계**(판2·판3.1~3.5와 동일 — D-060 편승 · **미배포 0.7.0**):
  `node interactive/scripts/doctor.mjs`로 설치본 manifest version을 확인하고 **이미 0.7.0
  설치본이 있는 머신이 발견되면 패치 범프 후 push**. (`HANDOFF.md` 재개점에도 있으나 그 블록은
  다음 판이 덮어쓰므로, 판이 넘어가도 남는 자리로 여기에 둔다.)

**판4 재료** (다음 판이 이어받는 것)

- **⚠ 1-E 계약이 판4의 선례다.** 판4가 다시 쓰는 `templates/oop-sample/` 20파일은 이 판이
  처리한 견본 5편과 **같은 성격**(코드 견본 · 소비자가 파일명·구조로 인용)이다. 그러므로
  **D-089 ⓑ**(동작 동결 재구현 — 시연 패턴·FM 흐름·교육 목적 동치 · 주석·메시지·변수명·구현
  스타일은 자체 저작)와 이 판의 실측 관습을 그대로 준용한다:
  - **ecc 트리오가 헤더 라벨 `What` / `How` / `Caution`으로 독립 수렴**했다 — 견본 헤더의
    사실상 표준이다.
  - **`ecc-ddic-fallback.md` §4의 축자 요소는 계약이다** — 이 판의 유일한 리뷰 FAIL이 그
    축자 이탈이었다(`378db9c7`). 판4는 **소비자 문서가 견본 문자열을 축자로 고정한 자리**를
    먼저 찾고 시작할 것.
  - **앵커 제약 — ⚠ 판4가 「보존」이 아니라 「전환」으로 닫았다.** 판3.6은 앵커 문자열
    `zrsc4sap_oop_ex`를 **보존 대상**으로 넘겼으나(개명 게이트가 6문서에서 지키고 있었다),
    판4는 정본이 옮겨가면 앵커도 따라간다고 판정해 **ANCHORS 6항을 `zsapkit_oop_ex`로
    교체**했다(**D-090** ⓐ · 음성시험 literal 동반 갱신). **현행 앵커는 신 이름이고 이
    문단의 「보존」 지시는 실효다** — 아래 판4 브리프가 정본이다.
- **⚠ 소비자 4곳 INCLUDE 순서 정렬 필요 (판4가 함께 정리하는 것이 자연스럽다)**:
  `main-program.abap` 재저작이 헤더 주석의 INCLUDE 순서 오기를 **본문 정본으로 정정**했고, 그
  정정이 **소비자가 인용하는 순서가 본문과 어긋남을 표면화**했다 — `include-structure.md:24`·
  `:32` · `clean-code-procedural.md:13` · `review-checklist.md:208`(MAJOR 게이트). ⚠ **정정
  전에도 완전 일치는 아니었다**(소비자 문자열에 `e`가 포함돼 있었다). 함께 표면화된 자기모순:
  S1 「6필드」 주장 순환 미확정 · S3 소비자 순서 문자열에 Procedural 금지 `e` 포함 · S4 c/a 허용
  여부가 `include-structure` 내부에서 모순 · S5 `review-checklist:208`이 이 파일로는 검증
  불가한 항목 지정(PBO/PAI는 o/i INCLUDE 소재) · S6 `f_` 접두 divergence. **판4가 템플릿을
  다시 쓰며 소비자 문서를 함께 정렬하는 것이 자연스럽다** — 이 판은 소비자 무접촉 계약이라
  손대지 않았다.
- **이월 전량 승계 (판3.5 브리프의 「판3.6 재료」 본문 전부 — 이 판도 손대지 않았다)**:
  소비자 오인용 4건 · 사실 의심 2건 · 제품명 표기 갈림 · 이식 전 참조 보존 목록 21곳(⚠ 그중
  1곳은 소비자 오인용 ④와 같은 건 — **이중 계수 금지**) · 소비자 서술 불일치 4건 · 헤딩 재작성
  기록 2건 · 판3.3~3.5가 등재한 모듈별 사실 의심 전부 · **참조표 사실 의심 밀도 경보
  BW·QM·TR·TM·WM** · 낡은 도구명 동결 55곳 · `BW/workflows:9` `AEDАТ` 키릴 동형 이체자 ·
  `PS/workflows:82` DDLS 행 무접촉 권고 · KR 「전기/계상」 갈림(열린 채) · 배치 내 상호 인용
  (모듈 `spro.md:9` → `common/spro.md` 헤딩 「IMG Activity Verification」 — **절 이름으로만**
  다루고 행 좌표를 박지 말 것).
- **⚠ `SD/bapi.md:31` 잔존 1건은 아직 열려 있다** — 판3.5가 「판3.6이 처리한다」고 넘겼으나
  판3.6 사양이 **범위 밖으로 명시**해 손대지 않았다(이 판의 배치는 잔량 29편 소진이었다).
  명부 체크는 이미 서 있으므로 **수리 커밋 1건**이면 닫힌다 — 내용 개정 판 또는 판4가 편승
  처리 가능.
- **판3.6 신규 등재 (전부 원문·현행 동작 보존 · 수리는 별건)**:
  - **훅 파서 갭 4종** — 패턴 파서가 첫 토큰만 취해 **`Y*` 커버리지 0**(문서 약속과 훅 동작의
    갭 · `ZPA_*`류는 `^Z…`가 우연 커버) · `| View | Wraps | Why |` 헤더 행이 헤더 스킵
    정규식(`Table|Pattern`만)에 안 걸려 **유령 엔트리 `VIEW`로 파싱**(CDS 하위표를 가진 전 파일
    공통 선재 결함 — 훅 정규식 1줄 수정감) · `| Table / Pattern |` 헤더도 미스킵(유령 2종) ·
    **비객체 JSON 입력 시 크래시 = 실질 fail-open**. **별도 D-결정 후보.**
  - **문서↔훅 불일치 5건**(`table_exception.md`) — `scripts/hooks/` 구경로 2곳(문서화된 스모크
    명령이 그대로는 실행 불가 · `data-extraction-policy.md:116`도 동일) · 제외 파일 서술 과소
    (실제 2파일 제외) · H2 리셋 서술이 현 구조와 불일치 · **와일드카드 행 형제 소실 실증**
    (`PA9000` deny ↔ `PB9000`/`PD9000` allow · `ZANYPII` ask ↔ `YANYPII` allow) · GetTable
    구도구명.
  - **브랜드 표기 3갈래 혼재** — 헤더 `SAPKIT` 전환 / 콘솔 접두 `[sapkit]` 전환 / 판정 이유
    `sc4sap policy` 유지(가족 4훅 일관 우선). 동작 위반은 아니고 **통일은 별건**.
  - **`sap-stocker.md:29`·`:48` 구경로 잔존**(`exceptions/custom-patterns.md` — 코드스팬이라
    `check-links` 밖).
  - **`screen-image-renderer` QUIRK 5건 동결 마킹** · **`fetch-sap-help-doc` 전송 실패 경로
    exit 크래시**(Node24/Windows undici — 선재 · 채록대로 동결).
  - **⚠ `md` 블록리스트는 상설 게이트가 읽지 않는다** — `conformance-server-gates.mjs`는 **서버
    번들 문자열**만 대조한다. 정책 12의 파싱 회귀에 대한 유일한 방어가 이 판의 **파싱 동치
    검증**이고 그것은 상설 게이트가 아니다. **상설화는 후속 후보.**
  - **신설 러너 CI 편입 = 판5 소관**(D-089 ⓔ) — `test-hook-decisions.mjs`는 지금 로컬 러너다.
    CI(`.github/workflows/offline-gates.yml`)와 `CLAUDE.md` 게이트 목록 등록을 판5가 판단한다.
- **정직 유보 승계 (D-089)**: Description 열 11셀 재저작(일관성 이탈 **명시 수용** — dead
  field · 사실 이탈 0) · 브랜드 표기 혼재 · 훅 파서 갭 동결 · md 블록리스트 CI 무방비 ·
  채록 74케이스는 **현행 동작의 스냅샷이지 「옳은 동작」의 정의가 아니다**(동작을 고치는 판은
  러너부터 고쳐야 한다).
- **최종 리뷰 보완 3건**(판정 후 회수·기록 — 커밋 참조): ① `table_create_sample.abap:10`
  `DDIF_* function group` 사실 변경 → `family`로 회수(형제 표현과 동렬 — 실제 함수그룹은
  SDIF이고 DDIF_*는 접두 계열이라 「function group」 범주 단언이 오류였다). ② 브랜드 표기
  변종은 3이 아니라 **4**다(`fetch-sap-help-doc`는 브랜드 토큰 **완전 제거**형). ③ 문서↔훅
  불일치는 5건이 아니라 **6건**(`table_exception.md:50`의 「`TABLExxx` = `*` 동일」 주장 —
  훅 실동작은 xxx=1회 이상/`*`=0회 이상 · 소문자 `includes` 탐지). 1-E 신조 판정 폭(원문에
  없던 정확한 DDIC 값 주석 추가 허용)은 판4가 선례로 삼을 때 명시 계약으로 승격할 것.

### 판4 — ⑶-c OOP 템플릿 재생성 (집필분) — **완료 (2026-08-18 · D-090)**

- **목표**(당초): `interactive/core/knowledge/abap/templates/oop-sample/`의 `zrsc4sap_*`
  20파일(프로그램 9 + 화면 2 외)을 SAPKIT 이름으로 새로 작성. **레포 템플릿 원본만** —
  SAP 시스템 내 기존 오브젝트는 무접촉.
- **완료 기준**: 새 템플릿 집필 완료 + 개명 게이트(`check-runtime-path-rename.mjs`)
  통과. **실 SAP DEV 생성→활성→기계 확인은 판6으로 이연**, 옛 템플릿 제거는 그 확인
  뒤(판6 완료 전 삭제 금지).

**도달점**: 자체 저작 **신 22파일**(`zsapkit_oop_ex` 계열 · prog 10쌍 + 화면 2)을 신설하고
**소비자 6문서와 개명 게이트의 정본 지위를 신 템플릿으로 전환**했다. 이 판이 새로 연 결정이
**D-090**이다(사용자 상시 위임 + 밤샘 위임 하 **산출자 권고 채택**) — 판3.6이 판4에 넘긴
「옛 앵커 `zrsc4sap_oop_ex` 보존」 지시를 **앵커 전환**으로 뒤집은 것이 그 핵심이다.
**옛 20파일은 무참조 상태로 존치**하며 제거는 판6 검증 뒤다. **명부는 무접촉**이다 —
oop 20은 ⑶-b 명부의 셈 밖(⑶-c 눈금)이고 `copy-baseline.md`의 **170/170·잔량 0**은 이 판으로
변하지 않는다.

- **D-090 결정 5종** (원문이 정본 — 여기는 요지):
  - **ⓐ 앵커 전환** — 게이트 ANCHORS의 ABAP 앵커 **6항을 `zsapkit_oop_ex`로 교체** +
    음성시험 literal 동반 갱신(keychain 1항 무접촉). 게이트 **데이터** 갱신이며 판정 로직은
    무접촉이다.
  - **ⓑ 신·구 병존** — 신 22가 정본 · 옛 20은 무참조 존치 · 제거는 **판6 뒤**(BLUEPRINT §3.2).
  - **ⓒ 소비자 인용의 정본 = 실코드** — 옛 인용의 생성자 모순(`NEW lcl_alv( go_data )`)을 신
    실코드 축자(`go_data = NEW #( ).` / `go_alv = NEW #( ).`)로 해소.
  - **ⓓ 체크 완료편 최소 수정 2곳** — `oop-pattern.md` :3(출처 서술 → 역사 서술) · :45(include
    열거에 `tst` 추가). 앵커 전환이 **만든** 사실 어긋남의 즉시 정합.
  - **ⓔ 코퍼스 픽스처 무접촉** — `sapkit-cli/fixtures/corpus/templates/` 11파일은 **구 vsp
    판정의 유일 잔존 기준선**이다. 신 템플릿으로 「동기화」하면 기준선이 영구 소실된다. **금지.**
- **옛 결함 3종 해소**(신 템플릿에 미승계): ① `INCLUDE ZRSC4SAP_OOP_EXTST` **댕글링**
  (`_tst` 신설로 해소 — `include-structure`의 OOP 필수 규칙 이행) · ② **미선언 변수**
  (`LV_001`/`LV_002`) · ③ **비존재 구문**(`AT SELECTION-SCREEN OUTPUT FOR FIELD`). 그 밖에
  트리 부모키 오배정·미사용 상수 26 등도 승계하지 않았다.
- **검증**: 독립 새-컨텍스트 리뷰 **2팀** · **회수 2건**.
  - **R-TP** = **FAIL 1(MINOR)** — Main 헤더 주석의 INCLUDE 계수 `ten`이 실측 9와 어긋남 →
    회수 `a66c4415`로 `nine` 정정. 그 밖 전 축 PASS: **상속 API 22종 시그니처 대조** ·
    TOP 고아 **0** · 축자 중복률 실측(키워드뿐) · 스크러빙 완결 grep(2차 차용 흔적 신 파일
    **0건**) · **게이트 판정 능력 독립 실증 6/6**(복제 트리에 개명을 심어 게이트가 실제로
    거부하는지 확인 — ⓐ가 판정을 무력화하지 않았다는 증명) · 게이트 11종 exit 0.
  - **RF(전모 리뷰)** = **PASS** — 게이트 **13종** exit 0 · **변경 전모 30파일 정확**
    (신설 22 + 소비자 6 + 게이트 1 + 음성시험 1) · 코퍼스 픽스처 diff **0** · 명부 무접촉 ·
    잔존 `zrsc4sap` 참조는 전부 역사/기준선 문서(HANDOFF·BLUEPRINT·RUN-PLAN·DECISIONS·
    copy-baseline·designs·provenance·fixtures/baseline).
  - **오프라인 검사**: 신 `.abap` **12파일 lint 12/12 무소견** · parse 미해석 0 ·
    자기 참조 정합(INCLUDE 선언 ↔ 실파일) 기계 확인.
  - 커밋: TP1 `bc86bfc9`(22파일 신설) · TP2 `aad03850`(소비자 6 + 앵커 6항) ·
    `afb1e786`(음성시험 literal — 13/13 복귀) · 회수 `a66c4415`.
  - **지식 `.md` 148 유지**(`templates/`는 계수 대상이 아니다) · `plugin-metadata.json`·
    생성물 7종·버전 무접촉.
  ⚠ **push 직전 설치본 확인 의무 승계**(판2·판3.1~3.6과 동일 — D-060 편승 · **미배포
  0.7.0**): `node interactive/scripts/doctor.mjs`로 설치본 manifest version을 확인하고
  **이미 0.7.0 설치본이 있는 머신이 발견되면 패치 범프 후 push**. (`HANDOFF.md` 재개점에도
  있으나 그 블록은 다음 판이 덮어쓰므로, 판이 넘어가도 남는 자리로 여기에 둔다.)

**판6 재료** (attended 판이 이어받는 것 — 이 판이 만든 SAP 부채)

- **⚠ 신 템플릿 22파일은 실 SAP에서 한 번도 검증되지 않았다.** 판6이 **생성 → 활성 →
  기계 확인**(`verify-applied.md`)을 태울 대상은 다음 전량이다: Main `zsapkit_oop_ex` ·
  INCLUDE 9종 `zsapkit_oop_ex{t,s,c,a,e,o,i,f}` + `zsapkit_oop_extst` · 화면 `0100`/`0200`
  (`.prog.xml` 10종은 abapGit 직렬화 동형 구조로 썼을 뿐 **실 왕복 미확인**). lint 무소견은
  **오프라인 검사기의 판정이지 SAP 구문 검사가 아니다.**
- **⚠ 미검증 구문 3곳 (릴리스 하한 포함 — 판6에서 실측할 것)**:
  ① `CALL TRANSACTION … WITH AUTHORITY-CHECK`(**7.52+**로 적었으나 미확인) ·
  ② `UP TO @gc_max_rows ROWS`(호스트 변수 행 상한) ·
  ③ `SELECTION-SCREEN COMMENT`의 전역 텍스트명 **8자 축약**.
- **⚠ 옛 20파일 제거 조건** — 위 SAP 검증이 **통과한 뒤**에만 제거한다(D-090 ⓑ ·
  BLUEPRINT §3.2). 통과 전 삭제는 되돌아갈 자리를 없앤다. 제거하더라도
  `sapkit-cli/fixtures/corpus/templates/`는 **그대로 둔다**(ⓔ — 그쪽은 옛 템플릿의 기준선
  스냅샷이고 소스가 사라져도 기준선으로 계속 산다).
- **⚠ `_tst` 접미 표기 재고**(R-TP F3) — 파일·INCLUDE 이름은 옛 관습 승계로 `…_extst`
  (EXTST)인데 `create-program.md`의 규범 표기는 `{PROG}_tst`다. 어느 쪽이 SAP에서 실제로
  통하는지 판6이 정하고, 갈림이 남으면 그때 한쪽으로 닫는다.
- **`LCL_EVENT_SALV` 3메서드는 미배선 참조 형판**이다(주석에 명기 — 죽은 코드 아님).
  배선 여부는 소비 판이 정한다.

**이월 승계** (이 판이 손대지 않은 것 — 판3.6 브리프의 「판4 재료」 전량 + 판4 신규)

- **판3.6분 전량 그대로**: 소비자 오인용 4건 · 사실 의심 2건 · 제품명 표기 갈림 · 이식 전
  참조 보존 목록 21곳(⚠ 그중 1곳은 소비자 오인용 ④와 같은 건 — **이중 계수 금지**) ·
  소비자 서술 불일치 4건 · 헤딩 재작성 기록 2건 · 판3.3~3.5 등재 사실 의심 전부 ·
  **참조표 사실 의심 밀도 경보 BW·QM·TR·TM·WM** · 낡은 도구명 동결 55곳 · 훅 파서 갭 4종 ·
  문서↔훅 불일치 6건 · 브랜드 표기 4갈래 혼재 · `sap-stocker.md:29`·`:48`과
  `data-extraction-policy.md:116` 구경로 · `screen-image-renderer` QUIRK 5건 ·
  `fetch-sap-help-doc` exit 크래시 · **md 블록리스트 상설 게이트 무방비** ·
  **`SD/bapi.md:31` 잔존 1건**(수리 커밋 1건이면 닫힌다 — 여전히 열려 있다).
- **⚠ 소비자 4곳 INCLUDE 순서 정렬은 여전히 열려 있다** — 판3.6이 「판4가 함께 정리하는 것이
  자연스럽다」고 넘겼으나 **판4 사양이 「6문서의 산문 개정(토큰 치환 외)」을 범위 밖으로
  명시**해 손대지 않았다. 대상은 `include-structure.md:24`·`:32` ·
  `clean-code-procedural.md:13` · `review-checklist.md:208`(MAJOR 게이트)이고, 함께
  표면화된 자기모순 S1~S6도 그대로다.
- **판4 신규 등재**: **F3** `_tst` 접미 표기 갈림(위 판6 재료) · **F4** `include-structure`의
  「6필드 머리글」 규범 **자기 붕괴**(선재 — 이 판의 신설물이 만든 것이 아니다 · S1과 같은
  계열) · **F9** `../common/` 경로 표기(선재 — 소비자가 쓰는 상대 경로 표기가 실제 폴더
  구조와 어긋나는 자리) · **F8** 검사기 `parse`의 `Unknown` 도달 불가(신호가 약해 실증거는
  수기 대조였다 — 검사기 쪽 후속 후보).

### 판5 — ⑴ 엔진 잔여 코드 (M2 인증 · D18 · keyring CI · 러너 CI 편입) — **완료 (2026-08-18 · D-091)**

- **목표**(당초): ⓐ M2 인증 확장 코드 — UAA 토큰 취득 + 그 위의 JWT 접속 계층(신 엔진
  `AdtClient`는 Basic만 다룬다 — M-표에서 M2만 `일부`인 이유) ⓑ D18 — 무접속 거부
  어휘를 `conformance-server-gates.mjs`가 구·신 **둘 다** 인식하게 넓히기(신 문구로
  교체 금지 — 구 번들이 현역인 동안 그쪽 판정이 깨진다) ⓒ keyring 폴백 CI 잡
  (`npm ci --omit=optional` — fallback 설계가 아직 어떤 CI로도 입증되지 않았다).
  ⓓ **`test-hook-decisions.mjs` CI 편입 판단**(D-089 ⓔ가 판5로 이연한 몫) — 지금은 로컬
  러너다. CI(`.github/workflows/offline-gates.yml`)와 `CLAUDE.md` 게이트 목록에 올릴지를
  이 판이 정한다(케이스 안정성 실측이 판단 재료).
- **완료 기준**(당초): jest·자체 게이트 green · M2가 M-표에서 `짓기 완료`로 · 교체 전 필수
  2건(D15 코드 측·D18)이 코드로 해소됨 · ⓓ가 결정으로 닫힘. 증거(destination 실접속)는
  판6 몫. ⚠ **이 중 「M2를 `짓기 완료`로」는 D-091 ⓑ가 기각했다** — 그 행의 승격 기준 ⓐ가
  **실접속 성공**이라 오프라인 판이 채울 수 없다. 나머지 셋은 성립했다. ⚠ ⓒ의 방식도
  `--omit=optional`에서 **require seam 차단으로 바뀌었다**(D-091 ⓒ — 더 강한 증거).

**도달점**: 네 갈래를 전부 닫았다 — ⓐ **M2 인증의 오프라인 절반**(auth 모듈 6종 신설 +
`ConnectionConfig` 선택 4필드 + `AdtClient`의 Bearer 분기 + `planServiceKeyConnection`) ·
ⓑ **D18 이행**(제품 게이트의 무접속 분류 어휘를 **구·신 병행 인식**으로 확장 + 엔진측
방어 소시험 신설) · ⓒ **keyring 폴백 실질 증명**(require seam 차단 스모크 12건 + 루프백
인바운드 0건) · ⓓ **러너 CI 편입**(D-089 ⓔ 이행 완료). 이 판이 연 결정이 **D-091**이다
(사용자 상시 위임 + 밤샘 위임 하 **산출자 권고 채택**). **변경 전모 25파일** · jest
**4,139 통과 / 1 skip** · 제품 게이트 **19종 전부 exit 0**. **M-표 승격은 없다 — M2는
`일부` 그대로**이고 실접속 검증은 판6 몫이다. **제품은 무접촉** — 구 번들(엔진 5.0.0)이
여전히 현역이고 교체는 판7-b다.

- **D-091 결정 4종** (원문이 정본 — 여기는 요지):
  - ⓐ **게이트 편집은 D-결정·판 큐가 명시 배정한 것만** — D18 이행이 그 첫 적용이다.
    `conformance-server-gates.mjs:316`의 무접속 분류 정규식을 **alternation으로 확장**했고
    (구 문구 유지 + `ERR_NO_CONNECTION` 추가 · **교체 금지**), 편집 실체는 **어휘 데이터
    2줄 · 판정 로직 무접촉 · C1 엄격도 불변**이다(리뷰 실증). 되돌림 방어로
    `sapkit-engine/gates/test-refusal-vocab.mjs`(**8건** — 제품 정규식을 복제하지 않고
    **읽어 와** 대입)를 신설해 CI에 배선했다.
  - ⓑ **M2 완료선 = 오프라인 절반** — 코드는 다 있으나 **M-표 승격 없음**(승격 기준 ⓐ =
    실접속). **startup 미배선은 의도된 정지선**이다 — 토큰 취득은 UAA 실왕복이고
    `authorization_code`는 사람이 필요하므로 기동이 말없이 시작하지 않는다(attended
    명시성). 설계 셋: 토큰 **무상태**(메모리 + `exp` 60초 버퍼 · keychain 영속 유보 —
    **D-080 ① 정합**) · 갱신 실패 **비폴백**(명시 진단 — 구 부품의 조용한 재로그인
    미승계) · **브라우저 미개시**(인가 URL 전달만). 세부 정본 = DIVERGENCES **D116**.
  - ⓒ **keyring 증명 방식 = require 실행의 관측** — **DI 주입만으로 「폴백 입증」을
    주장하지 않는다.** `--omit=optional` 트리보다 강한 증거다(모듈 부재 트리는 코드가
    require를 아예 안 불러도 똑같이 초록이라 아무것도 증명하지 못한다).
  - ⓓ **훅 판정 러너 CI 편입 완료**(D-089 ⓔ 이행) — `test-hook-decisions.mjs` **74케이스**를
    `node-gates` 잡의 `test-hook-switch` 직후에.
- **검증**: 독립 새-컨텍스트 리뷰 **2건** · **회수 1건**.
  - **R-E PASS**(기술 결함 0 · 절차 2) — E1 **비차용 축별 대조**(시크릿 위치·오류 문구·토큰
    수명·콜백 구조가 전부 갈린다) · **Basic 경로 비트 불변** · 시험 실효성(목이 실코드를
    실제로 타는지) · 시크릿 로그 **0** grep · DIVERGENCES 인용 행 실사 · 게이트·음성시험
    전종 재실행. **F-1**(D-091 미존재 — **이 마감이 해소**) · **F-2**(refusal-vocab 시험이
    CI에 없음) → **회수 `681e1f8b`**(CI 스텝 + `CLAUDE.md` 엔진 스모크 2종 등재).
    F-3(인용 1행 오차) · F-4(커밋 제목 어휘)는 **INFO**.
  - **RF PASS** — 변경 **전모 25파일** 정확 · 제품 게이트 **19종 exit 0** · 엔진 자체 게이트
    전종 · CI yaml 파스 정합.
  - **변이 시험으로 스모크의 실효성을 실증했다** — keyring 12케이스 중 **8케이스 red**로
    fail-open 역사 버그를 재현·검출했고 인바운드 3건도 잡았다(**에러 문자열의 부재는
    트래픽의 부재가 아니다** — 루프백 리스너를 세운 이유).
  - 커밋 = E1 `fbe685d5` · E2 `0bb8e040` · E3 `74829a89` · 회수 `681e1f8b`. 지식 `.md`
    **148 유지** · `plugin-metadata.json`·생성물 7종·버전 무접촉 · **`TOOL-LEDGER.md`
    무접촉**(접속 계층은 도구가 아니다 — **증거 대기 143종 그대로**).
  ⚠ **push 직전 설치본 확인 의무(D-060 편승 · 미배포 0.7.0) — 이 판은 실제로 이행했다**:
  `node interactive/scripts/doctor.mjs` 실측 결과 **이 머신 설치본 0.5.3**(레포 0.7.0보다
  낮다) → **패치 범프 불요**. **다른 머신은 그 머신에서 다시 실측할 것** — 0.7.0 설치본이
  발견되면 패치 범프 후 push.

**판6 재료 통합 목록** (attended 판이 태울 것 — **판6이 볼 단일 재료 목록**)

1. **M2 실접속 검증**(판5 코드의 증거) — destination 프로파일로 **실접속 성공**
   (inspection-only 강등 아님) · 진단 `MCP_DESTINATION_TOKEN_PENDING` /
   `MCP_DESTINATION_UNSUPPORTED`가 **더 이상 나오지 않는다** · **기존 Basic 경로 회귀 0**
   → **D15 해소** → **M-표 M2 승격**. 배선하며 함께 확인할 2건(D-091 정직 유보 ⓒ·ⓓ):
   **opaque 토큰(`exp` 없음)은 만료를 모르므로 호출부가 `invalidate()`를 명시로 불러야
   한다** · **`authType:'jwt'` 무토큰 생성자 throw의 진단 품질**(기동 자리에서 읽을 만한
   문구인지 아직 확인되지 않았다).
2. **D91·D112 전제 재확인** — `authType === 'jwt'`가 **이제 가능한 값**이라 두 항목의 근거
   한 다리가 바뀌었다(판정 결과는 그대로). 되짚을 자리는 **JWT 접속에서 그 도구 20종**
   (D91 16 + D112 4)**이 정말 `tools/list`에 안 뜨는가**다 — DIVERGENCES 「D91·D112 전제
   변화」 절이 정본.
3. **판4 신 템플릿 22파일의 SAP 확인** — 생성 → 활성 → **기계 확인**
   (`interactive/core/procedures/verify-applied.md`). **대상 전량 목록 · 미검증 구문 3곳 ·
   `_tst` 접미 표기 재고(F3)는 위 판4 브리프의 「판6 재료」**가 든다.
4. **옛 템플릿 20파일 제거 조건** — 위 3이 **통과한 뒤에만** 제거한다(D-090 ⓑ ·
   BLUEPRINT §3.2 — 통과 전 삭제는 되돌아갈 자리를 없앤다). 제거하더라도
   `sapkit-cli/fixtures/corpus/templates/` 11파일은 **그대로 둔다**(D-090 ⓔ — 구 vsp
   판정의 유일 잔존 기준선).
5. **잔여 증거 143종** — 재생 대조 **96** + attended 실기 **47**. **순서 M1→M6 불변**
   (D-081 · D-082 ④). 판정 파일은 `sapkit-engine/evidence/replay/`에 쌓이고, 그것이
   쌓여야 대장의 「증거 대기」가 실제로 줄어든다.
6. **CI 첫 push 실측** — 판5가 더한 **3스텝**(`test-hook-decisions` · keyring 스모크 ·
   `test-refusal-vocab`)은 로컬에서 **js-yaml 파스까지만** 확인됐다. GitHub Actions 스키마
   정합과 러너에서의 실제 동작(특히 keyring 스모크의 **headless skip 분기**)은 **첫 CI가
   처음 보여 준다.**
7. **판6.1 `/ready`가 물을 것** — 재생 대상 96종 **전량 채록** vs **실사용-우선 채록 + 꼬리
   증거 기준 인하**(별도 D-append 필요). **이 결정이 판6 전체의 크기를 정한다.**

**이월 승계** (이 판이 손대지 않은 것 — 판4 브리프의 「이월 승계」 전량 + 판5 신규)

- **판4분 전량 그대로**: **`SD/bapi.md:31` 잔존 1건**(수리 커밋 1건이면 닫힌다) ·
  **소비자 4곳 INCLUDE 순서 정렬**(`include-structure.md:24`·`:32` ·
  `clean-code-procedural.md:13` · `review-checklist.md:208`)과 함께 표면화된 자기모순
  S1~S6 · 소비자 오인용 4건 · 사실 의심 2건 · **브랜드·제품명 표기 갈림**(`SAPKIT` vs
  `sc4sap` · 훅 4갈래) · 이식 전 참조 보존 목록 **21곳**(⚠ 그중 1곳은 소비자 오인용 ④와
  **같은 건** — **이중 계수 금지**) · 소비자 서술 불일치 4건 · 헤딩 재작성 기록 2건 ·
  판3.3~3.5 등재 모듈별 사실 의심 전부 · **참조표 사실 의심 밀도 경보 BW·QM·TR·TM·WM** ·
  낡은 도구명 동결 55곳 · **훅 파서 갭 4종** · 문서↔훅 불일치 6건 ·
  `screen-image-renderer` QUIRK 5건 · `fetch-sap-help-doc` exit 크래시 ·
  **md 블록리스트 상설 게이트 무방비** · 판4 신규 **F3**(`_tst` 표기 갈림) · **F4**
  (「6필드 머리글」 규범 자기 붕괴) · **F8**(검사기 `parse`의 `Unknown` 도달 불가) ·
  **F9**(`../common/` 경로 표기).
- **판5 신규 등재 3건**:
  - **C1 evidence 문자열이 구 문구만 명시한다** — `conformance-server-gates.mjs:612`.
    **지금은 정확하다**(러너가 구 번들만 검사한다). 정리는 **구 번들이 제품 자리에서 물러나는 판7-b** 몫
    이고, 그때 함께 보지 않으면 조용히 낡는다.
  - **토큰 keychain 영속은 유보로 닫혔다** — 무상태를 골랐고(D-091 ⓑ ①), 도입하려면
    **별도 D-결정**이 필요하다(D-091 대안 (d)).
  - **`CLAUDE.md`의 엔진 게이트 서술이 느슨하다** — 게이트 목록 문장이 실제 스크립트
    구성과 정밀하게 맞지 않는 자리가 남아 있다(판5가 2줄을 더했으나 서술 자체의 정비는
    **별건**).

### 판6.1 — M1 증거 완주 (첫 attended SAP 판) — **완료 (2026-08-18 · D-092)**

- **한 일**: M1 19종의 잔여 증거를 전부 세워 **M1을 닫았다**. ⓐ 기존 픽스처 3종 재생 →
  판정 파일 3건(7종 기록화) ⓑ 연습 대상 신설 3종을 **제품 MCP(0.7.0)로** 생성·활성
  (도그푸딩 겸행) ⓒ **C3 대표 절차** — 사람이 `ZSAPKIT_M1_DEMO` 삭제 → 신 엔진이 같은
  생성 절차 수행 → 제품 MCP로 기계 확인 ⓓ `CreateInclude` 실기 녹화 ⓔ 신규 재생 **9종**
  시나리오 5벌 저작·녹화·재생 ⓕ 차이 장부 **D1 활성화**(이연 종료).
- **수치**: 대장 **증거 대기 143 → 126 · 증거 있음 43 → 60**. **M1 19종 전부 「증거 있음」.**
  jest **4,151 통과 / 1 skip** · 엔진 게이트 전종 + 음성시험 44 · 제품 게이트 9종 exit 0
  (`doctor`의 FAIL 1은 착수 전과 같은 선재 로컬 환경 상태).
- **결정** = **D-092**: 실사용 우선 채록 + 꼬리 기준 인하(**집행은 판6 종료판**) ·
  M2 destination 계열 **보류**(BTP 부재 · 순서 파급) · P2 실데이터 **단 한 건** 지정 ·
  비결정 필드(`execution_time`)는 **등재가 아니라 정규화**가 맡는다.
- **이 판이 남긴 함정 메모**는 `sapkit-engine/harness/scenarios/README.md`의
  「이 묶음이 남긴 설계 메모 셋」이 든다(치환은 같은 글자로 · 인클루드 숙주는 따로 ·
  비활성 목록은 인자가 없다).

### 판6.x — 최종 테스트 판 (attended · 유일한 SAP 구간 · 반복 가능)

- **목표**: 미뤄 둔 SAP 증거 전부 + 실사용 확인. **순서 고정 M1→M6**(D-081):
  1. ~~M1 잔여~~ — **판6.1에서 닫혔다**(2026-08-18 · D-092). 재생은 **9종**이었고
     `GetSourceDiff`는 요구 급이 `계약 시험`이라 애초에 재생 대상이 아니었다
     (이 자리에 「재생 대상 10종(`GetSourceDiff` 포함)」으로 적혀 있던 것이 오기다 —
     기계 정본은 `sapkit-engine/harness/build-plan.json`). `CreateInclude` 실기와
     C3 실기도 함께 닫혔다.
  2. M2 — destination 프로파일 실접속(판5 코드의 증거)
  3. M3 — 전송 3종·RFC 5경로 실증 4. M4·M5 — 도구 증거 5. M6 — 관문(D8 관찰 ·
     D18 확인 · 꼬리 재평가 → 새 D-항목 · 검증 기준 1~4)
  - 추가: **판4 신 템플릿 22파일의 SAP 확인**(생성→활성→기계 확인) → 통과 후 옛 20파일
    제거. **대상 전량 목록 · 미검증 구문 3곳 · 제거 조건 · `_tst` 표기 재고는 위 판4
    브리프의 「판6 재료」**가 든다(D-090 ⓑ).
  - 실사용 도그푸딩은 채록과 겸행 — 실제로 쓴 흐름을 그 자리에서 시나리오로 만든다.
- **~~첫 판(판6.1)의 /ready가 물을 것~~ — 물었고 답이 나왔다**(D-092 ⓐ):
  **실사용-우선 채록 + 꼬리 증거 기준 인하**. 전량 채록은 기각했다. **인하의 집행은
  판6 종료판** 몫이고 그때까지 대장의 「증거 대기」는 인하 미반영 수치다.
- **판6.2의 재료** (판6.1이 넘긴 것):
  1. **`--env=<name>` 명명 프로파일 접속 확인** — BTP 불요(D15 표의 `--env` 행).
     M2가 봉쇄돼도 이것은 선다.
  2. **판4 신 템플릿 22파일의 SAP 확인**(생성→활성→기계 확인) → 통과 후 옛 20파일 제거.
     대상 목록·미검증 구문 3곳·제거 조건·`_tst` 표기 재고는 위 판4 브리프의 「판6 재료」.
  3. **D91·D112의 JWT 축 20종이 `tools/list`에 안 뜨는지** 실측.
  4. **`GetSession`·`GetSystemInfo` 채록(M4)** — 응답에 **실명이 실린다**는 것을 판6.1이
     실측했다(`user_full_name`). 채록하려면 **마스킹 규칙 보강을 먼저** 검토할 것.
  5. **M2는 BTP 계정이 생기기 전까지 열 수 없다**(D-092 ⓑ). 순서 고정(D-081) 때문에
     M2 이후 「증거 완료」 전체가 그 뒤에 있다 — 단 **교체 검증 기준 1~4는 M2 실접속을
     요구하지 않는다**.
  6. **판6.1 독립 리뷰가 짚은 구조적 이월 셋**(전부 **별도 판단 필요** — D-092 정직 유보 ⓕ):
     ① **행 데이터 픽스처 하나가 배치 재생을 통째로 막는다** — 안전한 나머지 7종의 회귀
     재생까지 `--fixture` 단건이 된다. 별도 디렉터리 분리 또는 게이트 완화 중 택일이고
     **판정 기계를 고치는 일이라 D-결정이 필요**하다. ② `substituteEvidenceFromLedger`가
     **경로 실재만 보고 「통과」를 찍는다**(그 시험이 실제로 통과했는지 보지 않는다).
     ③ **판정 파일 ↔ 픽스처 대조 게이트가 없다** — 손으로 쓴 판정 파일이 대장을 부풀릴
     수 있다. 판6.1은 리뷰어가 **손으로** 9건 전건 대조했다.
- **재료**: `sapkit-engine/harness/scenarios/README.md`(**함정 ⑶** + 판6.1이 더한
  「설계 메모 셋」) · `sapkit-engine/TOOL-LEDGER.md`(증거 대기 **126**) ·
  **위 판5 브리프의 「판6 재료 통합 목록」** — 이 판이 태울 것의 단일 목록이다(여기부터 읽는다).
- **완료 기준** (**D-093 ⓐ로 갈렸다** — 2026-08-19): 이 줄은 원래 「대장 증거 대기 0 ·
  검증 기준 1~4 성립 · M1~M6 전부 `증거 완료`」 **셋 다**를 요구했다. 그중
  **「대장 증거 대기 0」과 「M1~M6 전부 `증거 완료`」는 교체(판7-b)의 전제가 아니다** —
  M-표는 제작 진도표이고 교체 관문은 **검증 기준 1~4** 하나다. 그래서 이 판 계열의
  완료는 이제 **두 갈래로 읽는다**:
  - **교체를 여는 조건** = BLUEPRINT 사다리 ⑴ **검증 기준 1~4 성립**. (판6.2 실사 기준:
    1·3·4 성립 · **2만 배선 필요** — 아래 판6.2 브리프.)
  - **제작 진도** = 대장 증거 대기 0(또는 꼬리 기준 인하 반영 후 잔여 0) · M1~M6
    `증거 완료`. **이쪽은 교체 뒤에도 계속 쌓인다.** 인하 집행은 D-092 ⓐ대로 판6 계열의
    종료판 몫이고, 이제 그 종료판이 **교체의 전제가 아니게 됐다**(D-093 ⓐ) — 인하는
    대장 수치의 정확성 문제로만 남는다.

#### 판6.2 — ⑶-c 마감 + 저비용 확인 + 사다리 방향 확정 — **완료 (2026-08-19 · D-093)**

- **판을 자른 기준**: 「SAP이 필요한가」 × 「새 코드가 필요한가」. 둘이 섞이면 희소 자원인
  attended 세션 한복판에서 설계 결정을 하게 된다. 그래서 M2는 **오프라인 배선 판 →
  attended 실접속 판**으로 갈라 뒤로 보냈다(판 큐의 `판M2-a`·`판M2-b`).
- **낸 것 넷**:
  1. **⑶-c 템플릿 갈래 마감** — 신 템플릿 **핵심 10종**(프로그램 1 + 인클루드 9)이
     온프렘 DEV `$TMP`에서 **생성·활성·되읽기 대조** 통과, 옛 20파일 제거.
     시험 인클루드 `zsapkit_oop_extst` → `zsapkit_oop_ex_tst` 개명(근거는 **규범 정합**
     하나 — F3의 「SAP이 정한다」는 전제가 틀렸다. 두 이름 다 유효하다).
     루트 `.gitattributes`로 ABAP 소스 LF 고정.
  2. **저비용 확인 2건** — `--env=<name>`은 **`~/.sapkit/profiles/`가 아니라
     `<home>/Documents/mcp-abap-adt/sessions/`를 본다**(구조적 발견). 같은 해석기를
     `--env-path`로 태운 실접속은 성립. JWT 축 20종은 **배포 축에서 0/20 노출**.
  3. **교체 관문 실사 1회** — 아래 「판7에 남은 것」.
  4. **결정 셋**(D-093 ⓐⓑⓒ) — 교체 관문 = 검증 기준 1~4 · M3 완료 = 전송 3종 ·
     attended 실기 46종 전용 판.
- **⚠ 못 선 것 — 화면 2종 · GUI 상태 2종(0/4)**. 원인은 템플릿이 아니라
  **`ZMCP_ADT_SRV` OData 서비스의 Dispatch 핸들러 고장**이다(등록·라우팅은 살아 있다 —
  Gateway 에러로그에 `Service: /SAP/-ZMCP_ADT_SRV-0` Frontend Error가 남는다.
  「미설치」가 아니다). 그래서 **`ZSAPKIT_OOP_EX`는 활성이지만 실행 완주 불가**이고,
  `.prog.xml` 10파일과 화면 흐름로직 2개는 **미확인으로 남는다**. 「22파일 확인 완료」로
  적으면 안 된다.
- **판7에 남은 것** (실사 산출 — **1~3은 `판7-a`가 소진했다**, 2026-08-19):
  1. ~~**기준 2 배선**~~ — **완료**(판7-a · D-094 ⓐⓒ). 두 스크립트에 `--target` 이름표를
     달았고 B0의 단일번들 전제를 다중 파일에 맞게 다시 세웠다.
  2. ~~NODE_PATH 분기 · 기준 파일 이원화 결정~~ — **완료**(판7-a · D-094 ⓐⓑ). NODE_PATH는
     대상표에 묶였고, 기준 파일은 **구·신 공용 하나**(`provenance/mcp-surface.json`)로
     확정했다 — 신 엔진이 제 채록본으로 자기를 채점하지 않게 하려는 것이고, 그 귀결로
     `--target=engine`+`--update`를 금지했다.
  3. ~~`GetTableContents`의 **MCP 왕복 음성시험**~~ — **해소**(판7-a · 예고대로 기준 2
     배선이 끝나자 자동으로 닫혔고, 「닫힐 것이다」가 아니라 **콜별 실측으로 확인**했다).
  4. (선택) tool-catalog 문서 3종 ↔ `tools/list` **자동 대조 게이트** — **여전히 없다**.
  - **기준 1·3·4는 지금도 선다** — 1: 문서 3종 실측 90/79/15=**184** ↔ 등록점 186,
    차 2종은 행 데이터 도구의 **의도적 제외**라 실질 diff 0. 3: 안전 게이트 13 +
    음성시험 44 + jest 4,153. 4: 판6.1 C3 기록 대조.
- **이월**(이 판이 손대지 않은 것): `verify-applied.md` ②의 `GetProgFullCode` 1순위 지정
  결함(**공백을 붕괴시켜 거짓 불일치를 만든다 — 이월 1순위**) · 판6.1 구조적 이월 셋
  (D-092 정직 유보 ⓕ) · 꼬리 기준 인하 집행 · `GetSession`·`GetSystemInfo` 채록(마스킹
  보강 선행) · BLUEPRINT의 낡은 jest 수(4,001 → 실제 4,153).

#### 판6.3 — attended 실기 46종 (`Create*`→`Delete*` 왕복) · **완료 (2026-08-20 · D-098)**

- **왜 이 판인가 — 두 가지를 한 번에 한다.**
  ⑴ **증거**: 증거 급 `attended 실기`인 46종은 **실사용 축적으로 절대 안 줄어든다**
  (D-093 ⓒ) — 정상 업무 흐름이 `Delete*` 25종을 부르지 않기 때문이다.
  ⑵ **교체 후 첫 실사용 사이클**: BLUEPRINT §3.1 ③이 `engine/` 은퇴(판7.5)의 전제로
  「전환 후 **최소 1회의 실사용 사이클**」을 요구하는데 판7-b는 **SAP 접속 0회**였다
  (D-096 ⓔ). 이 판이 그 자리다 — **신 엔진이 제품 자리에서 SAP을 실제로 미는 첫 판.**

- **대상 46종** (대장 `sapkit-engine/TOOL-LEDGER.md` 「증거 대기」에서 기계로 뽑은 것.
  `CreateInclude`·`CreateProgram`은 판6.1에서 이미 닫혀 여기 없다):
  - **`Create*` 21** — Class · Table · FunctionModule · Structure · View ·
    BehaviorDefinition · DataElement · ServiceBinding · Domain · **Transport** ·
    MetadataExtension · **GuiStatus** · TextElement · **Screen** · ServiceDefinition ·
    BehaviorImplementation · FunctionGroup · Interface · CdsUnitTest · **Package** ·
    UnitTest
  - **`Delete*` 25** — BehaviorDefinition · BehaviorImplementation · CdsUnitTest ·
    Class · DataElement · Domain · FunctionGroup · FunctionModule · **GuiStatus** ·
    Include · Interface · **LocalDefinitions** · **LocalMacros** · **LocalTestClass** ·
    **LocalTypes** · MetadataExtension · Program · **Screen** · ServiceBinding ·
    ServiceDefinition · Structure · Table · TextElement · UnitTest · View

- **⚠ 착수 전에 알아야 할 함정 넷** (판7-b가 대장을 훑으며 찾은 것 — 하나씩 부딪히면
  판이 늘어진다):

  1. **`CreateTransport`·`CreatePackage`는 `$TMP`에서 안 끝난다 — 둘 다 P4다.**
     AGENTS.md의 P4 정의가 「package/request create, assignment, release, or import」이고
     **P4는 CLAUDE.md 「판 실행 규약」의 묻는 자리 ②**다. `$TMP`는 로컬 객체 전용이라
     transport가 붙지 않으므로(판6.2 실측: 연습 객체 10종 전부 `transport_request: null`)
     이 둘만은 **다른 자리**가 필요하다. **판 착수 시 사용자에게 먼저 물을 것** — 이 둘을
     이 판에서 뺄 것인가, 아니면 P4 승인을 받고 전용 패키지를 세울 것인가.
     후자를 고르면 D-093 ⓕ가 기각했던 「전용 패키지가 서는 판」이 여기서 열린다.
  2. **화면·GUI 상태 4종은 지금 서지 못할 공산이 크다.**
     `CreateScreen`·`CreateGuiStatus`·`DeleteScreen`·`DeleteGuiStatus`는 판6.2에서
     **0/4로 실패한 그 경로**다. 원인은 템플릿이 아니라 **`ZMCP_ADT_SRV` OData 서비스의
     Dispatch 핸들러 고장**이고(등록·라우팅은 살아 있다 — D-093 정직 유보 ⓑ),
     **다음 걸음은 Basis 설치 확인이 아니라 Gateway 에러로그 + `ZMCP_ADT_FLUSH_CACHE`
     (register/diag)**다. 이 넷을 태우기 전에 그 고장부터 보거나, **못 선다고 적고 넘긴다.**
     ⚠ 「미설치」로 오귀인하지 말 것 — 판6.2가 한 번 그렇게 적었다가 독립 리뷰가 반증했다.
  3. **`DeleteLocal*` 4종은 숙주가 있어야 한다.** Definitions·Macros·TestClass·Types는
     독립 객체가 아니라 **클래스의 로컬 인클루드**다. 판6.1이 인클루드 재생을 위해
     `ZSAPKIT_M1_INCMAIN`을 따로 세운 것과 같은 구조 — **숙주 클래스를 먼저 만들고**
     거기에 로컬 4종을 채운 뒤에 삭제해야 왕복이 성립한다.
  4. **RAP/CDS 계열은 시스템 지원 여부가 미확인이다.** `Create/Delete` 짝인
     BehaviorDefinition · BehaviorImplementation · ServiceBinding · ServiceDefinition ·
     MetadataExtension · CdsUnitTest는 **온프렘 DEV에서 되는지 이 레포가 실측한 기록이
     없다.** 배치를 짜기 전에 **한 종으로 정찰 1회**를 먼저 하고, 안 되면 그 사실을
     증거 대신 **장부에 적고 넘긴다**(못 한 것을 한 것처럼 적지 않는다).

- **안전** (이 판은 P3 write가 본체다 — 대충 넘길 자리가 없다):
  - **`$TMP` 로컬 객체만.** DEV tier 확인 후 시작하고, 남의 패키지·남의 객체는 건드리지
    않는다. 함정 1의 P4를 사용자가 승인하지 않으면 그 둘은 **하지 않는다**.
  - **보호 자산 8종**(`ZSAPKIT_M1_DEMO` · `ZCL_SAPKIT_M1_DEMO` · `ZSAPKIT_M1_INCMAIN` ·
    `ZSAPKIT_M1_INC01` · `ZSAPKIT_M1_TAB` · `ZSAPKIT_M1_STR` · `ZSAPKIT_M1_FG` ·
    `Z_SAPKIT_M1_FM`)을 **마스크 3종**(`ZSAPKIT*` · `ZCL_SAPKIT*` · `Z_SAPKIT*`)으로
    **착수 전 · 중간 · 정리 후 세 번** 뜬다. ⚠ 이름 규칙 `ZSAPKIT_M1_*`로 거르면
    **뒤 둘을 놓친다**(판6.2가 실측). 도출 규칙은 「`evidence/replay/`의 판정 파일들이
    부르는 이름 전량」이다.
  - **연습 대상은 반드시 새로 만든다.** 기존 객체를 삭제 대상으로 쓰지 않는다.
  - **P2 0건** — 이 판에 행 데이터를 끌어올 이유가 없다.
  - **정리까지가 이 판이다.** 만든 것을 전부 지우고 재조회로 0건을 확인한다(판6.2 선례).
- **겸행할 것 둘** (D-097로 이 판에 배정):
  - **D8 관찰 판정** — 장부 D8(잔존 축소분: `406`/`415` 재협상 · 추가 폴백 경로 ·
    `skipSessionType`)의 판정 조건은 마일스톤이 아니라 **「C1 녹화에서 발동이
    관찰되는가」**다. 이 판이 그 녹화를 대량으로 낸다. 관찰되면 **결함으로 승격**하고,
    안 되면 **관찰되지 않았다고 적는다**(「없다」가 아니라).
  - **꼬리 기준 인하 집행**(D-092 ⓐ) — `harness/build-plan.mjs` 산식 변경 + 대장 재생성.
    대장의 「증거 대기 126」은 지금 **인하 미반영 수치**다. 이 판이 46종을 태우고 나면
    수치가 두 축으로 움직이므로 **한 번에 집행하고 전후를 함께 적는다.**
- **완료 기준**: ⓐ 46종 중 **실제로 선 것**과 **못 선 것**이 이유와 함께 갈려 있다
  (전량 성공이 조건이 아니다 — 함정 2·4가 실재한다) · ⓑ 대장이 그만큼 움직였고
  `render-ledger --check` 통과 · ⓒ 보호 자산 8종 무사 · 연습 객체 잔재 0 ·
  ⓓ **실사용 사이클 1회 성립** → 판7.5의 전제가 선다 · ⓔ 제품 게이트 전종 여전히 green.
- **재료**: `sapkit-engine/TOOL-LEDGER.md`(대상 정본) · `harness/scenarios/README.md`
  (함정 ⑶ + 설계 메모 셋) · `harness/ADDING-A-TOOL.md` · 판6.1/판6.2 브리프의 SAP 함정.

- **도달점** (2026-08-20 · 실행 기록 `docs/reference/audits/2026-08-20-phase63-attended-record.md`):
  - **46종 갈림 — 34종 섰다 / 12종 못 섰다.** 기계 집계이고 사람이 센 것이 아니다. 방법은
    **「대상 46종 안에서 픽스처의 도구별 `isError`를 센다」**이고, **대상 필터를 빼면 36이 나온다**
    (`CreateProgram`·`CreateInclude`가 늘어나는데 그 둘은 판6.1에서 이미 닫혀 이 판의 대상이
    아니다). 픽스처 **9편 · 107단계**, 엔진은 전부 `sapkit-engine 1.0.0`.
    못 선 12종은 **이유가 네 부류**다 — ⓐ **원리상 불가·SAP 호출 0회 2종**
    (`DeleteUnitTest`·`CreateCdsUnitTest`: 엔진 소스가 이미 적어 둔 사연을 실기로 확인했다)
    ⓑ **거짓 음성 2종**(`CreateMetadataExtension`·`CreateServiceDefinition`: **객체는 생겼는데
    도구가 오류를 보고한다** — 사슬이 생성 → 잠금 → 검사 순서라 검사가 깨져도 생성은 끝난 뒤다)
    ⓒ **시스템 측 고장 6종**(텍스트·화면·GUI — `ZMCP_ADT_SRV`가 HTTP 500. **브리프의 함정 2가
    예고한 4종이 아니라 6종이다** — 텍스트 엘리먼트도 같은 서비스를 탄다는 것이 이 판의 새 관측)
    ⓓ **엔드포인트 부재/P4 실패 2종**(`CreateUnitTest`: `/abapunit/runs`가 404 · `CreatePackage`).
  - **실사용 사이클 1회 성립** → 판7.5의 전제가 선다. **근거를 이름으로 적는다**:
    `sapkit-engine/fixtures/attended-only/zsapkit63-domain-dataelement.json`의 **0번 단계
    `CreateDomain`** — `engine.name: "sapkit-engine"` · `version: "1.0.0"`으로 `$TMP`에
    `ZSAPKIT_63_DOM`을 실제로 만들었고(`status: "active"`), 같은 픽스처의 1번 단계가 되읽고
    6번 단계가 지웠다. 「돌았다」가 아니라 **이 파일의 이 단계**가 근거다.
  - **대장 세 지점**: 착수 `증거 대기 126 / 증거 있음 60` → 실기 반영 후 **92 / 94**
    → 인하 반영 후 **30 / 156**. ⚠ **앞의 +34만 증거이고 뒤의 +62는 인하다** — 요구를 낮춘
    것이지 증거를 만든 것이 아니다(D-092 ⓐ 집행 · 요구 급 `replay` 96 → 34).
  - **P4 두 종의 처리 — 흔적 0건으로 닫혔다** (2026-08-20 사용자 확인 · **D-100**).
    `CreateTransport` **섰다**(`DEVK901065` · 비어 있고 미해제로 남았던 것을 **사용자가 SE01에서
    삭제**했다). `CreatePackage` **못 섰다**(세 갈래 다 거부) — 그리고 **아무것도 만들지 않았다.**
    ⚠ **판 마감 시점의 기록은 `ZSAPKIT_63_PKG`를 「반쯤 만들어졌다」로 적었고 그것이 오판이었다.**
    읽기 경로 다섯이 갈렸는데(`CreatePackage`의 「이미 있다」·`GetPackageContents`의 빈 목록이
    **존재**를, `GetPackage`·`SearchObject`가 **부재**를 가리켰다) 존재 쪽으로 기울인 것이 틀렸다.
    **부재 쪽이 옳았다.** 그 「이미 있다」는 **거짓 판정**이었고, 없는 객체에 성공 모양으로 답하는
    읽기 도구 둘(`GetPackageContents`·`ReadPackage`)이 백로그에 등재됐다(HANDOFF 18).
  - **안전**: 보호 자산 8종을 **착수 전·중간·정리 후 세 번** 떠서 **세 번 다 일치**. 연습 객체
    잔재 **0**(마스크 5종 — RAP 구현 클래스 이름을 SAP이 `ZBP_`로 정해 `ZBP_SAPKIT*`를 더했다).
    **P2 0건.**
  - **겸행 둘**: **D8은 「관찰되지 않았다」**(「없다」가 아니다) — 승격 없음. ⚠ 그러나 셋 중
    `406`/`415`만 C1으로 관측 가능하고 나머지 둘(리다이렉트·`skipSessionType`)은 **현재 채록
    설비로 원리상 관측되지 않는다**(픽스처는 응답 본문만 담고 자식 stderr에 HTTP 로그가 없다).
    **꼬리 인하 집행 완료** — 62종.
  - **부수 발견 셋**(전부 이 판 밖의 자리라 고치지 않았다): ① `ActivateObjects`가 `BDEF/BDO`를
    **틀린 ADT 주소로 푼다**(`ddic/bdef/sources` ↔ 실제 `bo/behaviordefinitions`) — **구·신 공통
    결함**이라 회귀가 아니다. `GetInactiveObjects` 재조회 오라클이 이것을 잡았다.
    ② **엔진이 실패를 접을 때 요청 URL을 문구에 싣는 경로**가 있어, 그 문구가 마스킹에 걸리면
    **한 단계가 시퀀스 전체의 증거를 날린다**(이 판에서 세 번 겪었다). ③ `CreatePackage`의 발행
    설명이 `software_component`를 「안 주면 SAP이 기본값을 정한다」고 적는데 **실제로는 도구가
    필수로 막는다**.
  - **판9가 받을 재료**: 46종의 갈림 표 · 얼린 관측 `harness/phase6-exercised.json`(78종) ·
    인하 62종의 명부(`harness/build-plan.json`의 `downgradedFrom`) · 실기 픽스처 9편.


#### 판7-a — ⑴ 교체 관문 배선 (기준 2) + 이월 1순위 — **완료 (2026-08-19 · D-094)**

- **판을 자른 기준**: 구 `판7`은 「교체(swap)**만**」이고 전제가 「검증 기준 1~4」다. 기준 2를
  **세우는** 일을 그 판 안에 넣으면 전제를 만드는 일이 전제를 요구하는 판 안에 있게 되어
  **순환한다**. 큐의 `판M2-a`/`판M2-b`와 같은 모양으로 갈랐다(D-094 ⓕ).
- **낸 것 다섯**:
  1. **대상 이름표 배선** — `smoke-mcp.mjs`·`conformance-server-gates.mjs`에
     `--target=bundle|engine`(기본 `bundle`). 대상 하나가 **기동 파일 + NODE_PATH +
     내장 문자열 검사 범위 + 표기**를 한 묶음으로 갖는다. **무인자 출력은 배선 전과
     바이트 동일**(두 게이트 모두 착수 기준선 채록본과 대조).
  2. **B0 다중파일 재설계** — 「보호 테이블이 배포물 자체에 있는가」라는 질문을 그대로
     두고 대상 모양만 갈랐다. `engine`에서 `dist/` **315파일**을 훑어 네 이름 전부 발견.
  3. **음성시험 20 → 24** — 없는 대상 이름 · **상속 키**(`constructor`) · `engine+update` ·
     기동 파일 부재. 넷째는 산출물을 잠깐 치웠다 되돌리는 방식이라 **dist 유무와 무관하게
     결정적**이다.
  4. **CI 상설화** — `sapkit-engine` 잡에 신 엔진 대상 2스텝 **순수 추가**. 기존 잡 무접촉.
  5. **이월 1순위 해소** — `verify-applied.md` ② 되읽기 1순위를 `GetProgram`/`GetInclude`로
     교정(D-093 정직 유보 ⓒ). 절차 22 · 스킬 17 전수 점검 결과 **바이트 대조에 그 도구를
     지정하는 자리는 그 한 곳뿐**이었다.
  6. **(리뷰 회수 중 추가) C1 증거 문구를 관측값으로** — `conformance-server-gates.mjs`의
     C1 증거 줄이 구 번들 문구(`Basic authentication requires SAP_CLIENT…`)를 상수로
     박고 있었다. 판정은 D18 alternation으로 구·신 두 어휘를 다 받으므로 **신 엔진을
     겨누면 낸 적 없는 문구를 관측했다고 적는** 상태가 됐다. **`--target`을 배선한 것이
     그 전제를 죽였다** — D-091 ⓐ가 이 자리를 이연하며 든 논거가 「지금은 정확하다 —
     러너가 구 번들만 검사한다」였기 때문이다. 관측한 어휘를 적도록 고쳤고(구 번들
     출력은 바이트 불변), **그 이연 항목은 이 판에서 닫혔다.**
- **기준 2 실측 — 성립**:

  | | exit | 결과 |
  |---|:--:|---|
  | `smoke-mcp.mjs --target=engine` | **0** | 구 번들 출력과 **바이트 동일** |
  | `conformance-server-gates.mjs --target=engine` | **0** | 22건 · PASS 21 · **등재된 차이 1** · FAIL 0 |
  | 두 게이트 무인자(`bundle`) | **0** | 착수 기준선과 **바이트 동일** |

- **⚠ 유일한 갈림 B2p는 신 엔진의 결함이 아니었다.** 그 단언은 푸는 노브
  (`MCP_BLOCKLIST_PROFILE=off`)와 조이는 노브(`MCP_BLOCKLIST_EXTEND`)를 **한 주입에**
  넣는데, `off`가 EXTEND 검사보다 먼저 단락한다 — **구·신 공통**(구
  `engine/src/lib/policy/tableBlocklist.ts:503` · 신 `sapkit-engine/src/safety/blocklist.ts:272`).
  즉 `want`는 **노브를 받는 어떤 구현으로도 만족할 수 없다**. 관측된 차이의 정체는
  **장부 D6**(분류: 수리)이고, 그래서 기대값을 낮추는 대신 **대상별 등재 칸**을 열었다
  (D-094 ⓓ · 사보타주로 「무조건 통과 아님」 확인).
- **⚠ 그 등재의 한계도 함께 적었다** — 이 단언은 「**조이는** 노브가 프로세스 env로 실제
  먹는가」를 **증명하지 않는다**(`off`가 가린다). 「D6가 확인됐다」로 읽으면 안 된다.
  증명하려면 `off` 없이 `EXTEND`만 주입하는 별도 단언이 필요한데, 그건 **구 번들 대상
  출력을 바꾸므로 별건**이다.
- **부수 해소** — **`GetTableContents`의 MCP 왕복 음성시험**(「판7에 남은 것」 3번)이
  실측으로 닫혔다. 신 엔진 상대로 거부·ask 왕복·감사 기록까지 관측(B1·B1b·B3·B3b·B4·B4b).
- **이월**(이 판이 손대지 않은 것): `conformance-server-gates.mjs`의 **음성시험 묶음 부재**
  (원래 없다 · 범위 밖) · tool-catalog 문서 3종 **자동 대조 게이트 부재**(「판7에 남은 것」
  4번 · 선택) · 판6.1 구조적 이월 셋(D-092 정직 유보 ⓕ) · **꼬리 기준 인하 집행**(D-092 ⓐ) ·
  `GetSession`·`GetSystemInfo` 채록(마스킹 보강 선행) · **M2 봉쇄**(BTP 계정 · 사용자 결정).
- **🔴 최종 독립 리뷰가 새로 짚은 판7-b 이월 둘** (회수 3건은 이 판에서 반영했다):
  1. **교체가 제품 문서 한 줄을 거짓으로 만든다** —
     `interactive/adapters/claude/hooks/README.md`의 층별 한계 표가 「`MCP_BLOCKLIST_PROFILE`·
     `MCP_BLOCKLIST_EXTEND`·`MCP_ALLOW_TABLE` 3종은 활성 프로파일 `sap.env`에 적을 때만
     유효하고 셸 export·`.mcp.json`의 `env`는 조용히 무시된다」고 **실측 근거와 함께**
     단언한다(근거는 구 번들 `engine/src/lib/profile.ts`). **신 엔진은 프로세스 env도
     받는다**(D6 · `resolveSafetyEnv` — 프로파일이 그 키를 선언하지 않으면 프로세스 env가
     이긴다). **오늘까지는 정확한 문서**이므로 이 판의 결함이 아니지만, 교체하는 순간
     틀려진다. ⚠ 무게가 가볍지 않다 — CLAUDE.md 안전 규칙과 **D-043의 소유자 머신 예외가
     「서버측 table blocklist 하한」에 기대고 있고**, 교체는 그 하한을 **누가 설정할 수 있는가**를
     바꾼다. 판7-b가 문서와 함께 그 파급을 판정해야 한다.
  2. **`recorded` 갈래가 대상 범위 밖이다** — `conformance-server-gates.mjs`의
     `checkRecordedGap`은 대상과 무관하게 구 번들 `recorded`를 받는다. 신 엔진이 언젠가
     프로세스 env 통로를 도로 버리면 B2p가 「기록된 격차」로 조용히 초록이 되어 **등재된
     수리가 사라진 것을 게이트가 못 잡는다**. 방향이 조이는 쪽이라 안전 사고는 아니나
     「등재되지 않은 차이는 결함」 규칙과 대칭이 안 맞는다. `recorded`를 `byTarget.bundle`로
     옮기면 닫힌다 — **교체 판에서 기대값을 손볼 때 함께**.
     ⚠ **집행하면 구 번들 무인자 출력이 바뀐다**(격차 줄에 `[대상 bundle]`이 붙고 꼬리가
     면책 3줄 → 미판정 3줄). 그 시점엔 교체 중이라 「바이트 불변」의 목적이 이미 소멸하지만,
     **모르고 하면 무접촉 증명의 기준선이 조용히 움직인다.** 옮기는 판이 그 사실을 적을 것.
     ⚠ 거울 항목 하나 더: `promotions` 갈래는 관측이 `want`와 같아지면 「**`recorded`**를
     `want`로 승격하라」고 안내하는데, **엔진 실측을 근거로 번들 기대값을 올리라는 지시**가
     된다. 오늘은 도달 불가(구·신 모두 `off`가 EXTEND보다 먼저 단락해 `want`가 만족
     불가능)라 실해는 없다 — 같이 손볼 때 함께 볼 것.

### 판7-b — ⑴ 교체(swap) **만** — **완료 (2026-08-19)** (은퇴는 분리 — D-093 ⓐ · 구 `판7`)

**도달점**: 제품 번들 `interactive/server/server.bundle.cjs`가 **자체 저작
`sapkit-engine` 1.0.0**의 산출물이 됐다 — **8,304,513 → 3,813,330 바이트(46%)**.
결정 기록 = **D-095**. **SAP 접속 0회 · P2 0건 · P4 0건**(전 구간 P0).

- **전제**(충족): BLUEPRINT 사다리 ⑴ 「검증 기준」 1~4. 1·3·4는 판6.2·판6.1 실사,
  2는 판7-a가 세웠다. 기준 5는 형태를 바꿔 유지된다 — 현역 자리는 넘겼고 소스
  `engine/`이 **롤백 자산**으로 남는다.
- **없던 것을 만들었다 — 번들러**(`sapkit-engine/tools/bundle.mjs` · esbuild).
  착수 시점 신 엔진의 산출물은 tsc `dist/` 트리뿐이었고, 제품은 설치 중 `npm install`을
  돌리지 않으므로 **파일 하나**여야 했다. external 2종(`node-rfc`·`@napi-rs/keyring`)만
  남기고 나머지 3종을 인라인. 구 external의 `pino`·`pino-pretty`는 **참조 0건**이라 빠졌다.
- **판 스탬프**(`__SAPKIT_ENGINE_VERSION__`) — 없으면 제품 배포 위치에서 **무조건
  `0.0.0`**이다. 번들 스모크는 그래서 번들을 **임시 디렉터리로 복사해** 띄운다
  (사보타주로 실측: `dist/`에 둔 채로는 스탬프를 지워도 통과했다).
- **판7-a 이월 둘을 판정했다**:
  1. **안전 파급 판정 — 그리고 그 판정을 독립 리뷰가 뒤집었다 (D-096).** 신 엔진은
     blocklist 노브를 프로세스 env로도 받는다(옛 GAP-2의 **수리**). 첫 판정은 「존치 +
     세 겹 방어」였으나 그 중 「프로파일이 이긴다」가 **`MCP_ALLOW_TABLE`에는 성립하지
     않았다** — 병합은 프로파일이 **그 키를 실제로 적었을 때만** 이기고 제품 마법사는
     셋 중 하나만 쓴다. **실측으로 보호 테이블이 나갔다**(죽은 루프백 · `BNKA`).
     회수: **「프로세스 env는 조일 수는 있어도 풀 수는 없다」** — `MCP_ALLOW_TABLE`은
     프로파일 파일만 · 층 이름은 더 조일 때만 · `EXTEND`는 합집합. 게이트 4분할
     (B2p·B2p2·B2p3·B2p4) + **사보타주 4회**. 같은 거짓 문장이 있던 `setup.md`·
     **`HANDOFF.md` §8**·`troubleshooting.md`도 함께 고쳤다(첫 판정의 「문서 **한 줄**」이
     틀렸다). `smoke-mcp` 검사 ⑦(배선 노브 0개)은 그대로 유효하다.
  2. **게이트 기대값 구조** — `byTarget.bundle`로 옮기는 대신 **GAP 기구 자체를 은퇴**시켰다
     (남은 격차 0). B2p를 셋으로 갈랐고(B2p·**B2p2**·**B2p3**) **사보타주 2회**로
     각각이 load-bearing임을 확인했다. 22 → **24건 · PASS 24 · FAIL 0**.
- **⑶-b 「서버 결부 copy 39건」 판정 — 3 해소 · 36 잔존**(D-095 ⓔ · 상세는
  [`reference/copy-baseline.md`](reference/copy-baseline.md) §3.2). ⚠ **판8의 목표가 바뀐다** —
  36건이 `interactive/` 아래 남는 한 그 서브트리 고지를 은퇴시키면 사실과 어긋난다.
- **provenance·CI 이전**: `check-engine-provenance`가 `sapkit-engine/`을 겨눈다 ·
  `--rebuild`가 npm을 거치지 않아 **소유자 머신에서 처음으로 재현 판정이 섰다** ·
  `engine-tests` 잡은 **롤백 소스 건강 확인**으로 성격이 바뀌었다 · 「구 번들 발행 표면
  표류 확인」 스텝은 내렸다(구 번들이 현역일 때만 뜻이 있던 질문).
- **검증**: 제품 게이트 9 + 음성시험 9(`test-smoke-mcp` **24 → 30**) + `sapkit-engine`
  전종(+번들 스모크 · jest **4,153 → 4,161**) + `sapkit-cli` 전종 + 신 엔진 dist 대상
  2종 — **전부 exit 0**. 서버 안전 게이트 **22 → 26건**.
  `doctor`만 exit 1이고 **착수 기준선과 동일**(Antigravity 설치본 버전 · 머신 사유).
- **하지 않은 것**: `engine/` 은퇴(판7.5) · **실사용 사이클(SAP 접속 0회)** · 대장 증거
  (증거 대기 **126 그대로**) · tool-catalog 재저작.
- **⚠ 그래서 다음이 판7.5가 아니다.** BLUEPRINT §3.1 ③이 「전환 후 **최소 1회의 실사용
  사이클**」을 은퇴의 전제로 못 박는다. 그 자리는 **판6.3**(attended 실기 46종)이고,
  판 큐의 `다음`을 그쪽으로 옮겼다(D-096 ⓔ).

### 판M2-a — ⑴ M2 오프라인 배선 (destination/UAA 토큰을 기동 경로에)

- **전제**: 없음(오프라인). **단 착수에 새 D-결정이 필요하다** — 아래.
- **왜 판이 갈려 있는가**: 판5가 auth 계층을 **다 지어 놓고 기동에 잇지 않았다.**
  그것은 미완이 아니라 **의도된 정지선**이다(D-091 ⓑ): 토큰 취득은 UAA로 나가는
  실왕복이고 `authorization_code`는 **사람이 브라우저 앞에 있어야** 끝나므로,
  기동 한복판에서 말없이 시작하지 않는다(attended 명시성). 그래서
  `MCP_DESTINATION_TOKEN_PENDING` 진단의 뜻이 「엔진이 못 받는다」가 아니라
  **「아직 안 받았다」**로 좁혀져 있다.
- **그러므로 이 판의 첫 일은 코드가 아니라 결정이다.** 「기동이 토큰 취득을 언제·어떻게
  시작해도 되는가」를 정하고 **새 D-항목으로 남긴 뒤** 배선한다. 정지선을 그대로 둔 채
  배선만 하면 D-091 ⓑ와 충돌한다. 고를 만한 모양: 명시 플래그로만 시작 /
  `client_credentials`만 자동·`authorization_code`는 사람 개시 / 기동은 그대로 두고
  전용 도구로 취득.
- **지어져 있는 것**(판5 산출 — 다시 짓지 말 것): `src/auth/` **6종**
  (`uaa`·`tokenSource`·`jwt`·`callback`·`errors`·`index`) · `ConnectionConfig`
  선택 4필드(`authType`·`jwtToken`·`refreshToken`·`uaa`) · `AdtClient`의 Basic/Bearer
  분기 · `planServiceKeyConnection`.
- **승계된 설계 제약 셋**(뒤집으려면 각각 근거가 필요하다): ① **토큰은 무상태**
  (프로세스 메모리 + `exp` 60초 버퍼 · 세션 파일·키체인 안 씀 — D-080 ①과 정합)
  ② **갱신 실패는 거기서 끝난다**(`UAA_REFRESH_FAILED` · 구 부품의 조용한 전체
  재로그인 폴백을 승계하지 않았다) ③ **엔진이 브라우저를 열지 않는다**(인가 URL을
  호출부에 건네고 끝).
- **함께 볼 것**: D-091 정직 유보가 남긴 「**던짐이 기동 자리에서 읽을 만한 진단으로
  보이는지 아직 확인되지 않았다**」 — 배선이 없어 확인할 자리가 없었다. 이 판이 그
  진단 품질을 함께 본다.
- **완료 기준**: 새 D-결정 기록 · 기동 경로가 토큰을 받는다(오프라인 시험으로) ·
  `MCP_DESTINATION_UNSUPPORTED`/`TOKEN_PENDING` 진단이 새 의미로 정리됨 ·
  기존 Basic 경로 **회귀 0** · 제품 게이트 전종 green.
- **하지 않는 것**: 실접속(판M2-b) · M-표 M2 승격(그 기준 ⓐ가 실접속이다).

### 판M2-b — ⑴ M2 attended 실접속 (BTP 계정이 생긴 뒤)

- **전제**: **BTP/클라우드 계정** — **사용자만 답을 가진 사실**이고 비용·계약이 걸려
  있다(CLAUDE.md 묻는 자리 ③). 체험 계정 개설 여부는 사용자가 정한다(D-092 ⓑ).
  그리고 판M2-a의 배선이 먼저다.
- **막고 있는 것의 범위를 정확히 읽을 것**: 순서 고정 M1→M6(D-081) 때문에 이 보류는
  M2 한 칸이 아니라 **M2 이후 「증거 완료」 전체의 봉쇄**를 뜻한다. **그러나 교체는
  이미 끝났다** — 검증 기준 1~4가 M2 실접속을 요구하지 않았기 때문이다(D-092 ⓑ ·
  D-093 ⓐ). 즉 이 보류는 **사다리 ⑴의 증거 완주를 막지 제품을 막지 않는다.**
- **목표**: destination 프로파일로 **실접속 성공**(inspection-only 강등이 아니라) ·
  `--mcp` 진단 `MCP_DESTINATION_UNSUPPORTED`가 더는 나오지 않는다 · Basic 회귀 0
  → **D15 해소** → M-표 M2 `짓기 완료` 승격.
- **⚠ 옆에 있는 반쪽**: `--env=<name>`은 **BTP가 필요 없고** 판6.2가 절반까지 세웠다 —
  그 통로는 `~/.sapkit/profiles/`가 아니라 `<home>/Documents/mcp-abap-adt/sessions/<name>.env`를
  본다(`src/profile/destination.ts`). 이 머신엔 그 디렉터리가 없어
  `ENV_DESTINATION_NOT_FOUND`로 끝났고, **같은 해석기를 `--env-path`로 태운 실접속은
  성립했다**. 세션 파일을 스테이징하는 것은 **자격증명 복제**라 사용자가 하지 않기로
  정했다 — 우회하지 말 것.

### 판7.5 — ⑴ `engine/` 은퇴 — **완료 (2026-08-22)**

**도달점**: 구 포크 `engine/`이 레포를 떠났다 — `engine/` 아래에서 **1,605개**가 빠졌고
그중 **1,604개가 삭제 · 1개(fixture)는 `interactive/`로 이관**이다(482,714줄 · 소스 570 ·
빌드 산출물 878 · 문서 85 · 그 밖). ⚠ **두 수가 갈리는 이유는 rename 탐지다** — 경로를
`engine/`으로 제한해 세면 이관분도 삭제로 잡혀 1,605가 된다. 루트 `LICENSE`의 상류 고지 표가
**2행 → 1행**이 됐고 남는 1건은 `interactive/LICENSE`다. 결정 기록 = **D-101** ·
은퇴 커밋 **`843a6aba`** + 재핀 **`ac26030b`**.
**SAP 접속 0회 · P2 0건 · P4 0건**(전 구간 P0).

- **검증 34/34 exit 0** — 제품 게이트 9 + `doctor` · 음성시험 9 · `sapkit-engine`
  전종 10 · `sapkit-cli` 전종 4 · **재현 빌드**(`--rebuild`). 재현 빌드는 **커밋 뒤에야
  도달한다**(작업 트리가 clean일 때만 그 갈래로 간다 — D-095 정직 유보 ⓑ).
- **재핀이 따라왔다** — 이 판이 `sapkit-engine/src/rfc/` 주석 2곳을 고쳐 번들 소스
  최종 커밋이 움직였고, provenance 게이트가 그것을 잡았다. **번들 바이트는 불변**
  (`56f12f160567…` · 3,813,330 B)이고 `VERSION`·`integrity.json`의 핀만 옮겼다.

- **브리프가 남긴 숙제 「순서 판정」은 착수 시점에 이미 답이 나 있었다** — BLUEPRINT
  §3.1 ③의 「전환 후 최소 1회 실사용 사이클」을 **판6.3이 세웠고**(D-098) 그 판이 SAP에
  남긴 흔적은 **0건**으로 닫혔다(D-100). 고를 것이 없었다.
- **⚠ 롤백이 한 걸음에서 두 걸음이 됐다** — 판7-b의 「교체 커밋 revert 한 번」은 이제
  **소스 없는 번들**을 되살린다. 현행 롤백: ① `engine/`이 온전한 마지막 커밋
  **`2264f89d`**에서 트리를 되뜬다 ② 그 위에서 교체분을 되돌린다. 「롤백 번들이 여전히
  지어진다」를 매 푸시 확인하던 CI `engine-tests` 잡도 함께 제거됐으므로 **롤백
  가능성은 이제 상시로 기계 확인되지 않는다.** 그것이 은퇴의 대가다.
- **작업량 대부분은 「라이브로 읽던 소비자 넷」이었다** — `build-plan.mjs`(없으면
  `exit 2`) · `ledger/collect.ts`(없으면 **조용히** 위임형 열이 「미상」) ·
  `delegation.test.ts`(구 엔진 실물 스캔) · `conformance-runtime-dir.mjs`(fixture가
  `engine/__tests__/` 아래 · **레포 유일본**). 채록본 둘로 얼렸다:
  **`harness/old-surface/handler-tree.json`**(도구 339 → 폴더 30 + 위임형 판정 전량) ·
  **`harness/replay/retired-evidence-paths.json`**(장부 근거 경로 **40종**). 각각
  만드는 스크립트를 함께 남겼다. **기계 증명**: `build-plan.json` **바이트 무변경** ·
  대장 위임형 **직접 46 · 간접 140 · 없음 0 불변**.
- **장부 무결성 방어는 없애지 않고 좁혔다** — `divergences.ts`의 `engine/` 인용
  **152곳**을 실재 검증하던 시험 3건을, **은퇴 전에 실재를 확인해 얼린 40종만** 면제하게
  바꿨다. 목록 밖의 `engine/` 경로는 여전히 떨어진다 — 「`engine/`이면 통과」로 열면
  장부에 아무 허구나 적을 수 있다.
- **유일본 하나를 옮기지 않기로 정했다** — `engine/abap/zcl_mcp_rfc_http_handler.abap`
  (다른 ABAP 2종은 `interactive/server/sap-assets/`에 해시 동일 사본이 있다). 제품은
  **이미 그것을 동봉하지 않고**(`install-sap-assets.md` Step 5 — 상류에서 구하라고
  안내한다) 상류 저작물을 신 트리에 들이면 방금 지운 고지를 다시 지는 꼴이기 때문이다.

**후속 판이 이어받는 것** — ⓐ **롤백을 실제로 해 본 적이 없다**(D-101 정직 유보 ⓐ).
두 걸음 경로는 논리로만 세웠고, 그것을 상시로 재던 CI 잡을 이 판이 없앴으므로 **이
유보는 자동으로 해소되지 않는다.** ⓑ `sapkit-engine/src/**`의 JSDoc 계보 인용
**425파일**은 이제 전부 죽은 경로를 가리킨다 — 기계 편집이 위험 대비 이득이 적어
**명시 이월**했다. ⓒ `engine/LICENSE`와 `engine/README.md`의 **저작권자 불일치는
규명된 적 없이 소멸**했다.

### 판8 — ⑷ 잔존 차용 36건 판정 — **완료 (2026-08-22)**

**도달점**: 판7-b가 넘긴 셋 중 **③을 본안 · ②를 집행으로 채택하고 ①을 기각했다**
(**D-103**). `interactive/LICENSE`는 **은퇴하지 않고**, 그것이 덮는 범위가
`interactive/` 서브트리 전체에서 **실제 잔존 36파일**로 좁혀졌다. ⑷의 완료 정의는
「고지 0건」에서 **「범위 정합 + 활성 차용분 0」**으로 바뀌었고, **끝그림 ③의 「최종
0건」은 도달 불가로 명시**됐다. **SAP 접속 0회 · P2 0건 · P4 0건**(전 구간 P0).

- **판정의 열쇠는 git 이력이 36을 갈랐다는 것이다** — `sap-assets/` 30은 이식 커밋
  `038085c2` 이후 **변경 커밋 0**(우리가 한 번도 손대지 않았다)이고, `tool-catalog/` 4 ·
  `verify-engine.mjs` · `bundle-keyring.mjs`는 각각 **6 · 3 · 3 커밋**으로 계속 개작해
  왔다. 끝그림 ②의 정의가 이미 「남의 코드를 **레포 안에서 개작·유지보수하지 않는다**」
  이므로, **그 정의로 재면 동결 30은 이미 조건을 만족하고 미달인 것은 활성 6**이다.
  차용의 무게를 **파일 수가 아니라 「우리가 그것을 떠안고 있는가」**로 잰다.
- **①을 기각한 이유는 셋이다** — `sap-assets/` 30이 **D-079 ⑥**(SAP 오브젝트 무접촉)과
  정면 충돌하고, `install-sap-assets.md`가 설치 소스로 직접 링크하는 **필수 자산**이라
  빼면 완전 오프라인 설치가 깨지며, 상류가 MIT이므로 **법적으로도 불필요**하다.
- **②와 ③은 배타가 아니라 짝이었다** — ②만 하면 계획서가 여전히 「언젠가 0」을 약속해
  ⑷가 안 닫히고, ③만 하면 배포되는 고지가 실제보다 넓게 걸린 채 남는다.
- **고지 4곳이 이제 같은 범위를 말한다** — 루트 `LICENSE` 표 Scope 칸 ·
  `interactive/LICENSE`의 **「Scope of this notice」 절 신설**(MIT 전문·상류 저작권
  문구는 **한 글자도 안 고쳤다**) · `THIRD_PARTY_NOTICES.md`(지식 갈래 행을 「재저작
  소진 + 계보 보존」으로 전환 · 36행을 **동결 30 / 활성 6 두 행으로 분할**) ·
  `interactive/README.md` 라이선스 줄(**제품 라이선스와 상류 고지를 갈라 적었다** —
  그전에는 `[MIT](LICENSE)`가 상류 고지 파일을 제품 라이선스처럼 가리켰다).
- **착수에서 명부 §4 레시피를 재현했다**(기계 근거) — 인벤토리 **487** · 미매칭 **0** ·
  copy 원본 **282** · `server/` 결부 **60** = keyring 21(끝그림 ②) + 번들 계열 3(대체
  완료) + **잔존 36**. 명부 수치가 그대로 재현됐다.
- **낡은 계보 서술 둘을 정정했다**(D-103 ⓕ) — `interactive/server/verify-engine.mjs`의
  머리주석이 **미래형으로** 「`engine/`은 판7.5에서 은퇴할 때까지 롤백 소스로 남는다」고
  말하고 있었다(판7.5는 끝났고 그 트리는 레포에 없다) · `HANDOFF.md` 🔙 절 끝의
  「되돌리는 법 = 교체 커밋 revert **한 번**」 문단은 **같은 문서 위쪽이 이미 무효화한
  서술**이 지워지지 않고 남은 것이었다.
- **검증**: 착수 기준선 **34/34 exit 0**(제품 게이트 9 + `doctor` · 음성시험 9 ·
  `sapkit-engine` 전종 10 · `sapkit-cli` 전종 4 · 재현 빌드) — 판7.5 마감과 동일.
  마감도 같은 34종. ⚠ `doctor`는 이 머신에서 **exit 0**이다(판7.5 착수부터).
- **하지 않은 것**: 활성 6 재저작(**판10**) · `bundle-keyring.mjs` 처분 판정(판10) ·
  36건과 상류 원본의 **파일 단위 대조**(동결 레포 무접촉 — D-103 정직 유보 ⓐ) ·
  롤백 두 걸음 시연(여전히 미시연 — D-101 ⓐ).

#### 회수 (2026-08-23 · D-104) — **독립 리뷰 R-FAIL · 집행 철회 · 판정 유지**

리뷰어가 기계 주장 셋을 **전부 재현**하고(레시피 487/0/282/60 · `interactive/LICENSE`
26추가 0삭제 · 게이트 10/10 exit 0) 판정 논리·① 기각 근거·목표 하향의 정직성까지
확인했으나, **집행에서 차단 2를 냈다.** 둘 다 인정했다.

- **차단 2 (본질) — `class: transform`을 통째로 빠뜨렸다.** 고지 범위를 `copy` 36으로
  좁혔는데, 명부 170은 `copy`만 세므로 **페르소나·절차는 애초에 그 안에 없었다.** 실측:
  transform 목적지 유니크 **42건 현존** · 그중 **38파일이 상류 원본과 60자 초과 동일 행
  1,326행 공유**(페르소나 **26 전량**이 이 범주 · 25파일이 **54~95%** · `sap-architect.md`
  58/61). 그래서 새 `interactive/LICENSE`의 「나머지는 전부 소유자 저작(personas,
  procedures 포함)」이 **사실이 아니었다.** → **② 철회**: 루트 `LICENSE`·`interactive/LICENSE`·
  `THIRD_PARTY_NOTICES.md`·`interactive/README.md`를 **서브트리 전체**로 되돌리고, 고지가
  「범위」 대신 **「알려진 내역」**을 적게 했다.
- **차단 1 — 같은 파일 안의 모순.** `THIRD_PARTY_NOTICES.md`의 「`template_base.xlsx`·ABAP
  템플릿·샘플은 sc4sap-custom 저작물」이 새 머리말과 어긋난 채 남았고, **그 문장 자체가
  이미 사실이 아니었다**(xlsx는 판3.6 재제작 `4d7e8ce5` · 견본 5는 1부 체크 · oop 20은
  판6.2 교체). 정정했다.
- **뿌리** — D-087 ①이 정한 것은 **명부의 계수 범위**인데 `copy-baseline` §3.2 표가 그것을
  **「차용분이 아님」**이라 적어 **귀속 판정으로 격상**시켰고, 판8이 그 위에 고지를 세웠다.
  `MIGRATION-MANIFEST.md`의 정의는 `transform` = **「형태 변환 이식」**이다.
- **권고 3건 전부 채택** — ⓐ 「6·3·3」은 이식 커밋 **포함** 총계임을 명시(「이식 이후」면
  5·2·2 · 한 열에 두 셈법이 섞여 있었다) · ⓑ 루트 `LICENSE`의 `tool-catalog/ (4)`를 글롭
  `sc4sap-mcp-tools*.md`로 못박음(폴더 실파일은 5) · ⓒ **D-067 ③의 「무접촉」 조항을
  supersede**한다고 명시.
- **여기서 남는 규칙**: 고지를 **넓게 두는 것**은 부정확할 뿐이지만 **좁히면서 「나머지는
  우리 것」이라 적는 것**은 방향이 다르다 — 판정이 설 때까지 넓은 쪽에 둔다.
- **신설**: **판3.7**(`transform` 42 재저작) · 명부 `copy-baseline.md` **§3.4**.
  ⚠ 이것은 **새로 생긴 일이 아니라 아무도 세지 않던 일**이다.

#### 2차 회수 (2026-08-23 · D-106) — **또 R-FAIL · §3.4가 42에서 미달이었다**

위 회수와 판9를 커밋한 뒤 다시 독립 리뷰를 돌렸고, 리뷰어가 R1·R2·R3·P2·P3·P4를 **전부
재현**하면서도 **차단 2**를 냈다. 둘 다 인정했다.

- **차단 1 — §3.4 명부 42는 미달.** **절차 12파일이 통째로 빠졌고** 그중 넷은 상류 문장을
  **54~65%** 이고 있었다(`program-to-spec.md` **90/138**). 원인은 §4 레시피의 **목적지
  도출**이다 — 규칙 `skills/**` → `core/procedures/`가 `<스킬>/SKILL.md`를 만드는데
  이식은 그 폴더를 **`<스킬>.md` 한 파일로 납작하게** 했다. 둘째 갈래로 **`targets` 없는
  transform 규칙 10종**, 셋째로 **`conventions/`의 「자체분」 3편 중 둘**(100% · 79%)이
  함께 드러났다.
- **⚠ 그래서 방법을 바꿨다** — §3.4는 이제 **장부에서 목적지를 도출하지 않는다.**
  **레포 파일에서 출발하는 역방향 스캔**으로 만들고, 원본을 **디렉터리 2단계까지 묶어**
  비교한다(납작해진 이식은 파일 단위로는 과소 계상된다 — `program-to-spec.md`가 파일
  단위 26%, 폴더 단위 **65%**). 결과 **42 → 56**.
- **차단 2 — 배포 고지가 D-104 자신의 결론과 어긋났다.** 「이식 **이후** 6·3·3」(D-104는
  「이식 **포함** 총계 6·3·3 · 이후면 5·2·2」로 정했다) · 「`core/procedures/` 5」(실측 4 ·
  D-104 안에서도 ⓐ와 ⓔ가 갈렸다). 전파분까지 정정했다.
- **권고 4건 채택** — §3.4 유보를 **「수치」에서 「파일 집합」으로** 확장 · 역방향 스캔을
  **절차로 못박음**(게이트로 만들지 않는다 — 명부 §1과 충돌) · D-105의 「엔진 135파일」을
  **참조 수**로 정정 · 판9 실측에 **inspection-only 155/65 병기**.
- **새 이월(§3.5)** — 역방향 스캔은 **`copy` 체크 완료분 128건에서도 겹침**을 본다.
  `tcodes.md`류 100%는 **D-088 ②로 정상**이지만 `spro.md` 85~92% · `enhancements.md`
  85% · `conventions/` 12편은 **재저작 커밋으로 체크**된 것들이다. **단정하지 않고**
  판3.7이 표본을 떠서 판정한다.
- **여기서 남는 교훈**: 유보를 넓게 쓰는 것만으로는 부족했다 — **세 번 연속 같은 자리에서
  걸렸고**(D-102 ⓐ → D-104 ⓒ → D-106 ⓓ) 답은 **방법 자체를 바꾸는 것**이었다.

### 판9 — ⑴ 표면 재평가 (꼬리 49종) — **완료 (2026-08-23) · 전량 유지**

**도달점**: D-079 ⑧이 2026-08-10에 예약한 **「재평가 1회」가 소진됐다**(**D-105**).
답은 **전량 유지 — 제품 표면에서 빼지 않는다**이고, 도구 **186종은 이름·인자·응답
형태까지 불변**이다. **SAP 접속 0회 · 전 구간 P0.**

- **유지 근거 다섯**: ① **묻는 무게가 달라졌다** — 그 「1회」는 교체 직전(아직 아무에게도
  안 나간 표면)을 상정했고, 지금은 **이미 배포된 제품에서 도구를 없애는 일**이다.
  ② **검증 기준 1**(표면 186 동일 이름·호출 계약)과 정면 충돌 — 교체를 연 관문을 사후에
  스스로 무너뜨린다. ③ **표본이 1인·1머신·1개월**(D-079 유보 ⓔ) — 「호출 0」은 「필요
  없다」가 아니고, `Delete*`는 **드문 것이 정상**인 부류다. ④ **저울이 뒤집혔다** —
  D-083 때는 「짓는 공수」가 한쪽이었으나 지금 0이고, 반대편에는 **제품 7파일**(안전 훅
  포함)과 **엔진 136파일**이 그 이름을 **참조**하며 **새 D-결정 + breaking change**가 따라
  붙는다. ⚠ **136은 「참조 수」이지 편집 대상 수가 아니다**(D-106 ⓔ③ 정정). ⑤ 아래 실측.
- **⚠ 실측이 판의 방향을 바꿨다** — 유일한 찬성 논거였던 「세션이 무겁다」의 과녁이
  꼬리가 아니었다:

  | 프리셋 | 도구 | `tools/list` | ≈토큰 | 그중 꼬리 |
  |---|---:|---:|---:|---|
  | `readonly` (**기본값**) | **74** | 54,763 B | ~13.7k | **11종** · 18.9% |
  | `readonly,high` | **186** | 159,627 B | ~39.9k | 49종 · 25.1% |

  ⚠ **위 표는 프로파일이 활성인 이 머신의 connected 표면**이다. 프로파일 없는 환경
  (inspection-only)의 독립 리뷰 실측은 **`readonly` 65종 48,925 B(꼬리 10종 18.7%) ·
  `readonly,high` 155종 130,741 B(꼬리 43종 26.8% · `Delete*` 20종)**이고, **핵심 주장은
  두 표면에서 모두 성립한다**(D-106 ⓔ④).

  **`Delete*` 25종은 기본 프리셋에 한 종도 없다.** 일상 세션에서 꼬리를 다 빼도 절감은
  **~2.6k 토큰**이고, 그 대가가 위 ④다. 겨눌 곳은 **기본 표면 74종의 설명문 밀도**
  (평균 **740 B/도구**)이며 → **`HANDOFF.md` §5-16** 백로그로 분리 등재했다.
  **둘을 섞으면 잘못된 해법에 도달한다** — 꼬리를 다 빼도 137종이 남아 무게는 그대로다.
- **정직한 반대 근거도 적었다**: 지식·절차·스킬 `.md`에서 꼬리 도구를 부르는 곳은
  **0건**이다 — 빼도 절차는 깨지지 않는다. 그럼에도 ④의 비용이 그 이득을 넘는다.
- **하지 않은 것**: 자산참조만 22종 판정(**이 판의 범위가 아니다** — 한쪽 증거가 있다) ·
  꼬리 7종의 「증거 대기」 해소(그중 둘은 **원리상 못 서는 것**으로 이미 판정 · D-098 ⓓ) ·
  설명문 축소 실행(백로그 — **줄이는 것 자체가 품질 위험**이라 균형을 먼저 물어야 한다).
- **⚠ 재현 함정 둘**(D-105 ⓔ): `--exposition=development`는 **무효한 값**이다
  (`launch.cjs`가 `readonly,high`로 번역해 넘긴다 — 그대로 주면 도구 **0종**) ·
  프로파일이 없으면 inspection-only **155/65**라 꼬리 몫이 달라진다.

### 판10 — ⑷ ㉯ 활성 차용 6 소진 (오프라인)

- **전제**: **판8 뒤.** 그 판이 과녁을 확정했다 — 잔존 36 중 **은퇴 대상은 활성 6뿐**이고
  동결 30은 영구 존치다(D-103 ⓓ·ⓔ). 코드 작업이므로 실 SAP은 필요 없다.
- **왜 이것이 남았나**: 판7-b가 ⑴ 교체를 하면서 이 6을 대체하지 않았다(D-095 ⓔ). 그
  이유가 각각 다르다 — `tool-catalog/` 4는 **교체가 주인만 바꿨고**(서술 대상이
  `sapkit-engine` 표면이 됐다), 유틸 2는 **번들과 무관한 도구**라 교체의 사정권 밖이었다.
- **목표 셋**:
  ① **`tool-catalog/` 4를 엔진 등록점에서 생성한다.** 자체 저작 대체안이 곧 생성이고,
     그러면 **판6.1부터 이월돼 온 「tool-catalog 자동 대조 게이트 부재」가 함께 닫힌다**
     — 하나로 둘을 갚는 자리다. ⚠ 생성물이 되면 **손으로 고치는 것이 금지**되므로
     `TOOL-LEDGER.md`와 같은 지위가 된다(게이트가 거부하게 만들 것).
  ② **`verify-engine.mjs`(81줄) 골격 재저작.** 판7-b가 문서·라벨·대상을 전면 갱신했으나
     골격은 차용분 그대로다. ⚠ **이 파일은 제품 게이트 본체**다 — 재저작이 곧 게이트
     계약의 변경이므로 무엇을·왜 바꿨는지가 커밋에 남아야 한다(CLAUDE.md 게이트 절).
  ③ **`bundle-keyring.mjs`(277줄) 처분 판정.** 판8이 정하지 않고 열어 뒀다(D-103 정직
     유보 ⓓ). 고를 만한 모양: 재저작 / 존치 확정(동결로 재분류) / keyring 번들 자체를
     끝그림 ②의 정식 의존성 경로로 옮기며 함께 정리.
- **완료 기준**: 활성 6이 자체 저작으로 치환됐음을 git 이력으로 확인 · `tool-catalog`
  대조 게이트가 서 있을 것 · 고지 4곳의 범위 서술이 **줄어든 잔존물로 함께 갱신**될 것
  (판8이 세운 정합을 깨지 않는 것까지가 이 판의 일이다) · 제품 게이트 전종 green.
- **⚠ 이 판이 끝나도 `interactive/LICENSE`는 남는다** — 동결 30 때문이다. 그때 그 고지는
  **부채의 표시가 아니라 SAP 측 자산의 정직한 귀속**이고, 그것이 ⑷의 종착지다(D-103 ⓓ).
- **하지 않는 것**: `sap-assets/` 30 손대기(무접촉 — D-079 ⑥) · SAP 접속.

### 판3.7 — ⑶-b 상류 잔존 56 재저작 (오프라인)

- **전제**: 없음(오프라인). **2026-08-23 신설**(D-104 ⓔ) · **범위가 같은 날 42 → 56으로
  넓어졌다**(D-106 ⓐ).
- **왜 이제 나왔나**: 이식 분류는 `copy`(무변환)·`transform`(**형태 변환**)·`obsolete`·
  `archive` 넷인데 명부 170은 **`copy`만 센다**(D-087 ①이 계수 범위를 그렇게 정했다 —
  정당하다). 문제는 `copy-baseline` §3.2가 그것을 **「차용분이 아님」**으로 옮겨 적어
  transform을 **소관 없는 범주**로 만든 것이고, 더 나아가 **§4 레시피의 목적지 도출이
  절차 12파일을 아예 만들어 내지 못했다**(`skills/<스킬>/**` → `<스킬>.md` 납작화).
- **대상 56건 · 명부 = [`reference/copy-baseline.md`](reference/copy-baseline.md) §3.4.**
  구성은 **페르소나 25 · 절차 16 · 어댑터 5 · 지식 5 · 정책 2 · 도구 3**.
  잔존이 짙은 순: `sap-architect.md` 58/61(95%) · `sap-critic.md` 69/76(91%) ·
  `program-to-spec.md` 90/138(65%) · `compare-programs.md` 88/141(62%) ·
  `analyze-symptom.md` 69/113(61%) · `extract-customizations.mjs` 111/217(51%).
- **규칙은 판3.1~3.6과 같다**(D-087 ⑤ · 명부 §2): **재저작만** 체크 · 부분 편집은 체크
  아님 · **독립 리뷰 통과 후** 체크 · 커밋 해시와 리뷰 근거 병기 · 리뷰는 **양방향**
  (원문 사실 **누락 0** + 원문에 없던 단언 **추가 0**).
- **⚠ 페르소나·절차는 지식 파일과 다르다** — 그것은 **행동 지시**라 문장을 다시 쓰면서
  지시의 강도·순서가 미묘하게 바뀌면 제품 동작이 바뀐다. 배치를 작게 끊고 회귀를 함께 볼 것.
- **⚠ 이 판은 §3.5 이월분도 맡는다** — `copy` 체크 완료분 128건에서 겹침이 남은 것들
  (`spro.md` 85~92% · `enhancements.md` 85% · `conventions/` 12편). **표본부터 뜬다**:
  첫 표본은 `core/knowledge/modules/SD/enhancements.md`(재저작 커밋 `f57e1d8b` · 64/75 =
  85%). 결과가 「체크 기준을 다시 봐야 한다」면 **새 D-항목**을 낸다.
- **⚠ 착수 시 역방향 스캔을 1회 돌린다**(D-106 ⓒ) — §3.4의 도출 절차가 그것이고, 명부가
  마지막으로 갱신된 뒤 레포가 움직였을 수 있다.
- **완료 기준**: §3.4 명부 **56/56 체크** + §3.5 표본 판정 · 그 뒤 **⑷ ㉮(고지 범위 정합)를
  다시 시도할 수 있다** — 그때 비로소 고지가 덮는 것을 좁혀 적을 수 있다.
- **하지 않는 것**: SAP 접속 · `copy` 명부 170 총계 수정(그것은 사실이다) ·
  동결 원본 수정(읽기 전용 · `private/` 금독).
  동결 원본 수정(읽기 전용 · `private/` 금독).
