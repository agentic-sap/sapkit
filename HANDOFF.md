# HANDOFF — 프로젝트 전체 상태와 재개 지침

> **목적: 컨텍스트/세션이 클리어돼도 이 문서 하나로 전부 복원.**
> 작성 2026-07-10 · 최종 갱신 2026-08-25. 새 세션은 ① 이 문서 → ② 필요 시 해당 트랙
> DESIGN.md 순으로 읽는다. 상태가 바뀌면 이 문서를 갱신하는 것까지가 작업의 일부다.
> **이 문서는 이제 재개점만 든다** — 불변 규칙은 `CLAUDE.md` 「안전 규칙」 절, 과거
> 기록은 `docs/reference/handoff-archive/`가 정본이다(맨 아래 두 절이 안내한다).
>
> ═══════════════════════════════════════════════════════════════════
> **▶▶ 재개점 — 여기부터 읽는다 (최상단 정본)**
> ═══════════════════════════════════════════════════════════════════
>
> ## ▶ **여기부터 읽는다** — 2026-08-25 마감(판C) · 다음 세션은 이 절만 읽으면 복원된다
>
> ## ✅ 방금 닫힌 것 — 판C: 제품 단독 사용의 세션 간 연속성 (2026-08-25 · D-139 · 정정 D-140)
>
> **판B가 비워 둔 과녁을 열었다.** 판B는 공방(이 레포)의 망각만 고쳤고, 소유자의 원래
> 문제의식이던 **플러그인이 깔린 사용자 프로젝트** 쪽은 손대지 않은 채 남아 있었다.
> 판C가 그 자리를 채웠다 — D-138 갈래 ⓐ(sapkit 자체 보유)의 집행이고 유보 ①·④를 채웠다.
>
> **선 것 넷.** ⑴ 사용자 프로젝트 루트에 세울 **재개점 템플릿 2종**
> (`interactive/assets/continuity/` — 상태 스냅샷 + 작업 큐) ⑵ **마감 스킬 `handoff`**
> (절차 + 래퍼) ⑶ **시작 안내 2층** — 기본은 setup이 사용자 `CLAUDE.md`에 승인 하에 더하는
> 한 줄, 선택은 `SessionStart` 훅(**기본 미설치** · **안전훅 6종과 분리된 자기 설치기** ·
> `adapters/claude/hooks/continuity/`) ⑷ `check-doc-size.mjs` **대상 인자화**(무인자 exit 2 ·
> 레포 판정 동등). 어댑터 3사 패리티 · 메타데이터 **0.9.0**.
>
> **중심은 fail-closed 소유 판별이다.** 남의 프로젝트에는 같은 이름의 파일이 이미 있을 수
> 있다. 표식 한 줄(`<!-- sapkit:continuity -->` · 앞 20줄 스캔)로 가리고, **표식 없는 동명
> 파일이 하나라도 있으면 세우지도 갱신하지도 않는다 — 크기조차 재지 않는다.** 읽기 실패도
> 「표식 없음」이다. 모든 갱신은 표식을 보존하며, 사용자가 표식을 지우면 sapkit이 손을 뗀다
> (탈출구다).
>
> **실측.** 게이트 **제품 10종**(= `CLAUDE.md` 「게이트」 절 11항에서 로컬 전용 `doctor`를
> 뺀 수다) + 음성시험 11종 + 공방 4종 **전부 exit 0** · `doctor` 자신은 exit 1이지만 그 FAIL은
> **`main`에서도 똑같이 나는 선재 로컬 설치 상태**다(Antigravity 설치 버전 ≠ 고정값 — 대조
> 실행으로 확인) ·
> **독립 리뷰 R-PASS · 차단 0 · 권고 7**(4건 회수 · 3건 사유와 함께 유지) ·
> **복원 시험 도달** — 레포 밖 스크래치 사용자 프로젝트에서 마감 스킬로 재개점을 세우고
> **그 파일들만** 받은 새 컨텍스트가 한 일·객체별 상태·다음 한 걸음을 정확히 복원했다.
> 표식 없는 프로젝트에서는 **쓰기 0건**, 갱신 왕복에서 표식 생존. 그 실행이 짚은 실사용
> 구멍 7건은 같은 판에서 회수했다.
>
> ⚠ **남은 유보 하나** — **0.9.0을 실제 설치 캐시에 올리지는 못했다.** 마켓플레이스 클론이
> GitHub `main`을 따라가는데 이 브랜치가 미푸시이고, 로컬 경로를 두 번째 마켓으로 붙이면
> 이름이 충돌해 현행 설치를 가린다. 대신 ⓐ 설치 캐시가 `interactive/` **전수 복사**임을
> 집합 대조로, ⓑ 레포 밖에 놓은 플러그인 루트 복제본에서 신규 자산 7파일 실재 · 무인자
> exit 2 · 스위치 왕복 byte 원복 · 훅 결정표를 실측했다. **푸시·병합 시점에 닫힌다.**
>
> ## 🔑 한 문단 요약
>
> **끝그림 ①~④가 전부 닫혔다.** 마지막까지 상류 코드로 남아 있던 SAP 측 ABAP 자산
> **7,087행**을 자작으로 다시 쓰고 이름까지 `SAPKIT`으로 옮겼으며(D-130·D-131),
> 그중 **9종을 실제 S/4HANA에 설치해 활성화까지 확인**했다(D-132). 제품 번들은
> **1.3.0**이고 전 게이트·CI가 green이다. **파일 단위로 상류에서 온 것은 이제 없다.**
>
> **그리고 2026-08-24에 판B가 「두뇌」를 고쳤다(D-135).** 세션 간 망각의 실측 원인
> 둘을 처방했다 — ① **웜 레이어 다이어트**: 이 문서를 **4,808줄**에서, `docs/RUN-PLAN.md`를
> **1,964줄**에서 재개점·큐만 남기고 잘랐다(삭제가 아니라 § 번호 보존 **이사** ·
> **줄어든 뒤의 현재 값은 아래 📌표가 가리키는 게이트가 센다**) ·
> 불변 규칙은 `CLAUDE.md` 「안전 규칙」 절 **한 벌로 단권화** · 상한을 **기계가**
> 지킨다(`check-doc-size.mjs` 신설). ② **행동 훅층**: `.claude/hooks/`가 처음으로
> **git 추적**에 들어와 다른 머신에 배포되고, 백틱 가드가 증설됐으며, 세션 시작 훅이
> 없어진 설비를 훑는 대신 **재개점 안내·원격 대조 지시**를 준다.
>
> **그리고 2026-08-25에 판W가 「콜드 층」에 지도를 놓았다(D-137).** 결정 로그·보관소·
> 감사 기록처럼 **원본이 불변인 기록**은 그동안 D-번호를 미리 아는 사람만 회수할 수
> 있었다. 판W가 그 위에 **주제별 합성 페이지 7편 + 콜드 전수 색인**을 얹고, 그 구조가
> 조용히 낡지 않도록 게이트를 걸었다.
> **▶ 주제로 과거를 찾을 때의 입구는 `docs/reference/wiki/INDEX.md`다** — 「왜 엔진을
> 갈아탔나」·「왜 vsp가 은퇴했나」·「왜 제품 훅이 기본 꺼져 있나」 같은 질문은 이 문서가
> 아니라 거기서 풀린다. 위키는 **정본이 아니라 재생성 가능한 파생물**이고, 어긋나면
> `docs/reference/DECISIONS.md`(append-only)가 이긴다.
>
> ## 🖥 다른 머신에서 재개할 때 — 이것만 하면 된다
>
> ```bash
> git fetch origin && git log --oneline main..origin/main   # 비어야 정상
> git pull                                                   # 최신 = 아래 커밋
> ```
> ⚠ **다른 머신에서 처음 pull 하기 전에, 그 머신의 비추적 `.claude/settings.json`을 옆으로
> 치우고 받는다** — 판B가 `.claude/settings.json`과 `.claude/hooks/`를 git 추적으로
> 편입해서, 로컬 사본이 남아 있으면 pull이
> `untracked working tree files would be overwritten`으로 막힌다.
>
> 오프라인 작업은 그대로 이어진다. **SAP이 필요한 작업만 아래 ⑵의 준비가 있어야 한다.**
>
> **⑴ 게이트 한 번 돌려 건강 확인** (PowerShell — Bash로 돌리면 자식 프로세스 수거에서 막힌다):
> ```
> node interactive/server/verify-engine.mjs
> node interactive/scripts/check-engine-provenance.mjs
> node interactive/scripts/smoke-mcp.mjs
> node interactive/scripts/conformance-server-gates.mjs
> node interactive/scripts/check-links.mjs interactive
> node interactive/scripts/check-runtime-path-rename.mjs
> node interactive/scripts/verify-checker.mjs
> ```
>
> **⑵ SAP에 붙어야 하면** — 이 머신에서 KR-DEV는 **VPN이 있어야** 닿는다
> (`192.168.1.225:8000` · 사내망). 프로파일은 `~/.sapkit/profiles/`에 있고 전부
> `SAP_TIER=DEV`다.
> ⚠ **이 문단은 아래 「✅ SAP 접속」 절이 갱신한다** — 프로파일은 **2종**이고(IDES-DEV는
> 이 머신에 없다) KR-DEV는 2026-08-24에 살아났다. 한때 여기 있던 「IDES-DEV 401 ·
> 비밀번호를 갱신하면 된다」 안내는 **없는 프로파일을 가리키던 낡은 서술**이라 지웠다.
>
> ⚠ **MCP 세션이 프로파일 전환을 못 따라올 수 있다.** 2026-08-23에 실제로 그랬다 —
> `.sapkit/active-profile.txt`를 바꾸고 `ReloadProfile`을 불러도 `alias: null`로
> 옛 시스템을 계속 봤다. **우회로가 있고 그것으로 그날 전 작업을 했다**: 제품 번들을
> 직접 띄우는 짧은 stdio 클라이언트(`MCP_ENV_PATH`로 프로파일 지정 ·
> `NODE_PATH`로 keyring). 기동 방식의 정본은
> `sapkit-engine/harness/recorder/childProcessTransport.ts`다. Claude Code를
> 재시작하거나 `/reload-plugins` 해도 풀린다.
>
> ## 📌 지금 상태
>
> | 축 | 값 |
> |---|---|
> | **제품 번들** | `sapkit-engine` **1.3.0** · **3,859,279 B** · sha256 `a3176d821d26…` · 소스 핀 **`f85049fd`** |
> | **도구 표면** | **186 / 155** 불변 |
> | **시험** | jest **4,261** |
> | **게이트** | 제품 **10종**(판B에서 `check-doc-size` 신설) · 엔진 전종 · 음성시험 **11종 + 공방 훅 2종** 전부 exit 0(판C가 `test-continuity-hook` 신설) · 판W가 **공방 문서 게이트 `scripts/check-wiki.mjs`와 그 음성시험**을 더했다(제품에 동봉되지 않는 층 — **목록 정본은 `CLAUDE.md` 「게이트」 절**) |
> | **웜 레이어** | 상한 `HANDOFF.md` **500** · `docs/RUN-PLAN.md` **300** — **현재 값은 `check-doc-size.mjs`가 센다**(여기 복제하지 않는다) |
> | **워킹 트리** | 판C 마감 시점 clean · 브랜치 `dryforge/plate-c-user-continuity`(main 병합 전 · **미푸시**) |
> | **CI** | 마지막 **실측 초록**은 `0019b493`(run `32707789382` · 잡 4개 전부 — 판B와 그 후속). ⚠ **판W·판C 둘 다 아직 러너에서 돌지 않았다** — 브랜치가 미푸시이고 워크플로 트리거가 **main 푸시·PR**이라 feature 브랜치 단독으로는 안 돈다. 러너를 못 탄 것은 **스텝 3종**이다 — 판W의 `check-wiki` + 그 음성시험, 판C의 `test-continuity-hook`. 여기에 **인자가 바뀐 `check-doc-size` 스텝**도 러너에서는 처음 도는 형태다. 전부 **로컬 실측만** 있다 |
> | **대장** | 증거 대기 **30** · 증거 있음 **156** (변하지 않았다) |
> | **SAP** | `$TMP`에 `ZSAPKIT_*` **10종 설치·활성**(FUGR 1 + FM 2 + INTF 1 + CLAS 6). ⚠ 위 요약과 ✅표의 **「9종」은 D-132 시점 값**이고 이 칸이 현재 실측이다 |
>
> ## ✅ 오늘 닫힌 것 (D-121 ~ D-139)
>
> | 갈래 | 결과 |
> |---|---|
> | 완주 4판(판R·E1·E2·Z) | 리뷰 회수 · 증거 판정 · 브랜드 단일 |
> | 브랜드 꼬리 3회 | 사용자가 README 화면에서 찾은 것 + **레포 전수조사**(텍스트 1,279파일) |
> | **ABAP 자산 재저작**(판S1~S5) | **7,087행** 자작 전환 + 개명 + 배선 + 재번들 |
> | **SAP 실기 검증** | **9종 활성화** · 되읽기 대조 · 결함 3건 수리 |
> | **판B — 프로젝트 두뇌**(D-135) | 훅층 git 편입 + 수리 2종 · 문서 다이어트(4,808줄·1,964줄에서 절단 — § 번호 보존 이사) · 불변 규칙 단권화 · 줄 수 게이트 신설 · 설계서 편입 |
> | **판C — 제품 사용자의 연속성**(D-139) | 재개점 템플릿 2종 + 마감 스킬 `handoff` + 시작 안내 2층 + 크기 게이트 인자화 · **표식 기반 fail-closed 소유 판별** · 독립 리뷰 **R-PASS**(차단 0 · 권고 7) · 복원 시험 **도달** |
> | **판W — 콜드 층 위키화**(D-137) | 합성 페이지 7편 + 콜드 전수 색인 `docs/reference/wiki/INDEX.md` · 위키 게이트·음성시험 신설 · CI·규약 배선 · 독립 리뷰 **R-PASS**(차단 0 · 권고 10) · 복원 시험 **3/3** |
>
> ## ⚠ 남은 것 — 부채 목록이지 계획이 아니다
>
> **⑴ 런타임 왕복 — 2026-08-24에 닫혔다 (D-134).** OData 서비스 **`ZSAPKIT_ADT_SRV`**를
> 세우고(SEGW 프로젝트는 사람이 SAPGUI에서 · MPC_EXT/DPC_EXT 주입과 `flush_cache`
> 설치는 MCP로) 실왕복을 실증했다 — `GetScreen(RSPARAM,1000)` 화면 4부 ·
> `GetTextElement(RSPARAM)` 텍스트 2건 · `GetGuiStatusList(SAPMV45A)` GUI 상태 64종.
> 프로파일 `SAP_RFC_ODATA_SERVICE_URL`도 신 서비스로 교체했다(백업
> `sap.env.bak-20260824`). ⚠ **읽기 경로만 실증됐다** — `CUA_WRITE`·`DYNPRO_INSERT`·
> `*_DELETE`는 P3라 연습 대상을 새로 만들어야 하고 하지 않았다.
>
> **⑵ 29파일 — 이 시스템에서는 닫혔다 (D-134).** 남아 있던 3종(MPC_EXT·DPC_EXT·
> `ZSAPKIT_ADT_FLUSH_CACHE`)을 설치·활성했다. ⚠ **ECC 전용 10종은 여전히 미검증이고
> 앞으로도 그렇다** — KR-DEV는 S/4라 절차가 Step 2를 영구 스킵한다. 같은 부류의
> 결함이 남아 있어도 이 시스템에서는 알 수 없다.
>
> **⑶ 신·구 오브젝트가 SAP에 함께 있다 — 이제 지울 수 있다.** ⑴이 닫히면서
> 전제가 풀렸다. 실측한 구 오브젝트는 **25종**이고 갈래마다 처방이 다르다:
> **ALV OOP 핸들러 7종**(`Z*_S4SAP_*`)은 신 7종이 이미 활성이라 **즉시 삭제 가능**
> (⚠ SE24 Where-Used로 레포 밖 프로그램의 상속을 먼저 확인) · **OData 계열 9종**
> (`ZMCP_ADT` IWPR + MPC/DPC 4 + FUGR + FM 2 + flush_cache)은 **이제 아무도 안 부르므로
> 삭제 가능** · **ECC DDIC 8종**은 애초에 이 시스템에 없을 공산이 크다(SE37 확인) ·
> **`ZCL_MCP_RFC_HTTP_HANDLER` / SICF `zmcp_rfc`는 그대로 둔다**(대응하는 새 이름이
> 없고 제품이 아직 그 이름을 부른다). ⚠ **`Z*SAPKIT_M1_*` 프로브 잔재 8종**도
> 판6.x 시험 찌꺼기로 남아 있다.
>
> **⑷ 상류 고지는 남아 있고, 이유가 바뀌었다.** `interactive/LICENSE`는 이제
> **`sap-assets/` 때문이 아니라** 표·코드값·출력 템플릿 같은 **산재 계약행 ~2,200행**
> 때문에 존속한다(바꾸면 동작·데이터가 틀어져 **의도적으로 보존**한 것 — D-112 ⓓ).
> 파일 단위 차용은 0이다.
>
> **⑸ 증거 대기 30종은 여전히 실 SAP 검증이 없다**(D-122). 완주는 D-120 ⓓ의 정의
> (**「✅ 또는 이유와 함께 못 섰다」**)로 닫혔고 그 30종은 후자다. ⚠ **판E1이 진단한
> 오프라인 결함 2건은 2026-08-24에 수리됐다** — 재생 수집이 재귀로 넘어가 18종에
> 도달하고(`harness/replay/batch.ts` 신설 · 수집 19 → 유지 16 · 거부 3),
> `replay-attended.mjs`가 keyring `NODE_PATH`를 물게 됐다. ⚠ **그래도 재생이 실제로
> 통과하는지는 모른다** — 수리는 「도달할 수 있게 됐다」까지고, SAP 접속이 있어야 치른다.
>
> **⑹ 그 밖 — 2026-08-24에 다섯이 닫혔다.** 훅 출력 표기(21지점 중 3 불일치 →
> 소문자 `sapkit` 기계 토큰으로 통일) · `atomicWriteFileSync` 0600(코드가 아니라
> **거짓 보장을** 고쳤다 — Windows에서 mode=666 실측) · 중립 스크립트의 어댑터
> import(1건 → `scripts/lib/`로 복원) · `track-a-rebase-v2`의 죽은 `file:line`
> (참조 54 중 죽은 14종 — 삭제 아닌 인라인 표시) · D-118 채록본 부재(**정말 없음을
> 확인하고 D-133으로 append**). ~~**남은 하나 = 체크박스 규범항 2파일 34줄** —
> 「의도적 보존」으로 닫지 말 것(재저작 가능한 산문이다 · D-112가 한 번 오분류했다 → D-113 정정).~~
> → **✅ 2026-08-24에 그 하나도 닫혔다 — 여섯이다.** `review-checklist.md` **24행**
> (§7~§12 · 그 구간 체크박스 44개 중 24) + `troubleshooting.md` **11행**(명부의 10 +
> 60자 컷 아래라 안 잡히던 1) = **35행 전부 재저작 · 포기 0**. 규범 강도·기술 식별자·
> 열거 개수·체크박스 형식을 보존했고, 역방향 스캔에서 `review-checklist.md`는
> **겹침 0**, `troubleshooting.md`는 **5**(curl 펜스 4 + 환경변수 열거 1 — 체크박스 아님)다.
> ⚠ **34가 아니라 35였고, 그중 스캔이 실제로 세고 있던 것은 23뿐이었다** — 나머지는
> D-130 개명이 이미 바이트 동일을 깨 놓았거나(9) 60자 컷 아래였다(1)
> · 세부는 `docs/reference/copy-baseline.md` §3.4.
>
> **⑺ 새로 드러난 것 (2026-08-24)**
>
> - **브랜드가 완전히 단일이 아니다** — zrfc 백엔드의 `ZCL_MCP_RFC_HTTP_HANDLER`와
>   SICF `/sap/bc/rest/zmcp_rfc`는 개명된 적이 없고 **제품 번들에 실려 나간다**
>   (번들 각 1회 · 엔진 소스 5/9회). D-130이 「번들 미포함 외부 산출물」이라
>   개명표에서 뺐는데, 판Z(D-123)가 「끝그림 ④ 닫힘」이라 적은 것과 어긋난다.
>   KR-DEV는 odata 백엔드라 **동작에는 영향 없다.**
> - **`test-gates.mjs` ⑧ 카탈로그 4건이 이 머신에서 red** — 선재 · `core.autocrlf=true`로
>   워크트리가 CRLF인데 음성시험의 변형 문자열이 LF 전제라 변형이 아예 안 먹는다.
>   본 게이트는 CRLF를 정규화하도록 설계돼 있어 **초록이고 CI도 초록이다.**
> - ~~비추적 `engine/` 753MB가 제품 게이트를 red로 만든다~~ → **해소됐다**(2026-08-24
>   실측 — 그 디렉터리가 이 머신에 더 이상 없다). `check-runtime-path-rename.mjs`는
>   로컬에서도 exit 0이다.
> - **SAP 자격증명이 그룹 읽기에 열려 있다** — `~/.sapkit`에 명시 ACE
>   `HONGPC\CodexSandboxUsers:(OI)(CI)(RX)`가 걸려 `profiles/*/sap.env`까지 상속된다.
>   의도한 것이면 그대로, 아니면 그 ACE를 뺀다. **레포 밖 · 사용자 소유.**
>
> **⑻ 판B가 남긴 것 (2026-08-24 · D-135 유보 전문은 그쪽에)**
>
> - ~~`zsapkit_adt_flush_cache.abap`이 레포에서 깨져 있다~~ → **✅ 2026-08-24에 수리했다**
>   (D-136). D-134 ⓐ가 「동적 호출로 되돌렸다」고 적은 수리가 **파일의 엉뚱한 자리에
>   적용돼** 등록 처리 FORM을 덮어썼던 것이다. 깨지기 전 판(`98cec4f~1`)을 바탕으로
>   **의도한 변경만** 제자리(`purge_gateway_caches` 본문)에 넣어 재구성했다. 실측
>   `FORM/ENDFORM 8/8` · `IF/ENDIF 4/4` · `TRY/ENDTRY 8/8` · `PERFORM` 대상 8/8 실재 ·
>   동봉 검사기 lint **findings 0**. ⚠ **SAP에는 반영하지 않았다**(P3 · 별건 승인 필요).
>   같은 사고가 다른 자산에도 있는지 `sap-assets/*.abap` **전수 검사 → 0건**.
> - ~~죽은 참조 3건~~ → **2건으로 줄었다.** `sapkit-engine/ADDING-A-TOOL.md:51`은
>   **고쳤다**(문서라 번들 출처 해시 범위 밖이다). 남은 것은
>   `sapkit-engine/src/rfc/README.md:93,95`의 **줄 번호 인용 2건**뿐이고 **판B 이전부터
>   약 1,520줄 어긋나 있었다** — `src/`는 출처 해시 대상이라 주석 한 줄에 **재번들·재핀**이
>   따라붙는다. 그 경로를 어차피 고칠 판에서 함께 닫을 자리다.
> - ~~보관소 상대 링크 8곳~~ → **✅ 고쳤다**(`](reference/…)` → `](../…)`).
>   표시 문구는 그대로 두고 괄호 안 경로만 바꿨으며, 보관소 머리 안내에 **그 예외를
>   명시**했다(이사 시점 바이트 동일성은 git 이력이 든다).
>   ⚠ 여전히 `check-links.mjs`는 `interactive/`만 훑는다 — **`docs/`는 어떤 게이트도
>   링크를 재지 않는다.** 사정거리를 넓히는 것은 별건 후보다.
> - ~~`docs/ARCHITECTURE.md`의 안전 규칙 7종 축약 나열~~ → **✅ 걷어내고 포인터만 남겼다.**
>   실제로 그 요약이 먼저 낡았다(superpowers 항의 행선지가 바뀌자 거짓이 됐다).
> - **처방 1의 ⓑⓒⓓ**(runner-shell-guard · gate-lock · doc-contract-check)는 **후보 등재
>   유지** — 승격 요건(같은 사고 3회 또는 소유자 지시) 미달. 횟수 대장은 auto-memory다.
> - **설계서가 두 곳에 있다** — 원본(`D:\claude-practice\project-brain-design.md`)을
>   사용자가 유지하기로 했다. 어긋나면 **레포 편입본이 정본**이다.
> - **aegis(설계서 §4)는 별도 레포에서 적용됐다**(2026-08-24 사용자 확인) — 이 레포의
>   미결이 아니다. 범위·결과는 그쪽 기록이 든다.
>
> **⑼ 복원 시험이 찾은 남은 구멍 (2026-08-24 · 판B 검증 ③)**
>
> 수술에 참여하지 않은 새 컨텍스트에게 **이 문서 · `CLAUDE.md` · `docs/RUN-PLAN.md`
> 셋만** 읽히고 세 질문에 답하게 했다. 판정은 **① 정답 · ② 부분 · ③ 정답**이고
> 더듬은 지점 **21건**이 나왔다. 그중 **수치 드리프트 6건과 낡은 서술 2건은 그 자리에서
> 고쳤다**(버전을 CLAUDE.md에서 지우고 이 표를 가리키게 함 · 게이트 「9종」 표기 ·
> 줄 수 복제 · 프로파일 3종 vs 2종 · SAP 설치 수 9/10/12 · 판E1 행의 수리 표시).
> **남은 구멍은 정의 부재 쪽이고 여기 등재한다**:
>
> - **어휘 범례를 CLAUDE.md에 넣었다**(끝그림 ①~④ · 사다리 ⑴~⑷ · 판/dryforge ·
>   renew R1~R5). 시험자는 이 넷을 **세 문서로는 끝내 못 풀었다** — 특히 「끝그림
>   ①~④가 전부 닫혔다」로 시작하는 이 문서가 그 넷의 이름을 어디에도 안 적고 있었다.
> - ⚠ **아직 안 닫힌 것**: 제품이 **어떻게 배포·설치되는가**가 세 문서에 없다(시험자가
>   질문 ①에서 가장 크게 추측한 자리) · **D-번호 괄호 근거 30여 개**는 세 문서가 결론만
>   주고 「왜」는 append-only 로그에 있다(단권화의 구조적 한계 — 결함이 아니라 성질).
>   → **앞의 하나는 2026-08-25(판W)에 메워졌고, 그것을 이번 회차 복원 시험이 실측했다**
>   (아래 ⑽). 위키 페이지 「배포·설치의 변천사」가 그 자리다. **뒤의 하나는 그대로다** —
>   위키는 D-번호를 **주제로 묶어** 찾게 해 줄 뿐 로그를 대신하지 않는다.
> - ⚠ **`D-135` 본문과 판B 커밋 메시지의 「209줄 / 88줄」은 그 시점 이전 값이라 낡았다.**
>   그 뒤 재개점 갱신·리뷰 회수로 이 문서가 더 자랐다. `DECISIONS.md`는 **append-only라
>   제자리 정정이 불가**하므로 여기 적는다 — **현재 값의 정본은 `check-doc-size.mjs`의
>   출력**이고, 상한(500·300) 안이면 그 숫자 자체는 판단에 쓰이지 않는다.
> - **검증 한계 1건**: D-135 ⓑ의 「훅 파이썬 5파일 바이트 무변경(해시 대조)」은 **지금
>   기계로 재확인할 수 없다** — 편입 전 원본이 비추적이라 디스크에 남지 않았다. 커밋
>   구조가 받친다(`4b88787`이 `.claude/` 전부에 대해 순수 추가·삭제 0이고 수리는
>   `38fabfa`·`285ce91`로 분리) — 그러나 그 한 문장은 자기보고 의존이다.
> - ⚠ **관찰(원인 미상)**: 세션 시작 시 하네스가 주입한 `CLAUDE.md` 사본이 **디스크
>   실물보다 낡을 수 있다.** 시험자가 그 차이를 실측했다(주입분에는 판B 산물이 없었다).
>   파일을 직접 읽는 세션은 영향이 없지만, **주입분만 믿는 세션은 판B 이전 세계관으로
>   판단한다.** 레포가 고칠 수 있는 자리인지 불명 — 관찰만 남긴다.
>
> **⑽ 판W가 남긴 것 (2026-08-25 · D-137)**
>
> **판이 세운 것** — `docs/reference/wiki/` 합성 페이지 **7편** + 콜드 전수 색인
> `INDEX.md` · 위키 게이트 `scripts/check-wiki.mjs`와 그 음성시험 신설 · CI(`node-gates`)와
> 규약(`CLAUDE.md`·이 문서·`docs/RUN-PLAN.md`) 배선. 전 스위트 초록(로컬 실측) ·
> 독립 리뷰 **R-PASS · 차단 0 · 권고 10** · 인용된 D-번호 **48개** 원문 대조 **어긋남 0**.
>
> **복원 시험 — D-137 정직 유보 ③이 실측으로 바뀌었다.** 그 유보는 「효과(회수 개선)는
> 새-컨텍스트 복원 시험으로만 확인 가능하며 이 결정 시점에는 **미검증**」이었다. 이번
> 회차 판정은 **3/3 도달**이다 — 진입 3문서(`HANDOFF.md`·`CLAUDE.md`·`docs/RUN-PLAN.md`)만
> 주고 **위키를 직접 쥐여주지 않은** 채 세 질문(왜 엔진을 교체했나 · 왜 vsp가 은퇴했나 ·
> 왜 제품 훅이 기본 꺼짐인가)에 답하게 했고, 위키 서술과 결정 로그 원문의 어긋남
> **0건**이었다. 직전 회차(판B)에서 미결로 남았던 **「배포·설치가 세 문서에 없다」가
> 메워진 것**도 이 회차가 실측했다.
> ⚠ **정직 유보**: 채점자 자신이 「회수 성공의 상당 부분이 **아주 최근에 생긴 다리**
> (위키) 덕이고, 다리가 없던 어제였다면 판정이 달랐을 개연성이 높다」고 적었다 —
> **이 3/3은 위키의 효과를 잰 것이지 문서 기관 전반의 건강을 잰 것이 아니다.**
>
> **약한 고리 2건은 이 판에서 닫았다** — ⓐ 위키로 나가는 주제별 입구가 이 문서 **맨
> 끝줄**뿐이라 「재개점만 읽으면 복원된다」는 안내를 따르면 위키를 못 봤다 → 재개점 안에
> 입구를 넣었다(맨 아래 포인터는 그대로). ⓑ 검사기 은퇴(**D-085**)가 진입 3문서 어디에도
> **번호로** 없어 「'왜'는 DECISIONS.md」 포인터가 무력했다 → 판 큐의 판1·판2·판3.x 행에
> D-번호를 채웠다(원문 대조 후).
>
> **별건 후보 — 이 판이 닫지 않은 것**
>
> - ⓐ **현행 배포·설치의 해부**는 여전히 살아있는 문서(`docs/ARCHITECTURE.md` 등) 소관이다.
>   위키 페이지가 담은 것은 **불변 결정사**뿐이다(소유자 선택 갈래).
> - ⓑ **설계서 오귀속 3곳** — `docs/reference/designs/2026-08-23-project-brain-design.md`가
>   §3.0 표 ①·§3.2·부록에서 「HANDOFF 317KB · ready 입력 부적합」 진단을 **D-082 기각항**
>   으로 적으나 원문은 **D-084 대안 (c)**다(D-082의 (c)는 다른 주제). 리뷰어가 세 곳 모두
>   사실로 확인했다. 판W는 원본 무수정 계약이라 **위키 쪽에 ⚠ 표시만** 했다.
> - ⓒ **위키 게이트 강화 3건**(리뷰 권고 미채택분 — 전부 spec §1.2가 정의한 판정 범위
>   **밖**이라 채택하면 계약 변경이 된다): 판정 ④를 「두부·인용 블록·YAML frontmatter
>   금지」까지 기계화 · **단방향(원본에 역링크 금지)을 재는 판정 신설**(오늘 위반 0 실측) ·
>   ③ 정규식이 정확히 3자리라 `D-79` 오타와 장래 `D-1000`이 조용히 통과한다.
> - ⓓ **`check-links.mjs`의 `docs/` 전면 확대**(D-136 ③) — 판W의 게이트는 **위키 구역만**
>   잰다.
> - ⓔ **가장 먼저 낡을 문장 2곳**(부기 규약이 먼저 볼 자리):
>   `docs/reference/wiki/track-a-lifecycle.md`의 「오늘 트랙 A에 남은 것은 라우팅 규약 한
>   장이다」 · `docs/reference/wiki/checker-lineage.md`의 「코퍼스 대조 게이트가 그 기준을
>   상시 지킨다」. 둘 다 정본 포인터를 달고 있어 계약 위반은 아니다.
> - ⓕ **로컬 환경 실패 1건(판W와 무관)**: `node interactive/scripts/doctor.mjs`가
>   「Antigravity 설치 1.1.1 ≠ 고정 1.1.4」로 FAIL한다. 판W는 그 도구가 읽는 파일을 하나도
>   건드리지 않았고(`interactive/` **0바이트**), doctor는 **로컬 전용**이라 CI에서도 안 돈다.
>
> ## ✅ SAP 접속 — 살아 있다 (2026-08-24)
>
> 프로파일은 **2종**(IDES-DEV는 이 머신에 없다). 활성은 **KR-DEV**
> (`192.168.1.225:8000` · sap-system **DEV** · client 700 · `HJAEWON`).
> 2026-08-24 세션 초반에 401이었으나 **비밀번호 갱신으로 풀렸다** — MCP·ADT 모두 정상.
> IDEA-JNC는 이 세션에서 재시도하지 않았다(초반 401 기록만 있다).
>
> ⚠ **`~/.sapkit`에 명시 ACE `HONGPC\CodexSandboxUsers:(OI)(CI)(RX)`가 걸려 있어
> `profiles/*/sap.env`(평문 SAP 비밀번호)를 그 그룹이 읽을 수 있다.** 의도한 것이면
> 그대로, 아니면 그 ACE를 뺀다. 더 나은 길은 `SAP_PASSWORD=keychain:<service>/<account>`
> 참조(엔진이 지원한다 — `src/profile/secrets.ts`).
>
> ## ▶▶ 다음 작업 — 여기서 시작한다 (2026-08-25 판C 마감 시점)
>
> **판 큐는 비어 있다.** 2026-08-24부터 이 자리를 차지하던 「제품 사용자의 세션 간
> 연속성」은 **판C가 닫았다**(D-138 갈래 ⓐ → D-139 집행 · 위 「방금 닫힌 것」 절).
>
> **▶ 다음 한 걸음은 소유자 판단이다 — 밖으로 나가는 것이라 물어야 한다.**
> 브랜치 `dryforge/plate-c-user-continuity`가 **미푸시**로 서 있고, 판C 범위는
> `c62852a..HEAD`다. 셋 중 하나를 고른다:
>
> 1. **`main` 병합** — 그러면 CI가 처음으로 판W·판C의 새 스텝들을 러너에서 돌리고,
>    마켓플레이스 클론이 `main`을 따라가므로 **재설치로 유보 ②가 닫힌다**(0.9.0
>    설치 캐시 실측).
> 2. **푸시 + PR** — 병합 전에 CI만 먼저 본다.
> 3. **브랜치 유지** — 아무것도 내보내지 않는다.
>
> ⚠ **1·2 어느 쪽이든 CI가 실제로 도는 것은 그때가 처음이다** — 판W가 건 스텝 2종과
> 판C가 건 스텝 1종은 지금까지 **로컬 실측만** 있다(워크플로 트리거가 main 푸시·PR이라
> feature 브랜치 단독으로는 안 돈다).
>
> **병합 뒤에 바로 할 것 하나** — 설치 캐시를 0.9.0으로 갱신하고
> `node interactive/scripts/doctor.mjs` ③이 「설치본 = 레포」로 돌아오는지, 그리고
> 새 자산 7파일이 캐시에 실렸는지 확인한다. 그것이 D-139 유보 ①의 나머지 절반이다.
>
> ## 🧭 그 밖에 하면 좋은 것 (해야 하는 것은 없다)
>
> 0. **수리한 SAP 자산을 실제로 반영할지 정하기** — `zsapkit_adt_flush_cache.abap`은
>    레포에서 고쳤지만 **SAP에는 안 올렸다**(P3 · 건별 승인). 지금 SAP에 설치된 사본이
>    어느 판인지 확인한 뒤 결정할 자리다.
> 1. **재생 대조 18종** — ⑸의 살아 있는 갈래. 판E1이 진단한 오프라인 결함 2건은
>    2026-08-24에 수리됐고 **SAP 접속도 살아 있으니 이제 실제로 칠 수 있다**(반나절).
>    ⚠ **갈래가 둘이다** — 실기 녹화는 SAP가 필요하지만, `attended-only/` 픽스처를
>    `--fixture`로 직접 재생하는 갈래는 **코드 변경 0 · 오프라인**이다(RUN-PLAN 말미).
> 2. **구 오브젝트 정리** — ⑶. 전제가 풀렸다. 위 ⑶의 갈래별 처방을 따른다.
> 3. **write 경로 실증** — `CUA_WRITE`·`DYNPRO_INSERT`·`*_DELETE`. P3라 연습 대상을
>    새로 만들어야 한다(`$TMP`). D-134 유보 ②가 그 자리다.
> 4. **동봉 FM 자산의 헤더 형식** — 레포 `sap-assets/*.abap`은 구식 `*"` 인터페이스
>    주석으로 시작하는데 `UpdateFunctionModule`이 그것을 **HTTP 400**으로 거부한다
>    (핸들러가 「트랜스포트 문제」로 오진해 원인을 가린다). 자산을 고칠지 핸들러를
>    고칠지는 정해지지 않았다. D-134 유보 ③.
> 5. **체크박스 35줄 재저작의 독립 리뷰** — 2026-08-24에 재저작만 하고 리뷰는
>    돌리지 않았다. `copy-baseline.md` §2가 커밋 전 새-컨텍스트 리뷰를 요구한다.
>
> **아무것도 안 해도 제품은 그대로 돈다** — 단 맨 위 「다음 작업」은 **밖으로 나가는
> 선택**(푸시·PR·병합)이라 소유자가 정할 자리다.

---

## 8. 불변 규칙 — 정본은 `CLAUDE.md` 「안전 규칙」 절이다

여기 있던 **불변 규칙 7개 항**은 2026-08-24(판B)에 **`CLAUDE.md`의 「안전 규칙」 절로
병합**됐다. 이사가 아니라 **단권화**다 — 같은 규칙이 두 곳에 있으면 언젠가 갈리고,
갈린 뒤에는 어느 쪽이 정본인지 아무도 모른다. 항별 병합 내역(채택 문안 · 버린 문구와
사유)은 그 수술 커밋 메시지에 있다.

- 안전·불변 규칙 → `CLAUDE.md` 「안전 규칙」 절
- 문서 갱신 의무 → `CLAUDE.md` 「문서 계약」 절
- 게이트 명령 목록 → `CLAUDE.md` 「게이트」 절

「HANDOFF §8-4」처럼 **하위 번호로 걸린 과거 인용**은 이 포인터를 거쳐 그 절로 이어진다.

## 과거 기록은 보관소에 있다 (§ 번호 보존)

재개점 아래에 쌓여 있던 **4,607줄** — 완료 판 기록 블록 전량(판M2-c ~ S0 시대 · 🔙 롤백
좌표 · 사다리 진척표) · `보조 머신 줄기` 절 · 구 본문 **§1~§7 · §9** — 은 2026-08-24에
**`docs/reference/handoff-archive/HANDOFF-archive-2026-08.md`**로 옮겨졌다. **한 글자도
바뀌지 않았고 § 번호·순서·구조가 그대로다.**

그러므로 「HANDOFF §5-11」·「HANDOFF §6 13-9」·「HANDOFF 🔙 절」 같은 **과거 인용은 거기서
그대로 풀린다.** `docs/reference/DECISIONS.md`가 이 문서를 157곳에서 인용하고 그중 30곳이
§ 번호를 직접 가리키는데 그 파일은 **append-only라 고칠 수 없다** — 번호 보존이 그
인용들을 살려 두는 장치다.

⚠ **보관소는 기록이므로 갱신하지 않는다.** 살아있는 정본은 이 문서의 재개점 ·
`CLAUDE.md`(안전 규칙·게이트·문서 계약) · `docs/RUN-PLAN.md`(판 큐)다.

**콜드 층을 주제로 묶은 지도는 `docs/reference/wiki/INDEX.md`다**(판W · D-137) — 보관소·결정
로그·감사 기록으로 들어가는 입구이고, 정본이 아니라 **재생성 가능한 파생물**이다.
