# 의존성 — 워크트리 공유 링크(심볼릭/정션)도 잡히도록 슬래시 없이 유지한다.
# 트레일링 슬래시 패턴은 링크를 매치하지 못해 `git add`에 딸려 들어간다.
node_modules

# 빌드 산출물 — **루트 앵커**가 필수다. 앵커 없는 `coverage`는 어느 깊이의 폴더든
# 삼켜서, 픽스처의 분류 커버리지 폴더가 `git add`에서 조용히 빠졌다(실증).
/dist
/coverage

# 접속 정보 — 절대 커밋 금지 (CLAUDE.md 안전 규칙)
.env
*.env
